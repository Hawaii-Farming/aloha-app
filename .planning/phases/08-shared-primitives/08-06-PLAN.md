---
plan_id: 08-06
phase: 8
plan: 6
type: execute
wave: 3
depends_on: []
files_modified:
  - packages/ui/src/shadcn/sheet.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-06]
must_haves:
  truths:
    - "sheetVariants base surface is `bg-card` (was `bg-background`) with `shadow-xl` (was `shadow-lg`)"
    - "Per-side variants get leading-corner radius only: right=`rounded-l-2xl`, left=`rounded-r-2xl`, top=`rounded-b-2xl`, bottom=`rounded-t-2xl`"
    - "All four sides have `border-border` on the edge facing the viewport (border-l/r/t/b)"
    - "SheetHeader = `flex flex-col gap-y-4 pb-4 border-b border-border text-center sm:text-left`"
    - "SheetFooter = `flex flex-col-reverse pt-4 border-t border-border sm:flex-row sm:justify-end sm:space-x-2`"
    - "SheetTitle bumps to `text-lg font-semibold text-foreground`"
    - "SheetOverlay UNCHANGED — retains `bg-glass-surface`"
    - "pnpm typecheck and pnpm lint pass"
  artifacts:
    - path: packages/ui/src/shadcn/sheet.tsx
      provides: "Side sheet with leading-corner radius, card surface, form-field spacing"
      contains: "rounded-l-2xl"
  key_links:
    - from: packages/ui/src/shadcn/sheet.tsx
      to: "app/components/crud/create-panel.tsx + edit-panel.tsx (6 SheetContent call sites)"
      via: "cn() + tailwind-merge dedupe existing border-b/pb-4 overrides cleanly"
      pattern: "bg-card.*shadow-xl"
---

<objective>
Restyle the Sheet primitive (PRIM-06): swap surface to `bg-card`, deepen shadow to `shadow-xl`, add leading-corner `rounded-l-2xl` on right-side sheets (and mirrors for the other three sides), and add form-field spacing to SheetHeader/SheetFooter with border dividers. Keep SheetOverlay untouched.

Purpose: Sheet is the CRUD create/edit container — PRIM-06's form-field spacing and leading-corner radius are what makes create panels feel like the Aloha prototype. `cn()` + `tailwind-merge` deduplicate the existing caller overrides in `create-panel.tsx` / `edit-panel.tsx` cleanly (confirmed via RESEARCH caller audit).
Output: One edited file; no prop contract changes; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle sheetVariants cva, SheetHeader, SheetFooter, SheetTitle</name>
  <files>packages/ui/src/shadcn/sheet.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/sheet.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-14, D-15, D-16)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"8. sheet.tsx (PRIM-06)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Sheet recipe + caller audit for create-panel.tsx)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/SlidePanel.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/sheet.tsx`, make the following targeted edits. Do NOT touch imports, Radix primitive wiring, forwardRef types, displayName, exports, `Sheet`, `SheetTrigger`, `SheetClose`, `SheetPortal`, `SheetOverlay`, or `SheetDescription` JSX bodies.

    **1. Rewrite the `sheetVariants` cva base string** to EXACTLY:
       `'bg-card fixed z-50 gap-4 p-6 shadow-xl transition ease-in-out data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=closed]:duration-300 data-[state=open]:duration-500'`

       Deltas vs current: `bg-background → bg-card`, `shadow-lg → shadow-xl`. Preserve positioning, gap, padding, animation utilities.

    **2. Rewrite the four `side` variants** to EXACTLY these strings:

       top: `'inset-x-0 top-0 border-b border-border rounded-b-2xl data-[state=closed]:slide-out-to-top data-[state=open]:slide-in-from-top'`

       bottom: `'inset-x-0 bottom-0 border-t border-border rounded-t-2xl data-[state=closed]:slide-out-to-bottom data-[state=open]:slide-in-from-bottom'`

       left: `'inset-y-0 left-0 h-full w-3/4 border-r border-border rounded-r-2xl data-[state=closed]:slide-out-to-left data-[state=open]:slide-in-from-left sm:max-w-sm'`

       right: `'inset-y-0 right-0 h-full w-3/4 border-l border-border rounded-l-2xl data-[state=closed]:slide-out-to-right data-[state=open]:slide-in-from-right sm:max-w-sm'`

       Note: leading-corner only — `rounded-l-2xl` on right (corner facing viewport), `rounded-r-2xl` on left, `rounded-b-2xl` on top, `rounded-t-2xl` on bottom. Preserve `sm:max-w-sm` on left/right (do not widen — defer width tuning per CONTEXT risk flags).

       `defaultVariants` stays `{ side: 'right' }`.

    **3. Rewrite `SheetHeader` className** to EXACTLY:
       `'flex flex-col gap-y-4 pb-4 border-b border-border text-center sm:text-left'`

       Delta: `gap-y-3 → gap-y-4`, add `pb-4 border-b border-border`.

    **4. Rewrite `SheetFooter` className** to EXACTLY:
       `'flex flex-col-reverse pt-4 border-t border-border sm:flex-row sm:justify-end sm:space-x-2'`

       Delta: add `pt-4 border-t border-border`.

    **5. Rewrite `SheetTitle` className** to EXACTLY:
       `'text-lg font-semibold text-foreground'`

       Delta: bump from current `text-foreground font-medium` (or similar) to `text-lg font-semibold text-foreground`.

    **6. `SheetOverlay` — UNCHANGED.** Per D-16, retains its existing `bg-glass-surface` (or whatever class it currently has). Do not touch it.

    **7. `SheetDescription` — UNCHANGED.** Stays `text-sm text-muted-foreground`.

    No `dark:` overrides. No hardcoded slate/gray/white/black classes. Preserve every other line of the file including the `SheetContent` JSX structure (with its `SheetPortal`, `SheetOverlay`, `SheetPrimitive.Content`, and close button).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "bg-card" packages/ui/src/shadcn/sheet.tsx` → match (base)
    - `grep -q "shadow-xl" packages/ui/src/shadcn/sheet.tsx` → match (base)
    - `grep -q "rounded-l-2xl" packages/ui/src/shadcn/sheet.tsx` → match (right side — CRUD pattern)
    - `grep -q "rounded-r-2xl" packages/ui/src/shadcn/sheet.tsx` → match (left side)
    - `grep -q "rounded-b-2xl" packages/ui/src/shadcn/sheet.tsx` → match (top side)
    - `grep -q "rounded-t-2xl" packages/ui/src/shadcn/sheet.tsx` → match (bottom side)
    - `grep -q "border-l border-border" packages/ui/src/shadcn/sheet.tsx` → match (right side)
    - `grep -q "border-r border-border" packages/ui/src/shadcn/sheet.tsx` → match (left side)
    - `grep -q "gap-y-4 pb-4 border-b border-border" packages/ui/src/shadcn/sheet.tsx` → match (SheetHeader)
    - `grep -q "pt-4 border-t border-border" packages/ui/src/shadcn/sheet.tsx` → match (SheetFooter)
    - `grep -q "text-lg font-semibold text-foreground" packages/ui/src/shadcn/sheet.tsx` → match (SheetTitle)
    - `grep -q "sm:max-w-sm" packages/ui/src/shadcn/sheet.tsx` → match (preserved width)
    - `grep -q "bg-glass-surface" packages/ui/src/shadcn/sheet.tsx` → match (SheetOverlay untouched — D-16)
    - `grep -q "shadow-lg" packages/ui/src/shadcn/sheet.tsx` → no match (old shadow removed)
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/sheet.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/sheet.tsx` → no matches
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    sheetVariants + 4 side variants + SheetHeader + SheetFooter + SheetTitle all updated to 08-UI-SPEC §8 strings; SheetOverlay + SheetDescription untouched; typecheck+lint green; existing CRUD create-panel.tsx / edit-panel.tsx callers compatible via `cn()` dedupe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on a presentational container primitive.

## STRIDE Threat Register

No new threat surface — className-only edit on a visual container primitive. No auth, data handling, input parsing, or network surface affected. Radix UI focus-trap, escape-dismissal, and ARIA wiring are fully preserved (no JSX body changes). Existing threat model for the Sheet primitive (none) unchanged.
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green
- All grep acceptance criteria pass (critically: `rounded-l-2xl`, `bg-card`, `shadow-xl`, `gap-y-4 pb-4 border-b border-border`, `bg-glass-surface` still present)
- Manual smoke (phase end): open CRUD create sheet from any HR list route — leading-corner radius visible on viewport-facing edge, card surface, deeper shadow, header/footer dividers visible
</verification>

<success_criteria>
- PRIM-06 fully satisfied per 08-UI-SPEC §8
- All four sides get correct leading-corner radius (right/left/top/bottom)
- SheetOverlay intentionally untouched (D-16)
- Single commit `feat(08-06): sheet Aloha restyle` after typecheck+lint pass
- Zero prop contract changes; CRUD panel callers remain byte-compatible via `cn()` dedupe
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-06-SUMMARY.md` capturing: file changed, 4-side variant diff table, header/footer/title diffs, overlay untouched note, grep results, typecheck+lint status.
</output>
