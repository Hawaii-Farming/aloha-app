---
plan_id: 08-01
phase: 8
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/shadcn/card.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-02]
must_haves:
  truths:
    - "Card primitive renders rounded-2xl border border-border bg-card text-card-foreground shadow-sm"
    - "All existing Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter exports preserved with identical prop contracts"
    - "pnpm typecheck and pnpm lint pass after edit"
    - "No hardcoded slate/gray/white/black classes introduced"
  artifacts:
    - path: packages/ui/src/shadcn/card.tsx
      provides: "Restyled Card primitive matching Aloha Supabase-inspired spec"
      contains: "rounded-2xl border border-border bg-card text-card-foreground shadow-sm"
  key_links:
    - from: packages/ui/src/shadcn/card.tsx
      to: app/styles/theme.css
      via: "Tailwind semantic tokens (bg-card, border-border, text-card-foreground)"
      pattern: "bg-card.*border-border.*shadow-sm"
---

<objective>
Restyle the Card primitive (PRIM-02) to match the Aloha Supabase-inspired spec: rounded-2xl hero radius, explicit border-border, soft shadow-sm. Wave 1 — independent surface primitive with no dependencies.

Purpose: Card is the most widely consumed surface primitive in the app (list views, dashboards). Restyling it in Wave 1 unlocks visual consistency for everything downstream.
Output: One edited file; no prop contract changes; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Card root with rounded-2xl + shadow-sm</name>
  <files>packages/ui/src/shadcn/card.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/card.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-13 decision anchor)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"2. card.tsx (PRIM-02)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Card recipe + caller audit)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/Card.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/card.tsx`, change ONLY the root `Card` forwardRef className. Replace the current:
      `'bg-card text-card-foreground rounded-lg border'`
    with exactly:
      `'bg-card text-card-foreground rounded-2xl border border-border shadow-sm'`

    Do NOT touch `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter` — leave every sub-component className byte-identical. Do NOT change imports, exports, forwardRef types, or displayName assignments. Do NOT add new variants, props, or cva blocks.

    No prop contract changes. No new imports. Preserve the existing `cn()` call pattern and `className` prop pass-through.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "bg-card text-card-foreground rounded-2xl border border-border shadow-sm" packages/ui/src/shadcn/card.tsx` → match
    - `grep -q "rounded-2xl" packages/ui/src/shadcn/card.tsx` → match
    - `grep -q "shadow-sm" packages/ui/src/shadcn/card.tsx` → match
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/card.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/card.tsx` → no matches
    - `grep -c "rounded-lg border'" packages/ui/src/shadcn/card.tsx` → 0 (old string removed)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    Card root renders with rounded-2xl + shadow-sm + explicit border-border; all sub-components untouched; typecheck+lint green; no dark: overrides introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on a presentational primitive with no data, network, or auth surface.

## STRIDE Threat Register

No new threat surface — className-only edit on a visual primitive. No auth, data handling, input parsing, or network surface affected. Existing threat model for the Card primitive (none) is unchanged. Phase 8 introduces zero new attack surface by design (CONTEXT D-01 scope guardrail).
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green
- Grep acceptance criteria above all pass
- Manual smoke (deferred to phase end): Card surface renders correctly in light + dark on any HR module list route
</verification>

<success_criteria>
- PRIM-02 requirement fully satisfied per 08-UI-SPEC §2
- Single commit `feat(08-01): card Aloha restyle` after typecheck+lint pass
- Zero consumer breakage (no prop contract changes)
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-01-SUMMARY.md` capturing: file changed, exact class string diff, grep verification results, typecheck+lint status.
</output>
