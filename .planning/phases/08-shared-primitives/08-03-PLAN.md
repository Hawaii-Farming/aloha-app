---
plan_id: 08-03
phase: 8
plan: 3
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/ui/src/shadcn/button.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-01]
must_haves:
  truths:
    - "Button `default` variant renders `bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25 hover:shadow-xl`"
    - "Button `brand` and `pill` variants retuned to the same gradient recipe (3 total gradient occurrences)"
    - "Button `secondary` = `bg-background text-foreground border border-border hover:bg-muted`"
    - "Button base uses new focus recipe: `ring-2 ring-primary ring-offset-2 ring-offset-background` (§9.1 Option A)"
    - "Button `default` size = `px-5 py-3` (h-9 removed — height is padding-driven); rounded-2xl base"
    - "All 8 existing variants preserved (default, destructive, outline, secondary, ghost, link, pill, brand)"
    - "pnpm typecheck and pnpm lint pass"
  artifacts:
    - path: packages/ui/src/shadcn/button.tsx
      provides: "Button primitive with Aloha gradient primary + generous padding + new focus ring"
      contains: "from-green-500 to-emerald-600"
  key_links:
    - from: packages/ui/src/shadcn/button.tsx
      to: "40 call sites across 18 files"
      via: "variant='default' consumers inherit gradient automatically"
      pattern: "bg-gradient-to-r from-green-500 to-emerald-600"
---

<objective>
Restyle the Button primitive (PRIM-01) to the Aloha brand gradient primary with rounded-2xl, generous py-3 padding, and the §9.1 Option A focus ring recipe. Retune `brand` and `pill` to the same gradient. Preserve all 8 existing variants, sizes, and prop contracts.

Purpose: Button is the single most visible brand element. The gradient primary is the signature Aloha look. Wave 2 runs Button + Avatar in parallel because they are the gradient pair.
Output: One edited file; no new variants; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite buttonVariants cva — base, variants, sizes</name>
  <files>packages/ui/src/shadcn/button.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/button.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-03, D-04, D-05, D-07, D-12)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"1. button.tsx (PRIM-01)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Button recipe + §9.1 Option A focus remediation)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/Button.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/button.tsx`, rewrite ONLY the `buttonVariants` cva call. Keep imports, `Slot` usage, `ButtonProps` interface, forwardRef, displayName, and the component body byte-identical.

    The new base string is EXACTLY:
      `'inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-2xl text-sm font-medium transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:pointer-events-none disabled:opacity-50'`

    Variants (`variant` key) MUST contain exactly these 8 entries with these exact class strings:

      default: 'bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25 hover:shadow-xl'
      destructive: 'bg-destructive text-destructive-foreground hover:bg-destructive/90'
      outline: 'border border-border bg-background text-foreground hover:bg-muted'
      secondary: 'bg-background text-foreground border border-border hover:bg-muted'
      ghost: 'hover:bg-accent hover:text-accent-foreground'
      link: 'text-primary underline-offset-4 hover:underline'
      pill: 'rounded-full bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25 hover:shadow-xl px-8 py-2 font-medium'
      brand: 'bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25 hover:shadow-xl'

    Sizes (`size` key) MUST contain exactly these 4 entries with these exact class strings (note: `default` drops the old `h-9` entirely — height is padding-driven):

      default: 'px-5 py-3'
      sm: 'px-3 py-1.5 rounded-xl text-xs'
      lg: 'px-6 py-3 rounded-2xl text-sm'
      icon: 'h-10 w-10 rounded-2xl'

    `defaultVariants` stays `{ variant: 'default', size: 'default' }`.

    Do NOT change: imports, `asChild` Slot logic, forwardRef types, `ButtonProps` interface, displayName, exports. Do NOT introduce hardcoded slate/gray/white/black classes (the `text-white` inside gradient variants is the documented brand-literal exception per D-11 and 08-UI-SPEC §Color). Do NOT add `dark:` overrides anywhere.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from-green-500 to-emerald-600" packages/ui/src/shadcn/button.tsx` → exactly 3 (default + pill + brand)
    - `grep -q "shadow-lg shadow-green-500/25 hover:shadow-xl" packages/ui/src/shadcn/button.tsx` → match
    - `grep -q "ring-2 ring-primary ring-offset-2 ring-offset-background" packages/ui/src/shadcn/button.tsx` → match
    - `grep -q "rounded-2xl text-sm font-medium transition-all" packages/ui/src/shadcn/button.tsx` → match
    - `grep -q "px-5 py-3" packages/ui/src/shadcn/button.tsx` → match (default size)
    - `grep -q "px-3 py-1.5 rounded-xl text-xs" packages/ui/src/shadcn/button.tsx` → match (sm size)
    - `grep -q "px-6 py-3 rounded-2xl text-sm" packages/ui/src/shadcn/button.tsx` → match (lg size)
    - `grep -q "h-10 w-10 rounded-2xl" packages/ui/src/shadcn/button.tsx` → match (icon size)
    - `grep -q "bg-background text-foreground border border-border hover:bg-muted" packages/ui/src/shadcn/button.tsx` → match (secondary)
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-gray-|text-gray-" packages/ui/src/shadcn/button.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/button.tsx` → no matches
    - `grep -q "h-9" packages/ui/src/shadcn/button.tsx` → no match (old default height removed)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    Button renders gradient primary with new focus ring recipe; all 8 variants and 4 sizes preserved with exact class strings from 08-UI-SPEC §1; typecheck+lint green; zero prop contract changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on a presentational primitive.

## STRIDE Threat Register

No new threat surface — className-only edit on a visual primitive. No auth, data handling, input parsing, or network surface affected. Existing threat model for the Button primitive (none) unchanged. The `text-white` on gradient is a brand-literal exception documented in CONTEXT D-11 and UI-SPEC §Color; no security implication.
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green
- All grep acceptance criteria pass (critically: gradient count = 3, focus recipe present, h-9 removed)
- Manual smoke (phase end): `/auth/sign-in` primary button renders green gradient with visible green shadow in both themes; tab to focus shows ring-offset halo
</verification>

<success_criteria>
- PRIM-01 fully satisfied per 08-UI-SPEC §1
- §9.1 Option A focus ring remediation applied (base string)
- Single commit `feat(08-03): button Aloha restyle` after typecheck+lint pass
- All 40 Button call sites across 18 files inherit gradient primary automatically (zero external edits)
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-03-SUMMARY.md` capturing: file changed, variant+size diff tables, grep results (especially the gradient-count=3 check), typecheck+lint status.
</output>
