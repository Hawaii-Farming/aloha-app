---
plan_id: 08-05
phase: 8
plan: 5
type: execute
wave: 3
depends_on: []
files_modified:
  - packages/ui/src/shadcn/input.tsx
  - packages/ui/src/shadcn/textarea.tsx
  - packages/ui/src/shadcn/select.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-03]
must_haves:
  truths:
    - "Input, Textarea, and Select trigger all render `rounded-2xl border border-border bg-background px-4 py-3 text-base text-foreground placeholder:text-muted-foreground`"
    - "All three use the new focus recipe: `ring-2 ring-primary ring-offset-2 ring-offset-background` (§9.1 Option A)"
    - "Fixed `h-9` removed from all three — height is padding-driven (taller touch target, ~48px)"
    - "Textarea bumps `min-h-[60px]` → `min-h-[80px]`"
    - "SelectContent, SelectItem, SelectLabel, SelectSeparator, SelectScrollUpButton, SelectScrollDownButton — UNCHANGED (inherit popover tokens from Phase 7)"
    - "pnpm typecheck and pnpm lint pass"
  artifacts:
    - path: packages/ui/src/shadcn/input.tsx
      provides: "Form input with Aloha sizing + green focus halo"
      contains: "rounded-2xl border border-border bg-background px-4 py-3 text-base"
    - path: packages/ui/src/shadcn/textarea.tsx
      provides: "Form textarea with Aloha sizing + green focus halo"
      contains: "min-h-[80px]"
    - path: packages/ui/src/shadcn/select.tsx
      provides: "Form select trigger with Aloha sizing + green focus halo"
      contains: "rounded-2xl border border-border bg-background px-4 py-3 text-base"
  key_links:
    - from: packages/ui/src/shadcn/{input,textarea,select}.tsx
      to: "FormField consumers across all CRUD create/edit sheets"
      via: "FormControl wraps primitives; FormFieldGrid auto-sizes cells to accommodate taller intrinsic height"
      pattern: "ring-2 ring-primary ring-offset-2 ring-offset-background"
---

<objective>
Restyle the three form primitives (PRIM-03) with a shared recipe: 16px text, rounded-2xl, py-3 padding, green-500 focus ring with offset halo (§9.1 Option A remediation). Three files, one recipe, one plan. Preserve all prop contracts and all Select sub-components.

Purpose: Form primitives drive every CRUD create/edit sheet. 16px text prevents iOS zoom-on-focus; py-3 hits 48px touch target; the offset-halo focus recipe is the §9.1 #11 blocker remediation.
Output: Three edited files; no prop contract changes; one commit after all three pass typecheck+lint together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/input.tsx
@packages/ui/src/shadcn/textarea.tsx
@packages/ui/src/shadcn/select.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Input root className</name>
  <files>packages/ui/src/shadcn/input.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/input.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-08, D-11)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"3. input.tsx (PRIM-03)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Input recipe + §9.1 Option A)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/FormField.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/input.tsx`, replace ONLY the root className string passed to `cn()` with EXACTLY:
      `'flex w-full rounded-2xl border border-border bg-background px-4 py-3 text-base text-foreground placeholder:text-muted-foreground transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50'`

    Explicit deltas from the prior string:
    - `border-input` → `border-border`
    - remove `h-9` entirely (height is now padding-driven)
    - `px-3 py-1` → `px-4 py-3`
    - `text-sm` → `text-base`
    - `rounded-md` → `rounded-2xl`
    - `bg-transparent` → `bg-background`
    - old `ring-1 ring-ring` / `focus-visible:ring-*` → `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background`
    - PRESERVE all `file:*` utility classes verbatim
    - PRESERVE `disabled:cursor-not-allowed disabled:opacity-50`

    Do NOT change: imports, forwardRef types, `InputProps`, displayName, `type` prop handling, or exports. Do NOT add `dark:` overrides. Do NOT introduce hardcoded slate/gray/white/black classes.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rounded-2xl border border-border bg-background px-4 py-3 text-base" packages/ui/src/shadcn/input.tsx` → match
    - `grep -q "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background" packages/ui/src/shadcn/input.tsx` → match
    - `grep -q "placeholder:text-muted-foreground" packages/ui/src/shadcn/input.tsx` → match
    - `grep -q "file:border-0 file:bg-transparent file:text-sm file:font-medium" packages/ui/src/shadcn/input.tsx` → match (preserved)
    - `grep -q " h-9" packages/ui/src/shadcn/input.tsx` → no match
    - `grep -q "border-input" packages/ui/src/shadcn/input.tsx` → no match
    - `grep -q "rounded-md" packages/ui/src/shadcn/input.tsx` → no match
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/input.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/input.tsx` → no matches
  </acceptance_criteria>
  <done>
    Input renders with new recipe; all file:* classes preserved; h-9 removed; typecheck+lint green (verified at end of plan).
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle Textarea root className</name>
  <files>packages/ui/src/shadcn/textarea.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/textarea.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-08)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"4. textarea.tsx (PRIM-03)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Textarea recipe — identical to Input except min-h)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/FormField.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/textarea.tsx`, replace ONLY the root className string passed to `cn()` with EXACTLY:
      `'flex min-h-[80px] w-full rounded-2xl border border-border bg-background px-4 py-3 text-base text-foreground placeholder:text-muted-foreground transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50'`

    Explicit deltas:
    - `min-h-[60px]` → `min-h-[80px]` (3-row default with taller padding)
    - identical rest-of-recipe to Input (border, bg, padding, text, ring, placeholder, disabled)
    - PRESERVE resize-handle native behavior (do not add resize classes)

    Do NOT change: imports, forwardRef types, `TextareaProps`, displayName, or exports. No `dark:` overrides. No hardcoded slate/gray/white/black.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "min-h-\[80px\]" packages/ui/src/shadcn/textarea.tsx` → match
    - `grep -q "rounded-2xl border border-border bg-background px-4 py-3 text-base" packages/ui/src/shadcn/textarea.tsx` → match
    - `grep -q "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background" packages/ui/src/shadcn/textarea.tsx` → match
    - `grep -q "min-h-\[60px\]" packages/ui/src/shadcn/textarea.tsx` → no match (old value removed)
    - `grep -q " h-9" packages/ui/src/shadcn/textarea.tsx` → no match
    - `grep -q "border-input" packages/ui/src/shadcn/textarea.tsx` → no match
    - `grep -q "rounded-md" packages/ui/src/shadcn/textarea.tsx` → no match
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/textarea.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/textarea.tsx` → no matches
  </acceptance_criteria>
  <done>
    Textarea renders with shared recipe; min-h-[80px] set; typecheck+lint green (verified at end of plan).
  </done>
</task>

<task type="auto">
  <name>Task 3: Restyle SelectTrigger root className + run combined typecheck/lint</name>
  <files>packages/ui/src/shadcn/select.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/select.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-08)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"5. select.tsx (PRIM-03)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Select recipe — trigger only)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/FormField.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/select.tsx`, modify ONLY the `SelectTrigger` forwardRef className. Replace the current className string passed to `cn()` with EXACTLY:
      `'flex w-full items-center justify-between rounded-2xl border border-border bg-background px-4 py-3 text-base text-foreground placeholder:text-muted-foreground transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background disabled:cursor-not-allowed disabled:opacity-50 [&>span]:line-clamp-1'`

    Explicit deltas:
    - Drop `h-9`
    - `border-input` → `border-border`
    - `px-3 py-1`/`py-2` → `px-4 py-3`
    - `text-sm` → `text-base`
    - `rounded-md` → `rounded-2xl`
    - Old focus/ring classes → `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background`
    - PRESERVE `[&>span]:line-clamp-1`
    - PRESERVE the `CaretSortIcon` child markup (inside the trigger — do NOT touch the JSX body)

    **LEAVE ENTIRELY UNCHANGED:** `SelectContent`, `SelectItem`, `SelectLabel`, `SelectSeparator`, `SelectScrollUpButton`, `SelectScrollDownButton`, `SelectValue`, `Select`, `SelectGroup` — these inherit popover tokens from Phase 7 and are explicitly out of scope per 08-UI-SPEC §5. Do NOT change any Radix imports, forwardRef types, displayName assignments, or exports.

    No `dark:` overrides. No hardcoded slate/gray/white/black classes.

    After this edit, run `pnpm typecheck && pnpm lint` once — this validates all three files in Plan 08-05 together.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rounded-2xl border border-border bg-background px-4 py-3 text-base" packages/ui/src/shadcn/select.tsx` → match
    - `grep -q "focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background" packages/ui/src/shadcn/select.tsx` → match
    - `grep -q "\[&>span\]:line-clamp-1" packages/ui/src/shadcn/select.tsx` → match (preserved)
    - `grep -q " h-9" packages/ui/src/shadcn/select.tsx` → no match
    - `grep -q "border-input" packages/ui/src/shadcn/select.tsx` → no match (trigger updated)
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/select.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/select.tsx` → no matches
    - `pnpm typecheck` exits 0 (for input.tsx + textarea.tsx + select.tsx combined)
    - `pnpm lint` exits 0 (for input.tsx + textarea.tsx + select.tsx combined)
  </acceptance_criteria>
  <done>
    SelectTrigger renders with shared recipe; all Select sub-components untouched; combined typecheck+lint across all three PRIM-03 files is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on presentational form primitives.

## STRIDE Threat Register

No new threat surface — className-only edits on visual primitives. No auth, data handling, validation logic, input parsing, or network surface affected. These primitives receive user input but do NOT parse, validate, or transmit it — validation remains the responsibility of upstream Zod schemas (`.schema.ts` files) and server actions. Existing threat model unchanged. Note: the §9.1 Option A focus ring remediation is an accessibility fix (WCAG 2.2 AA), not a security mitigation.
</threat_model>

<verification>
- `pnpm typecheck` green for the combined diff across input.tsx + textarea.tsx + select.tsx
- `pnpm lint` green for the combined diff
- All grep acceptance criteria pass across all three files
- Manual smoke (phase end): tab through `/auth/sign-in` email+password fields — green focus ring with offset halo is clearly visible in both themes; CRUD create sheet form fields render at ~48px height with 16px text
</verification>

<success_criteria>
- PRIM-03 fully satisfied per 08-UI-SPEC §3, §4, §5
- §9.1 Option A focus ring remediation applied to all three form primitives
- Single commit `feat(08-05): input/textarea/select Aloha restyle` after all three files pass typecheck+lint together
- Zero changes to Select sub-components (content/item/label/separator/scroll buttons)
- Zero prop contract changes
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-05-SUMMARY.md` capturing: three files changed, shared-recipe diff, Select sub-component untouched list, grep results, combined typecheck+lint status.
</output>
