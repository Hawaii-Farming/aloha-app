---
phase: 07-design-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
  - app/styles/shadcn-ui.css
autonomous: true
requirements: [DESIGN-01, DESIGN-02, DESIGN-04, DARK-01]
must_haves:
  truths:
    - "DESIGN.md is the Aloha theme spec (no Supabase references) and documents Inter, slate, green-500→emerald-600 gradient, rounded-2xl, soft shadow scale, and hand-authored dark palette."
    - "app/styles/shadcn-ui.css :root block uses Aloha light palette values (slate/green hex) with --radius: 1rem."
    - "app/styles/shadcn-ui.css .dark block uses hand-authored Aloha dark palette (slate-900/800/700, green-400/emerald-500 primary)."
    - "A --gradient-primary token is exposed for Phase 8 consumption without overwriting --primary."
    - "Semantic tokens (red/amber/green/blue bg/fg/border) are retuned to Aloha Tailwind-default hex values in both themes."
    - "Supabase-era junk tokens (--supabase-*, --sb-*, --glass-surface, --slate-alpha-wash) are removed from shadcn-ui.css."
  artifacts:
    - path: "DESIGN.md"
      provides: "Aloha design system spec (rewritten)"
      contains: "Inter Variable"
    - path: "app/styles/shadcn-ui.css"
      provides: ":root + .dark CSS variable blocks (Aloha palette)"
      contains: "--gradient-primary"
  key_links:
    - from: "app/styles/shadcn-ui.css :root"
      to: "Shadcn primitives (bg-primary, text-foreground, ...)"
      via: "CSS variable inheritance (key names preserved per D-01)"
      pattern: "--primary:\\s*#22c55e"
    - from: "app/styles/shadcn-ui.css .dark"
      to: ".dark class on <html> via next-themes"
      via: "theme toggle"
      pattern: "\\.dark\\s*\\{"
---

<objective>
Rewrite `DESIGN.md` as the Aloha theme source of truth and swap every CSS variable value in `app/styles/shadcn-ui.css` (light `:root` + hand-authored `.dark`) to the Aloha palette. Preserve every existing token KEY (per D-01) so downstream primitives / shell / AG Grid keep working without code changes.

Purpose: Establishes the foundation palette/tokens Phase 8/9/10 depend on. Nothing ships visually until Plan 02 wires Tailwind `@theme` and Plan 03 verifies contrast, but this plan's files are the canonical source of truth.

Output:
- `DESIGN.md` fully rewritten (Aloha spec, no Supabase wording).
- `app/styles/shadcn-ui.css` with Aloha `:root` + `.dark` blocks, `--radius: 1rem`, `--gradient-primary`, and retuned semantic triples.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-foundations/07-CONTEXT.md
@.planning/phases/07-design-foundations/07-RESEARCH.md
@.planning/phases/07-design-foundations/07-VALIDATION.md
@DESIGN.md
@app/styles/shadcn-ui.css
@/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/index.css
@/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/aloha-redesign-strategy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DESIGN.md as Aloha theme spec</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (current Supabase-era content — to be replaced wholesale)
    - .planning/phases/07-design-foundations/07-CONTEXT.md (locked decisions D-01..D-16)
    - .planning/phases/07-design-foundations/07-RESEARCH.md (§DESIGN.md Rewrite Outline, §Prototype Token Values, §Dark Palette Spec, §Shadow Scale Recommendation)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/index.css
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/aloha-redesign-strategy.md
  </read_first>
  <action>
    REPLACE DESIGN.md wholesale with an Aloha theme spec. Follow the 10-section outline from 07-RESEARCH.md §DESIGN.md Rewrite Outline verbatim:

    1. Visual Theme & Atmosphere — light-first, polished farm-ops ERP, soft surfaces, green gradient brand moment.
    2. Color Palette & Roles:
       - Brand: `#22c55e` (green-500), `#059669` (emerald-600), gradient `linear-gradient(135deg, #22c55e, #059669)`.
       - Neutrals table: slate-50 `#f8fafc`, slate-100 `#f1f5f9`, slate-200 `#e2e8f0`, slate-300 `#cbd5e1`, slate-400 `#94a3b8`, slate-500 `#64748b`, slate-600 `#475569`, slate-700 `#334155`, slate-800 `#1e293b`, slate-900 `#0f172a`.
       - Semantic triples (red/amber/green/blue × bg/fg/border × light/dark) — copy the exact table from 07-RESEARCH.md §Semantic.
       - Light mode token table AND dark mode token table — copy the full pairs table from 07-RESEARCH.md §Dark Palette Spec (all 20+ rows).
    3. Typography — family `'Inter Variable'` (sans + heading), `'Geist Mono Variable'` (mono). Base 16px. Weight 400/500/600. Scale table (display/h1/h2/h3/body/small).
    4. Radius — base `--radius: 1rem` (rounded-2xl). Derived: `--radius-sm = calc(var(--radius) - 4px) = 12px`, `--radius-md = calc(var(--radius) - 2px) = 14px`, `--radius-lg = var(--radius) = 16px`.
    5. Shadows — document the soft slate-900 alpha scale verbatim from 07-RESEARCH.md §Shadow Scale Recommendation (sm/md/lg/xl/2xl for light, dropped-opacity rgb(0 0 0 / ...) for dark).
    6. Component Stylings — one paragraph pointing Button/Card/Input/etc. work to Phase 8. Note the signature primary CTA uses `background: var(--gradient-primary)` + `shadow-lg shadow-green-500/25` (button-level utility, NOT a global token).
    7. Layout Principles — 72px navbar, 220/68 sidebar, `py-3` inputs/buttons, `py-4` table rows (Phase 9 hints).
    8. Dark Mode — derivation rules (NOT algorithmic — hand-authored). Document: backgrounds → slate-900/950, cards → slate-800, text → slate-50/100, muted → slate-400, primary → green-400/emerald-500, borders → slate-800/700. Every foundation pair passes WCAG AA.
    9. WCAG AA Verification — placeholder table listing the 12 foundation pairs (copy from 07-RESEARCH.md §WCAG Verification Approach). Mark all 24 entries as "pending — see scripts/verify-wcag.mjs output in Plan 03".
    10. Do's and Don'ts — no bold green surfaces, no shadow ≥ 2xl on interactive elements, gradient only on primary CTAs.

    Hard rules:
    - Do NOT leave ANY reference to Supabase, oklch Supabase tokens, or `--sb-*` / `--supabase-*` / `--glass-surface` / `--slate-alpha-wash`.
    - Use hex values ONLY for palette (not oklch) — matches the prototype.
    - The gradient MUST be documented as exposed via `--gradient-primary: linear-gradient(135deg, #22c55e, #059669)`, with `.dark` variant `--gradient-primary: linear-gradient(135deg, #4ade80, #10b981)`. `--primary` stays solid `#22c55e` (light) / `#4ade80` (dark) per D-05.
    - Document the `--primary-foreground` contrast caveat (Option C from 07-RESEARCH.md): solid `--primary` is color-of-record; interactive button face uses the gradient; foundation WCAG scope treats `--primary` pair at 3:1 UI threshold.
  </action>
  <verify>
    <automated>grep -q "Inter Variable" DESIGN.md &amp;&amp; grep -q "#22c55e" DESIGN.md &amp;&amp; grep -q "#059669" DESIGN.md &amp;&amp; grep -q "linear-gradient(135deg" DESIGN.md &amp;&amp; grep -q "1rem" DESIGN.md &amp;&amp; grep -q "rounded-2xl" DESIGN.md &amp;&amp; grep -q "slate-900" DESIGN.md &amp;&amp; grep -q "WCAG" DESIGN.md &amp;&amp; ! grep -qi "supabase" DESIGN.md &amp;&amp; ! grep -q "oklch" DESIGN.md &amp;&amp; ! grep -q "\-\-sb-" DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Inter Variable" DESIGN.md` (Inter is body font)
    - `grep -q "#22c55e" DESIGN.md` (green-500 primary documented)
    - `grep -q "#059669" DESIGN.md` (emerald-600 gradient end documented)
    - `grep -q "linear-gradient(135deg" DESIGN.md` (gradient formula present)
    - `grep -q "\-\-gradient-primary" DESIGN.md` (gradient token exposed)
    - `grep -q "1rem" DESIGN.md` and `grep -q "rounded-2xl" DESIGN.md`
    - `grep -q "slate-900" DESIGN.md` and `grep -q "slate-100" DESIGN.md`
    - `grep -q "Geist Mono" DESIGN.md` (mono retained per D-08)
    - `grep -q "WCAG" DESIGN.md` (WCAG section present)
    - `! grep -qi "supabase" DESIGN.md` (no Supabase residue)
    - `! grep -q "oklch" DESIGN.md` (no oklch residue — all hex)
    - `! grep -q "\-\-sb-" DESIGN.md` (no Supabase scaffolding tokens)
  </acceptance_criteria>
  <done>DESIGN.md is fully rewritten as the Aloha theme spec with all 10 sections, all hex palette values, gradient token, dark mode table, and WCAG placeholder table. Zero Supabase references.</done>
</task>

<task type="auto">
  <name>Task 2: Swap app/styles/shadcn-ui.css to Aloha palette (light + hand-authored dark)</name>
  <files>app/styles/shadcn-ui.css</files>
  <read_first>
    - app/styles/shadcn-ui.css (current Supabase oklch values)
    - .planning/phases/07-design-foundations/07-CONTEXT.md (D-01 keep keys, D-03 remove Supabase, D-05 primary green solid, D-10 radius 1rem, D-13 dark palette rules)
    - .planning/phases/07-design-foundations/07-RESEARCH.md (§Dark Palette Spec full table, §Current Shadcn Token Inventory, §Prototype Token Values, §Shadow Scale Recommendation)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/index.css
    - DESIGN.md (just written in Task 1 — must match)
  </read_first>
  <action>
    Rewrite `app/styles/shadcn-ui.css` inside `@layer base { :root { ... } .dark { ... } }`. Every token KEY from 07-RESEARCH.md §Current Shadcn Token Inventory is preserved — only values change. DELETE the Supabase scaffolding tokens listed under "DELETE" (`--supabase-*`, `--sb-*`, `--glass-surface`, `--slate-alpha-wash`).

    Update `--font-sans`, `--font-heading`, `--font-mono` string values at the top of `:root`:
    - `--font-sans: 'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;`
    - `--font-heading: var(--font-sans);`
    - `--font-mono: 'Geist Mono Variable', 'Source Code Pro', Menlo, monospace;` (unchanged per D-08)

    `:root` (LIGHT) — write these EXACT values (hex, not oklch). All values come from 07-RESEARCH.md §Dark Palette Spec "Light" column:

    ```
    --background: #f1f5f9;
    --foreground: #0f172a;
    --card: #ffffff;
    --card-foreground: #0f172a;
    --popover: #ffffff;
    --popover-foreground: #0f172a;
    --primary: #22c55e;
    --primary-foreground: #ffffff;
    --secondary: #f1f5f9;
    --secondary-foreground: #0f172a;
    --muted: #f1f5f9;
    --muted-foreground: #64748b;
    --accent: #f1f5f9;
    --accent-foreground: #0f172a;
    --destructive: #dc2626;
    --destructive-foreground: #ffffff;
    --border: #e2e8f0;
    --input: #cbd5e1;
    --ring: #22c55e;
    --radius: 1rem;
    --gradient-primary: linear-gradient(135deg, #22c55e, #059669);

    --chart-1: #22c55e;
    --chart-2: #059669;
    --chart-3: #3b82f6;
    --chart-4: #f59e0b;
    --chart-5: #a855f7;

    --semantic-red-bg: #fef2f2;
    --semantic-red-fg: #dc2626;
    --semantic-red-border: #fee2e2;
    --semantic-amber-bg: #fffbeb;
    --semantic-amber-fg: #d97706;
    --semantic-amber-border: #fef3c7;
    --semantic-green-bg: #f0fdf4;
    --semantic-green-fg: #16a34a;
    --semantic-green-border: #dcfce7;
    --semantic-blue-bg: #eff6ff;
    --semantic-blue-fg: #2563eb;
    --semantic-blue-border: #dbeafe;

    --sidebar-background: #ffffff;
    --sidebar-foreground: #475569;
    --sidebar-primary: #22c55e;
    --sidebar-primary-foreground: #ffffff;
    --sidebar-accent: #f0fdf4;
    --sidebar-accent-foreground: #15803d;
    --sidebar-border: #e2e8f0;
    --sidebar-ring: #22c55e;
    ```

    `.dark` (hand-authored per D-04/D-13) — write these EXACT values. All values come from 07-RESEARCH.md §Dark Palette Spec "Dark" column:

    ```
    --background: #0f172a;
    --foreground: #f8fafc;
    --card: #1e293b;
    --card-foreground: #f8fafc;
    --popover: #1e293b;
    --popover-foreground: #f8fafc;
    --primary: #4ade80;
    --primary-foreground: #052e16;
    --secondary: #334155;
    --secondary-foreground: #f8fafc;
    --muted: #1e293b;
    --muted-foreground: #94a3b8;
    --accent: #334155;
    --accent-foreground: #f8fafc;
    --destructive: #ef4444;
    --destructive-foreground: #ffffff;
    --border: #334155;
    --input: #475569;
    --ring: #4ade80;
    --gradient-primary: linear-gradient(135deg, #4ade80, #10b981);

    --chart-1: #4ade80;
    --chart-2: #10b981;
    --chart-3: #60a5fa;
    --chart-4: #fbbf24;
    --chart-5: #c084fc;

    --semantic-red-bg: rgb(239 68 68 / 0.15);
    --semantic-red-fg: #f87171;
    --semantic-red-border: rgb(239 68 68 / 0.30);
    --semantic-amber-bg: rgb(245 158 11 / 0.15);
    --semantic-amber-fg: #fbbf24;
    --semantic-amber-border: rgb(245 158 11 / 0.30);
    --semantic-green-bg: rgb(34 197 94 / 0.15);
    --semantic-green-fg: #4ade80;
    --semantic-green-border: rgb(34 197 94 / 0.30);
    --semantic-blue-bg: rgb(59 130 246 / 0.15);
    --semantic-blue-fg: #60a5fa;
    --semantic-blue-border: rgb(59 130 246 / 0.30);

    --sidebar-background: #0f172a;
    --sidebar-foreground: #cbd5e1;
    --sidebar-primary: #4ade80;
    --sidebar-primary-foreground: #052e16;
    --sidebar-accent: #14532d;
    --sidebar-accent-foreground: #bbf7d0;
    --sidebar-border: #1e293b;
    --sidebar-ring: #4ade80;
    ```

    Constraints (per D-15 phase_scope_reminder):
    - Do NOT rename any variable key. Only value swaps + deletions of the Supabase scaffolding tokens.
    - Do NOT add shadow tokens here — shadows are added in Plan 02 global.css unlock (this file stays color/radius/font/gradient only).
    - Do NOT touch any file outside `app/styles/shadcn-ui.css`.
    - Keep the `@layer base { :root { ... } .dark { ... } }` wrapper structure intact.
  </action>
  <verify>
    <automated>grep -q "\-\-primary:\s*#22c55e" app/styles/shadcn-ui.css &amp;&amp; grep -q "\-\-background:\s*#f1f5f9" app/styles/shadcn-ui.css &amp;&amp; grep -q "\-\-radius:\s*1rem" app/styles/shadcn-ui.css &amp;&amp; grep -q "\-\-gradient-primary:\s*linear-gradient(135deg, #22c55e, #059669)" app/styles/shadcn-ui.css &amp;&amp; grep -q "\-\-primary:\s*#4ade80" app/styles/shadcn-ui.css &amp;&amp; grep -q "'Inter Variable'" app/styles/shadcn-ui.css &amp;&amp; grep -q "'Geist Mono Variable'" app/styles/shadcn-ui.css &amp;&amp; ! grep -q "\-\-supabase" app/styles/shadcn-ui.css &amp;&amp; ! grep -q "\-\-sb-" app/styles/shadcn-ui.css &amp;&amp; ! grep -q "\-\-glass-surface" app/styles/shadcn-ui.css &amp;&amp; ! grep -q "oklch" app/styles/shadcn-ui.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\-\-primary:\s*#22c55e" app/styles/shadcn-ui.css` (light primary = green-500 solid)
    - `grep -q "\-\-background:\s*#f1f5f9" app/styles/shadcn-ui.css` (light bg = slate-100)
    - `grep -q "\-\-foreground:\s*#0f172a" app/styles/shadcn-ui.css` (light fg = slate-900)
    - `grep -q "\-\-radius:\s*1rem" app/styles/shadcn-ui.css` (radius = 1rem per D-10)
    - `grep -q "\-\-gradient-primary:\s*linear-gradient(135deg, #22c55e, #059669)" app/styles/shadcn-ui.css`
    - `grep -q "\-\-primary:\s*#4ade80" app/styles/shadcn-ui.css` (.dark primary = green-400)
    - `grep -q "\-\-background:\s*#0f172a" app/styles/shadcn-ui.css` (.dark bg = slate-900)
    - `grep -q "\-\-card:\s*#1e293b" app/styles/shadcn-ui.css` (.dark card = slate-800)
    - `grep -q "'Inter Variable'" app/styles/shadcn-ui.css`
    - `grep -q "'Geist Mono Variable'" app/styles/shadcn-ui.css` (retained per D-08)
    - `grep -q "\.dark\s*{" app/styles/shadcn-ui.css` (.dark block exists)
    - `! grep -q "\-\-supabase" app/styles/shadcn-ui.css` (Supabase tokens removed)
    - `! grep -q "\-\-sb-" app/styles/shadcn-ui.css`
    - `! grep -q "\-\-glass-surface" app/styles/shadcn-ui.css`
    - `! grep -q "oklch" app/styles/shadcn-ui.css` (all values are hex/rgb, not oklch)
    - `pnpm typecheck` exits 0 (CSS changes don't break TS)
  </acceptance_criteria>
  <done>shadcn-ui.css has Aloha light + hand-authored dark palettes with --radius: 1rem, --gradient-primary, all semantic tokens retuned, Supabase scaffolding deleted, and Inter/Geist Mono font strings.</done>
</task>

</tasks>

<threat_model>
  <scope>
    Pure documentation (DESIGN.md) + CSS token values (shadcn-ui.css). No auth, data, routes, loaders, actions, server code, user input surfaces, or external I/O.
  </scope>
  <threat id="T-7-01-01" severity="info">
    Stale Supabase-era token references in dependent files after deletion (`--glass-surface`, `--slate-alpha-wash`, `--sb-*`) could cause unstyled fallbacks if still consumed elsewhere.
    Mitigation: Plan 02 Task 1 (theme.css update) will remove the matching `@theme` `--color-glass-surface`/`--color-slate-alpha-wash` mappings. If any component in `packages/ui/` or `app/components/` references these, Phase 8/9 will inherit the issue — not Phase 7's concern per D-15, but noted for retrospective.
  </threat>
  <threat id="T-7-01-02" severity="info">
    `--primary-foreground` on `--primary` solid green-500 with white text computes ~2.75:1 (below 4.5:1 body text threshold).
    Mitigation: Scoped to UI/large-text 3:1 threshold per D-14 + Option C documented in DESIGN.md. Plan 03 WCAG script validates at 3:1 for this specific pair. Button labels in Phase 8 use the gradient variant whose effective luminance is darker.
  </threat>
</threat_model>

<verification>
- `grep` acceptance criteria above all pass.
- `pnpm typecheck` and `pnpm lint` still green (no TS files touched — sanity only).
- Visual smoke deferred to Plan 03 (requires Plan 02 Tailwind wiring first).
</verification>

<success_criteria>
- DESIGN.md is the Aloha theme spec with zero Supabase residue and all foundation tables (palette, dark pairs, shadow scale, WCAG placeholder).
- shadcn-ui.css `:root` + `.dark` use Aloha hex values, `--radius: 1rem`, `--gradient-primary`, and retuned semantic/sidebar tokens.
- All existing Shadcn primitive call sites (`bg-primary`, `text-foreground`, ...) compile and resolve — verified by `pnpm typecheck` passing.
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-foundations/07-01-SUMMARY.md` documenting:
- Final DESIGN.md outline
- Complete light + dark token tables shipped (as executed, for Plan 03 WCAG verification)
- Any deviations from 07-RESEARCH.md §Dark Palette Spec (should be zero)
</output>
