---
phase: 03-enhancement-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/ui/src/shadcn/card.tsx
  - packages/ui/src/shadcn/alert.tsx
  - packages/ui/src/shadcn/heading.tsx
  - packages/ui/src/shadcn/dialog.tsx
  - packages/ui/src/shadcn/alert-dialog.tsx
  - packages/ui/src/shadcn/sheet.tsx
  - packages/ui/src/shadcn/select.tsx
  - packages/ui/src/shadcn/badge.tsx
autonomous: false
requirements: [ENHN-02, ENHN-03]

must_haves:
  truths:
    - "Body text renders at weight 400 by default"
    - "Nav links and button labels render at weight 500"
    - "No element in the shadcn component library uses font-bold (700) or font-semibold (600)"
    - "Card titles have -0.16px letter-spacing instead of Tailwind tracking-tight"
    - "Dialog, sheet, and alert-dialog titles use font-medium (500)"
    - "Headings level 1-4 use font-normal (400)"
  artifacts:
    - path: "packages/ui/src/shadcn/card.tsx"
      provides: "CardTitle with font-normal tracking-[-0.16px]"
      contains: "font-normal tracking-[-0.16px]"
    - path: "packages/ui/src/shadcn/heading.tsx"
      provides: "Headings 1-4 at font-normal"
      contains: "font-normal"
    - path: "packages/ui/src/shadcn/alert.tsx"
      provides: "AlertTitle at font-normal"
      contains: "font-normal"
  key_links:
    - from: "app/styles/global.css"
      to: "packages/ui/src/shadcn/*.tsx"
      via: "body font-weight: 400 provides default; component font-medium overrides where needed"
      pattern: "font-weight: 400"
---

<objective>
Enforce the Supabase typography weight restraint across all shadcn components (ENHN-02) and apply -0.16px card title letter-spacing (ENHN-03). Then perform end-to-end verification including font-weight audit, WCAG contrast check on new semantic tokens, and visual checkpoint.

Purpose: Completes the typography refinement that gives the application the refined, lightweight Supabase look — no heavy bold weights, precise letter-spacing, consistent hierarchy.

Output: All 8 shadcn component files updated with correct weights, verified via grep audit and visual inspection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enhancement-verification/03-CONTEXT.md
@.planning/phases/03-enhancement-verification/03-RESEARCH.md
@.planning/phases/03-enhancement-verification/03-01-SUMMARY.md

<interfaces>
<!-- Font-weight audit from RESEARCH.md — all 11 sites across 8 files -->
<!-- card.tsx:35 CardTitle — font-semibold tracking-tight -> font-normal tracking-[-0.16px] -->
<!-- alert.tsx:47 AlertTitle — font-bold tracking-tight -> font-normal -->
<!-- badge.tsx:8 base — font-semibold -> font-medium -->
<!-- heading.tsx:15 H1 — font-bold tracking-tight -> font-normal tracking-tight -->
<!-- heading.tsx:26 H2 — font-semibold tracking-tight -> font-normal tracking-tight -->
<!-- heading.tsx:37 H3 — font-semibold tracking-tight -> font-normal tracking-tight -->
<!-- heading.tsx:48 H4 — font-semibold tracking-tight -> font-normal tracking-tight -->
<!-- heading.tsx:59 H5 — already font-medium (keep) -->
<!-- heading.tsx:70 H6 — already font-medium (keep) -->
<!-- dialog.tsx:114 DialogTitle — font-semibold -> font-medium -->
<!-- alert-dialog.tsx:74 AlertDialogTitle — font-semibold -> font-medium -->
<!-- sheet.tsx:107 SheetTitle — font-semibold -> font-medium -->
<!-- select.tsx:114 SelectLabel — font-semibold -> font-medium -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply font-weight restraint and card title letter-spacing across all shadcn components</name>
  <files>
    packages/ui/src/shadcn/card.tsx,
    packages/ui/src/shadcn/alert.tsx,
    packages/ui/src/shadcn/badge.tsx,
    packages/ui/src/shadcn/heading.tsx,
    packages/ui/src/shadcn/dialog.tsx,
    packages/ui/src/shadcn/alert-dialog.tsx,
    packages/ui/src/shadcn/sheet.tsx,
    packages/ui/src/shadcn/select.tsx
  </files>
  <read_first>
    - packages/ui/src/shadcn/card.tsx (current CardTitle className)
    - packages/ui/src/shadcn/alert.tsx (current AlertTitle className)
    - packages/ui/src/shadcn/badge.tsx (current base variant className)
    - packages/ui/src/shadcn/heading.tsx (all heading level classNames)
    - packages/ui/src/shadcn/dialog.tsx (DialogTitle className)
    - packages/ui/src/shadcn/alert-dialog.tsx (AlertDialogTitle className)
    - packages/ui/src/shadcn/sheet.tsx (SheetTitle className)
    - packages/ui/src/shadcn/select.tsx (SelectLabel className)
    - DESIGN.md (S3 typography rules — weight 400/500 only, S7 "Don't: use bold 700")
  </read_first>
  <action>
Apply the following exact class replacements per D-04, D-05, D-06 and the research font-weight audit table:

**card.tsx line 35** — CardTitle `<h3>`:
- Replace `font-semibold tracking-tight` with `font-normal tracking-[-0.16px]`
- This applies both ENHN-02 (weight 400) and ENHN-03 (-0.16px letter-spacing)

**alert.tsx line 47** — AlertTitle `<h5>`:
- Replace `font-bold tracking-tight` with `font-normal`
- Remove `tracking-tight` entirely (alert titles do not need negative tracking per DESIGN.md)

**badge.tsx line 8** — Badge base class string:
- Replace `font-semibold` with `font-medium`
- Badge labels function as indicators; 500 is defensible per DESIGN.md "captions 400-500"

**heading.tsx** — Four changes:
- Line 15 (H1): Replace `font-bold tracking-tight` with `font-normal tracking-tight`
- Line 26 (H2): Replace `font-semibold tracking-tight` with `font-normal tracking-tight`
- Line 37 (H3): Replace `font-semibold tracking-tight` with `font-normal tracking-tight`
- Line 48 (H4): Replace `font-semibold tracking-tight` with `font-normal tracking-tight`
- Lines 59, 70 (H5, H6): Leave as `font-medium` — these are already correct per research recommendation

**dialog.tsx line 114** — DialogTitle:
- Replace `font-semibold` with `font-medium`
- Modal titles are UI landmarks; 500 is acceptable

**alert-dialog.tsx line 74** — AlertDialogTitle:
- Replace `font-semibold` with `font-medium`

**sheet.tsx line 107** — SheetTitle:
- Replace `font-semibold` with `font-medium`

**select.tsx line 114** — SelectLabel:
- Replace `font-semibold` with `font-medium`
- Group labels in dropdowns are navigational context; 500 acceptable

After all changes, run:
1. `pnpm typecheck` — must pass
2. `pnpm format:fix` — format all modified files
3. `pnpm lint:fix` — fix any lint issues

Then perform a codebase-wide grep audit to confirm NO font-bold or font-semibold remains in `packages/ui/src/shadcn/`:
```bash
grep -rn "font-bold\|font-semibold" packages/ui/src/shadcn/
```
This must return zero results.
  </action>
  <verify>
    <automated>grep -rn "font-bold\|font-semibold" packages/ui/src/shadcn/ | wc -l | grep -q "^0$" && grep "font-normal tracking-\[-0.16px\]" packages/ui/src/shadcn/card.tsx | grep -q "font-normal" && grep "font-normal" packages/ui/src/shadcn/alert.tsx | grep -q "font-normal" && grep "font-medium" packages/ui/src/shadcn/dialog.tsx | grep -q "font-medium" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "font-bold\|font-semibold" packages/ui/src/shadcn/` returns zero results
    - card.tsx CardTitle contains `font-normal tracking-[-0.16px]` (not `tracking-tight`)
    - alert.tsx AlertTitle contains `font-normal` (not `font-bold`, not `tracking-tight`)
    - badge.tsx base class contains `font-medium` (not `font-semibold`)
    - heading.tsx H1 contains `font-normal` (not `font-bold`)
    - heading.tsx H2, H3, H4 contain `font-normal` (not `font-semibold`)
    - heading.tsx H5, H6 still contain `font-medium` (unchanged)
    - dialog.tsx DialogTitle contains `font-medium` (not `font-semibold`)
    - alert-dialog.tsx AlertDialogTitle contains `font-medium` (not `font-semibold`)
    - sheet.tsx SheetTitle contains `font-medium` (not `font-semibold`)
    - select.tsx SelectLabel contains `font-medium` (not `font-semibold`)
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>All 11 font-weight sites updated. Zero font-bold/font-semibold occurrences remain in shadcn components. Card titles use -0.16px letter-spacing. Typography weight restraint fully enforced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 3 theme enhancements</name>
  <files>none — verification only</files>
  <action>
Start the dev server with `pnpm dev` and present the application at http://localhost:5173 for visual inspection. The user will verify all Phase 3 enhancements across both themes: typography weight restraint (400 body, 500 nav/buttons only), card title letter-spacing, semantic color tokens on alerts/badges, and translucent surface tokens.
  </action>
  <what-built>
    All Phase 3 enhancements: translucent surface tokens, semantic color tokens wired to alert/badge components, neutral gray scale completion, tech-label utility, body weight 400 default, component weight audit (all headings/titles updated), and -0.16px card title letter-spacing.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open http://localhost:5173
    2. **Typography weight check (ENHN-02, ENHN-03):**
       - Navigate to any page with cards — card titles should appear lighter weight than before (400 vs 600)
       - Card title letter-spacing should be slightly looser than before (-0.16px vs -0.6px from tracking-tight)
       - Open browser DevTools, inspect a card title — confirm `font-weight: 400` and `letter-spacing: -0.16px`
       - Navigate to any page with headings (H1-H4) — should render at regular weight, not bold
       - Dialog/modal titles should appear at medium weight (500)
       - Badge text should remain readable at medium weight (500) in both themes
    3. **Semantic color check (ENHN-05):**
       - If any page displays alert or badge variants (success/warning/info), verify they render with the new semantic colors
       - Toggle dark/light mode — colors should automatically adapt (no more hardcoded green-600/orange-600/blue-600)
    4. **Theme toggle:**
       - Switch between dark and light themes — all elements should look correct in both modes
       - No flash of unstyled content, no broken colors
    5. **Overall polish:**
       - The application should have a refined, lightweight feel — no heavy bold text anywhere
       - All text should feel consistent in weight hierarchy
  </how-to-verify>
  <verify>User visually confirms all enhancements render correctly in both themes</verify>
  <done>User approves the visual presentation of Phase 3 enhancements</done>
  <resume-signal>Type "approved" or describe any visual issues that need fixing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply — this plan modifies only Tailwind CSS class names within UI components. No authentication, data access, or user input processing involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | N/A | Component classes | accept | Class name changes only, no security surface |
</threat_model>

<verification>
1. Zero `font-bold` or `font-semibold` in shadcn component directory
2. CardTitle uses `tracking-[-0.16px]` not `tracking-tight`
3. Body defaults to `font-weight: 400` via global.css
4. Visual inspection confirms lightweight typography in both themes
5. `pnpm typecheck` passes
</verification>

<success_criteria>
- All 11 font-weight sites across 8 files updated per the research audit table
- No font-bold or font-semibold remains in packages/ui/src/shadcn/
- Card titles use -0.16px letter-spacing
- User visually approves the complete Phase 3 enhancements in both themes
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhancement-verification/03-02-SUMMARY.md`
</output>
