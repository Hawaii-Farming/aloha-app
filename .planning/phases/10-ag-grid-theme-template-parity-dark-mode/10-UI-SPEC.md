---
phase: 10
slug: ag-grid-theme-template-parity-dark-mode
status: draft
shadcn_initialized: true
preset: aloha-internal (packages/ui/components.json)
created: 2026-04-10
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for the AG Grid theme rewrite, template parity, and dark-mode shell fixes. Generated by gsd-ui-researcher (2026-04-10). Consumed by gsd-ui-checker, gsd-planner, gsd-executor, gsd-ui-auditor.

**Phase type:** Retrofit / visual parity — not greenfield. Phase 10 closes the remaining visual and structural gaps between the shipped v2.0 shell (Phases 7–9) and the reference prototype at `/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype`. No new components, no new packages, no primitive rewrites.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `packages/ui/components.json`) |
| Preset | Aloha internal (Phase 7 token foundation, see `DESIGN.md §2`) |
| Component library | Radix UI 1.4.3 primitives via `@aloha/ui/shadcn/*` — **read-only this phase** (Phase 9 D-09 guardrail) |
| Icon library | `lucide-react` — `PanelLeft`, `ChevronDown`, `LayoutGrid`, `ChevronLeft`, `Command`, `Search` only |
| Font (app) | **Inter Variable** via `@fontsource-variable/inter` (`DESIGN.md §3`, set by Phase 7) |
| Font (AG Grid) | **Inter Variable** — upgrade from current `'Geist Variable'` literal in `ag-grid-theme.ts` to match the rest of the app. (Research deviation from CONTEXT D-02; planner-approved via research recommendation — one-string change, removes divergent font surface.) |
| Font mono | `Geist Mono Variable` (retained) |
| AG Grid theming API | `themeQuartz.withParams(light, 'light').withParams(dark, 'dark')` chain preserved; hex literals only (no `var()` refs — AG Grid v35 does not resolve CSS vars inside `withParams`) |

---

## Spacing Scale

Multiples of 4, consumed via Tailwind v4 semantic classes. No new tokens this phase.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| xs  | 4px  | Sidebar sub-module row `gap-1`; chevron → label gap; AG Grid inline controls |
| sm  | 8px  | Sidebar sub-module vertical separation (`mt-1 mb-1` = 4px top + 4px bottom); section-header padding |
| md  | 16px | AG Grid toolbar padding; sidebar section-group padding; main content `p-4` |
| lg  | 24px | Sidebar section vertical rhythm between NAVIGATION and MODULES groups |
| xl  | 32px | — (not used this phase) |
| 2xl | 48px | — (not used this phase) |
| 3xl | 64px | — (not used this phase) |

**Fixed surface dimensions (locked from Phase 9, re-asserted):**

| Surface | Dimension | Source |
|---------|-----------|--------|
| Navbar height | 72px | `DESIGN.md §7`, Phase 9 shell |
| Sidebar expanded width | 220px | `DESIGN.md §7`, Phase 9 shell |
| Sidebar collapsed width | 68px | `DESIGN.md §7`, Phase 9 shell |
| Scrollbar width/height | 6px | Prototype `index.css`, CONTEXT D-15 |
| AG Grid row vertical padding | `rowVerticalPaddingScale: 1.6` | Phase 7 `DESIGN.md §7` "comfortable scan density" |
| AG Grid header font size | 13px | AG Grid theme params (this phase) |
| AG Grid body font size | 14px | AG Grid theme params (this phase) |
| AG Grid toolbar search radius | `rounded-md` (6px) | GRID-03, overrides inherited `rounded-2xl` |

Exceptions: none. Every value is a multiple of 4 or a locked surface dimension from Phase 9.

---

## Typography

Inter Variable 16px base. Weights: **400** (body) and **600** (section headings, labels, active states). 500 is retained only for existing Phase 8 primitives (button, badge, nav label) and AG Grid header — no new 500 usage this phase.

| Role | Size | Weight | Line Height | Used for (this phase) |
|------|------|--------|-------------|-----------------------|
| Body  | 16px (1rem)   | 400 | 1.5 | Grid cell text, sidebar sub-module labels, navbar search placeholder |
| Label | 14px (0.875rem) | 500 | 1.4 | Sidebar section headers ("NAVIGATION", "MODULES"), AG Grid column headers, module row labels, "Focused" footer label |
| H3    | 20px (1.25rem)  | 600 | 1.3 | AG Grid list-view page titles (existing — not changed) |
| H2    | 24px (1.5rem)   | 600 | 1.25 | Sub-module detail page headings (existing — not changed) |

**Scale locked from `DESIGN.md §3`.** This phase introduces no new sizes or weights.

**Section header copy style:** `text-xs font-semibold uppercase tracking-wider text-muted-foreground` — matches prototype exactly. Size resolves to ~12px for the section labels only (accepted deviation from the 14px label minimum because this is a micro-label, not body content, and `uppercase + tracking-wider` preserves legibility at the 3:1 UI-component WCAG threshold).

---

## Color

60 / 30 / 10 split, token-sourced from Phase 7 `shadcn-ui.css`. Accent is reserved — see the explicit allow-list below.

| Role | Light | Dark | Usage |
|------|-------|------|-------|
| Dominant (60%) | `#f1f5f9` (slate-100, `--background`) | `#0f172a` (slate-900, `--background`) | Page canvas behind cards, grid rows, and content area |
| Secondary (30%) | `#ffffff` (`--card`, `--sidebar-background`, `--popover`) | `#1e293b` (slate-800, `--card`) + **`--sidebar-background` fix → `#1e293b`** | Navbar surface, sidebar surface, cards, AG Grid row background |
| Accent (10%) | `#22c55e` → `#059669` gradient (`--gradient-primary`) + `#22c55e` solid (`--primary`) | `#4ade80` → `#10b981` gradient + `#4ade80` solid | **Allow-list below** |
| Destructive | `#dc2626` (red-600, `--destructive`) | `#ef4444` (red-500) | Delete-confirm destructive button only; no destructive actions added this phase |

**DARK-03 one-line fix (research recommendation, supersedes CONTEXT D-17 / D-17b):** In `app/styles/shadcn-ui.css` `.dark { ... }` block, set `--sidebar-background: #1e293b` (slate-800, matching `--card`) and `--sidebar-border: #334155` (slate-700). Navbar already consumes `bg-card`; sidebar consumes `bg-sidebar` via the Shadcn primitive. Both now render on the same elevated surface (slate-800) above the page canvas (slate-900). **No new `--color-chrome` token.** **No `dark:bg-slate-900` literal overrides on containers.** This is the single authoritative dark-mode surface fix.

### Accent reserved for — explicit allow-list

The green gradient (`--gradient-primary`) and solid green primary (`--primary`) are used ONLY on:

1. **Active sidebar module pill** — `bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25` (light) / `from-green-400 to-emerald-500` (dark). Locked from Phase 9 D-09. **BUG-01 must preserve this recipe verbatim.**
2. **Active sub-module chip** — `bg-green-50 text-green-700 font-medium rounded-lg` (light) / `dark:bg-green-900/40 dark:text-green-200 dark:font-medium` (dark override added this phase, carries over Phase 9 D-11 deferral).
3. **AG Grid `accentColor` param** — `#22c55e` light, `#4ade80` dark — row selection checkbox color and row focus ring only.
4. **AG Grid `selectedRowBackgroundColor`** — `#f0fdf4` (green-50) light, `rgba(34, 197, 94, 0.15)` dark.
5. **Aloha logo square + wordmark gradient** — existing Phase 9 surface, not changed this phase.
6. **Primary CTA buttons** (existing Phase 8 primary variant) — not introduced this phase; listed for auditor completeness.

**NEVER use the green gradient on:** AG Grid row hover, navbar search trigger, scrollbar thumb, sidebar section headers, hover states on inactive rows, border accents, focus rings on non-primary inputs. Hover states use `--muted` / `slate-100` (light) / `slate-700` (dark).

### AG Grid token-pair contract (this phase locks these hexes)

Light mode `withParams` values:

| Param | Hex | Token source |
|-------|-----|--------------|
| `backgroundColor` | `#ffffff` | `--card` |
| `foregroundColor` | `#0f172a` | `--card-foreground` (slate-900) |
| `headerBackgroundColor` | `#f1f5f9` | `--muted` (slate-100) |
| `headerTextColor` | `#475569` | slate-600 (`--sidebar-foreground`) |
| `borderColor` | `#e2e8f0` | `--border` (slate-200) |
| `accentColor` | `#22c55e` | `--primary` (green-500) |
| `rowHoverColor` | `#f1f5f9` | `--muted` (slate-100) |
| `selectedRowBackgroundColor` | `#f0fdf4` | green-50 |
| `oddRowBackgroundColor` | `#f8fafc` | slate-50 (subtle zebra) |
| `browserColorScheme` | `'light'` | — |

Dark mode `withParams` values:

| Param | Hex | Token source |
|-------|-----|--------------|
| `backgroundColor` | `#1e293b` | `--card` (slate-800) |
| `foregroundColor` | `#f8fafc` | `--card-foreground` (slate-50) |
| `headerBackgroundColor` | `#0f172a` | `--background` (slate-900) — header slightly deeper than rows |
| `headerTextColor` | `#cbd5e1` | slate-300 |
| `borderColor` | `#334155` | `--border` (slate-700) |
| `accentColor` | `#4ade80` | `--primary` dark (green-400) |
| `rowHoverColor` | `#334155` | slate-700 |
| `selectedRowBackgroundColor` | `rgba(34, 197, 94, 0.15)` | semantic green dark wash |
| `oddRowBackgroundColor` | `#1e293b` | same as row bg — no zebra in dark |
| `browserColorScheme` | `'dark'` | — |

Shared params (both modes): `fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif"`, `fontSize: 14`, `headerFontSize: 13`, `headerFontWeight: 500`, `checkboxBorderRadius: 4`, `rowVerticalPaddingScale: 1.6`, `columnBorder: true`.

### Scrollbar token contract (PARITY-05)

Global rule in `app/styles/global.css` (or the existing entry point — planner locates), keyed off Phase 7 tokens. Zero per-component overrides.

```css
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--border); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--muted-foreground); }

html { scrollbar-width: thin; scrollbar-color: var(--border) transparent; }
```

Resolved pairs:

| Mode | Thumb | Thumb hover |
|------|-------|-------------|
| Light | `#e2e8f0` (slate-200, `--border`) | `#64748b` (slate-500, `--muted-foreground`) |
| Dark  | `#334155` (slate-700, `--border`) | `#94a3b8` (slate-400, `--muted-foreground`) |

Thumb token is `--border` (CONTEXT D-15 default, research-confirmed over `--muted` because slate-200/slate-700 gives the visible-but-quiet prototype thumb).

### WCAG AA audit deliverable (Success Criterion #8, CONTEXT D-24..26)

Required deliverable: a token-pair table inside `10-WCAG-AUDIT.md` (or embedded in `10-PHASE-VERIFICATION.md`). Every row must contain `{surface, fg, bg, ratio, pass/fail, remediation}`. Minimum rows:

**Shell chrome (light + dark, 2 rows each):**
- Navbar fg vs bg (`--card-foreground` vs `--card`)
- Sidebar fg vs bg (`--sidebar-foreground` vs `--sidebar-background` — post-fix)
- Active pill text vs gradient midpoint (`#ffffff` vs `#16a34a` light; `#052e16` vs `#22c55e` dark)
- Sub-item chip fg vs bg (`#15803d` vs `#f0fdf4` light; `#bbf7d0` vs `#14532d` dark)
- Navbar centered search trigger fg vs bg (`--muted-foreground` vs `--muted` on `--card` surface)

**AG Grid (light + dark, 5 rows each):**
- Body text vs row bg (`foregroundColor` vs `backgroundColor`)
- Body text vs hover row bg (`foregroundColor` vs `rowHoverColor`)
- Body text vs odd row bg (`foregroundColor` vs `oddRowBackgroundColor`)
- Header text vs header bg (`headerTextColor` vs `headerBackgroundColor`)
- Border vs row bg (`borderColor` vs `backgroundColor`) — 3:1 UI threshold, not 4.5:1

**Phase 8 primitives (static checklist row per primitive, 2 modes):**
- Button primary, button secondary, button ghost
- Badge default, badge destructive, badge semantic (green/red/amber/blue)
- Input default + focused, input placeholder text
- Card body text
- Avatar fallback initials on slate-700 bg (dark) and slate-100 bg (light)

**Methodology:** static checklist against declared hex values (CONTEXT D-25). No runtime sampling. Any FAIL row becomes a remediation task before phase close.

**Pre-existing known FAILs carried from Phase 7 `DESIGN.md §9`** (must be either remediated or explicitly waived in audit):
- `primary-foreground / primary` light — 2.28:1 (FAIL, needs 3.0:1)
- `muted-foreground / background` light — 4.34:1 (FAIL, needs 4.5:1)
- `muted-foreground / muted` light — 4.34:1 (FAIL, needs 4.5:1)

Planner assigns these to the WCAG audit task; executor either picks a darker `muted-foreground` or documents the waiver with rationale (e.g., muted copy is large-text only).

---

## Surface Contracts

Phase 10 is a retrofit. The design contract is expressed per surface, not per screen. Each surface below is authoritative — planner and executor reference these exact recipes.

### Surface 1 — AG Grid (GRID-01, GRID-02, GRID-03)

**Theme hex contract:** see the two tables under "AG Grid token-pair contract" above.

**Container sizing (GRID-02):** The grid must fill its available width and height across all 9 HR module list views. Root cause is in `app/routes/workspace/layout.tsx` (main content area missing `min-h-0`). Fix recipe:

```tsx
// app/routes/workspace/layout.tsx main content area
<main className="flex min-h-0 flex-1 flex-col overflow-hidden">
  <div className="flex min-h-0 flex-1 flex-col p-4">
    <Outlet />
  </div>
</main>
```

Required class contract on the chain: every flex-column ancestor between `<main>` and `AgGridWrapper` must carry `min-h-0` alongside `flex-1` / `flex flex-col`. `overflow-hidden` on `<main>` (not `overflow-y-auto`) — the grid's virtual scroller owns the scrollbar, not the page.

**Toolbar search rounding (GRID-03):** The shared `Input` primitive at `packages/ui/src/shadcn/input.tsx` ships with `rounded-2xl`. Phase 10 override is a `className="rounded-md"` prop passed through the AG Grid toolbar's search input **only**. Do not change the primitive. Do not cascade to non-grid surfaces (pills/badges/buttons keep `rounded-full` / `rounded-2xl`). `tailwind-merge` collapses the conflict correctly.

**State recipes:**

| State | Light | Dark |
|-------|-------|------|
| Row default | `#ffffff` bg, `#0f172a` fg | `#1e293b` bg, `#f8fafc` fg |
| Row odd (zebra) | `#f8fafc` bg | `#1e293b` bg (no zebra) |
| Row hover | `#f1f5f9` bg | `#334155` bg |
| Row selected | `#f0fdf4` bg | `rgba(34, 197, 94, 0.15)` bg |
| Row active (focused) | `#0f172a` fg on `#f0fdf4` bg, 2px `#22c55e` focus outline | `#f8fafc` fg on `rgba(34, 197, 94, 0.15)` bg, 2px `#4ade80` focus outline |
| Header | `#f1f5f9` bg, `#475569` fg, 13px/500 weight, columnBorder on | `#0f172a` bg, `#cbd5e1` fg, 13px/500 weight, columnBorder on |
| Empty state | Default AG Grid "No Rows To Show" — no custom overlay this phase |
| Loading state | Default AG Grid loading overlay — no custom overlay this phase |

**Test parity update (CONTEXT D-04):** `app/components/ag-grid/__tests__/ag-grid-theme.test.ts` must assert the new hex values, not be deleted or skipped.

### Surface 2 — Sidebar (PARITY-01, PARITY-04, PARITY-05, BUG-01)

Port prototype structure onto the existing `app/components/sidebar/module-sidebar-navigation.tsx`. No new files.

**Structural elements (top → bottom):**

1. **NAVIGATION section header** — `text-xs font-semibold uppercase tracking-wider text-muted-foreground px-3 pb-2 pt-3`. Copy: `NAVIGATION` (all caps, per prototype).
2. **Home/Dashboard link** (if present in current nav config — do not add new items).
3. **`SidebarSeparator`** — single 1px `border-border` horizontal line between sections (CONTEXT D-11).
4. **MODULES section header** — same label style. Copy: `MODULES`.
5. **Module rows** — each row is a `SidebarMenuButton` with:
   - Left icon (module icon from `module-icons.config.ts` — unchanged)
   - Label (14px/500 weight)
   - **Chevron dropdown** — `ChevronDown` lucide icon, right-aligned, rotates 180° when expanded (`transition-transform duration-200`). Color: inactive = `text-muted-foreground`; active = inherits pill text color (white light / green-950 dark).
6. **Accordion sub-module container** (when module is expanded):
   - Wrapper: `ml-5 border-l-2 border-green-200 dark:border-green-900/60 pl-3 gap-1 mt-1 mb-1` — adds the missing vertical separation (PARITY-04).
   - Each sub-module row: 14px/400 weight default; active state uses the sub-item chip recipe (see below).
7. **`SidebarSeparator`** above footer.
8. **"Focused" footer** — render **disabled placeholder** (CONTEXT D-14 default). Structure: `<button aria-disabled="true" className="flex w-full items-center gap-2 px-3 py-2 text-xs font-medium text-muted-foreground opacity-60 cursor-not-allowed">`. Icons: `LayoutGrid` (left) + `ChevronLeft` (right). Label: `Focused`. No click handler. No focus ring.

**Inline collapse affordance:** **omitted** (CONTEXT D-10). The navbar `PanelLeft` toggle is the single source of truth. Document this deviation inline in the phase plan.

**Active module pill recipe (locked from Phase 9 D-09 — do not alter):**

```
bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25
dark: from-green-400 to-emerald-500 text-green-950 shadow-green-400/20
rounded-xl px-3 py-2 font-medium
```

**Active sub-item chip recipe (new dark override added this phase — carries over Phase 9 D-11 deferral):**

```
Light: bg-green-50 text-green-700 font-medium rounded-lg
Dark:  dark:bg-green-900/40 dark:text-green-200
```

**BUG-01 interaction contract:** The active-module gradient pill MUST appear:
- On initial route load, computed from `currentPath.startsWith(modulePath)` (derive-on-render, `useMemo`, no `useEffect`).
- Immediately on module row click, before any sub-module is selected.
- In both collapsed and expanded branches of `module-sidebar-navigation.tsx` — both branches must consume the same `isModuleActive` derivation. No dual state paths.

**Implementation constraint (CLAUDE.md):** No new `useEffect`. Active state is derived on render from `useLocation().pathname` via `useMemo`. The "fix" unifies the expanded branch with the collapsed branch's `isModuleActive` logic.

**Scrollbar:** sidebar inherits the global 6px themed scrollbar from Surface 5 (Scrollbars). No per-component override.

### Surface 3 — Navbar (PARITY-02, PARITY-03, BUG-02)

**Layout (locked from Phase 9, re-asserted for Phase 10 verification only):**

```
[ PanelLeft toggle ] [ Aloha logo square + wordmark ]  ........  [ centered command-palette search ]  ........  [ avatar ]
        ↑ leftmost
```

- Height: 72px
- Background: `bg-card` (consumes the post-fix elevated surface in dark)
- Border: `border-b border-border`
- `PanelLeft` toggle at index 0 — **verify only** (CONTEXT D-23); already shipped in Phase 9.

**Avatar initials contract (PARITY-03):**

- Source field: `workspace.currentOrg.org_name` (**NOT `display_name`** — the research corrected CONTEXT D-20; `display_name` does not exist on `OrgWorkspace.currentOrg`).
- Helper: `app/lib/workspace/get-org-initials.ts` — pure function, unit-testable.
- Algorithm: split `org_name` on whitespace, take the first letter of up to 2 words, uppercase. Example: `"Hawaii Farming"` → `"HF"`.
- Fallback chain: `org_name` → first letter of user email → `"A"`.
- Prop drill: `app/routes/workspace/layout.tsx` passes `currentOrg` (or precomputed `initials` string) to `WorkspaceNavbar`.
- **Both** the navbar avatar AND the sidebar-footer profile menu avatar (`workspace-navbar-profile-menu.tsx`) must consume the same helper — one UI, one source of truth.
- Avatar fallback bg: `bg-muted` (light) / `bg-slate-700` (dark). Fallback fg: `text-foreground`. Font: 14px/600.

**Centered navbar search trigger contract (DARK-02):**

- Classes (unchanged from Phase 9): `bg-muted rounded-2xl px-4 py-2 text-sm text-muted-foreground` within a `flex-1 max-w-md` wrapper.
- Dark-mode legibility verification: `--muted` (slate-800 `#1e293b`) on `--card` (slate-800 `#1e293b`) would collapse — mitigated because the post-fix puts the navbar on `--card` but the search trigger needs visible separation. **Required this phase:** add `dark:bg-slate-700` override to the search trigger wrapper only (not token-level). `--muted-foreground` text at slate-400 on slate-700 must pass WCAG AA 4.5:1 — verified in audit.
- Placeholder copy: `Search for a module, sub-module, or action...` (see Copywriting Contract).
- Cmd+K hint: `<kbd className="ml-2 rounded border border-border bg-background px-1.5 py-0.5 text-xs text-muted-foreground">⌘K</kbd>`.

**BUG-02 interaction contract (navbar command-palette module nav):**

- Selecting a module-level entry from the command palette MUST navigate to that module's page reliably.
- Fix recipe (CONTEXT D-28 + research deep-dive): change `cmdk`'s `CommandItem` props from `value={`${item.label} ${item.path}`}` to `value={item.path}` + `keywords={[item.label]}`. The `value` must be unique (the path) to avoid cmdk's default normalization collision when a module path is a prefix of its sub-module paths. `keywords` keeps the label searchable.
- `onSelect` closure-captures `item.path` and calls `navigate(item.path)` then `setOpen(false)` — in that order, so `setOpen` does not unmount the handler mid-navigate.
- No new `useEffect`. No router imperative API beyond `useNavigate()`.

### Surface 4 — Dark-mode shell surfaces (DARK-02, DARK-03)

**The one-line fix (research recommendation, locked):**

```css
/* app/styles/shadcn-ui.css .dark block */
.dark {
  /* existing tokens... */
  --sidebar-background: #1e293b; /* was #0f172a — collided with --background */
  --sidebar-border: #334155;
}
```

That's the entire DARK-03 structural fix. Navbar already consumes `bg-card` (slate-800). Sidebar now also renders on slate-800 via `var(--sidebar-background)` through the Shadcn sidebar primitive. Page canvas remains slate-900 via `var(--background)`. The navbar and sidebar now sit on a distinct elevated surface above the page.

**Theme toggle no-regression contract (DARK-02):**
- The `next-themes` toggle (existing, in profile menu) must produce zero layout shifts on transition.
- Playwright regression: snapshot the DOM dimensions of `<main>`, `<aside>` (sidebar), and `<nav>` (navbar) before/after `setTheme('dark')` and `setTheme('light')`. Asserted equal within 1px tolerance.
- No new classes appear/disappear on toggle. All tokens resolve in both modes.

**Surfaces table (post-fix):**

| Surface | Light bg | Dark bg | Foreground pair |
|---------|----------|---------|-----------------|
| Page canvas | `#f1f5f9` | `#0f172a` | body 16.30:1 / 16.30:1 — PASS |
| Navbar | `#ffffff` | `#1e293b` | card-fg 17.85:1 / 15.81:1 — PASS |
| Sidebar | `#ffffff` | `#1e293b` | sidebar-fg TBD in audit — likely PASS |
| Card / AG Grid row | `#ffffff` | `#1e293b` | card-fg — PASS |
| Muted / Hover / Odd row | `#f1f5f9` | `#334155` | muted-fg 4.34:1 light (known FAIL) / TBD dark |
| Active pill | gradient green-500→emerald-600 | gradient green-400→emerald-500 | white / green-950 — TBD in audit |
| Sub-item chip | `#f0fdf4` | `#14532d` | green-700 / green-200 — TBD in audit |

### Surface 5 — Scrollbars (PARITY-05)

See "Scrollbar token contract" above under Color. One global rule. No per-component overrides. Applies to sidebar, main content, AG Grid virtual scroller, and every other scrollable region. Firefox fallback via `scrollbar-width: thin; scrollbar-color: var(--border) transparent` on `html`.

### Surface 6 — Navbar toggle placement (PARITY-02)

**Verify, not implement.** Already shipped in Phase 9. Contract: `PanelLeft` icon button is the leftmost visible control in `workspace-navbar.tsx`, rendered before the Aloha logo square. If verification finds a regression (e.g., logo at index 0), fix by swapping order. No structural work expected.

---

## Copywriting Contract

Phase 10 does not introduce new features or destructive actions. The copy surfaces are limited to section labels, placeholders, empty states for regressions, and the existing "Focused" footer label.

| Element | Copy |
|---------|------|
| Primary CTA | **N/A** — this phase introduces no new CTAs. Existing Phase 8 primary buttons keep their labels unchanged. |
| Sidebar NAVIGATION section header | `NAVIGATION` (all caps, uppercase via CSS) |
| Sidebar MODULES section header | `MODULES` (all caps, uppercase via CSS) |
| Sidebar "Focused" footer | `Focused` (title case, disabled visual state, no action) |
| Navbar search placeholder | `Search for a module, sub-module, or action...` |
| Navbar search keyboard hint | `⌘K` (mac) — rendered as `<kbd>` element |
| Navbar search empty result | `No results found.` (cmdk default — keep) |
| AG Grid empty state heading | `No data to display` (AG Grid default — keep) |
| AG Grid empty state body | `Try adjusting your filters or search.` (AG Grid default `overlayNoRowsTemplate` — keep if already set, else leave default) |
| Error state (shell) | Handled by existing `RootErrorBoundary` — no new error copy this phase |
| Destructive confirmation | **None this phase** — no destructive actions introduced |
| Avatar initials fallback | Computed (e.g., `HF`) — falls back to single letter from user email, then `A` |
| Theme toggle label | Existing profile menu copy — unchanged |

**Tone guidance for section labels:** Prototype uses ALL CAPS for section headers. Aloha follows suit — lowercase section labels ("Navigation", "Modules") are explicitly rejected. The `uppercase` utility + `tracking-wider` preserves legibility.

**i18n note:** Section header copy (`NAVIGATION`, `MODULES`, `Focused`) and the search placeholder should route through existing i18next keys if the app's i18n surface covers shell chrome. If keys do not yet exist, add them to `public/locales/en/common.json` under `shell.sidebar.nav_section`, `shell.sidebar.modules_section`, `shell.sidebar.focused_footer`, `shell.navbar.search_placeholder`. Planner decides based on Phase 9 i18n scope; default is add the keys (cheapest future-proofing).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official (internal workspace package `@aloha/ui`) | SidebarGroup, SidebarMenu, SidebarMenuButton, SidebarSeparator, SidebarGroupLabel, SidebarGroupContent, Command, CommandDialog, CommandItem, Input, Avatar, AvatarFallback, Button, Badge | not required (internal, read-only this phase) |
| Third-party registries | **none** | not applicable |

**Zero third-party registry blocks** this phase. CONTEXT and CLAUDE.md explicitly forbid new UI libraries. All composition uses already-installed `@aloha/ui` primitives (which themselves wrap Radix). Registry vetting gate: **not triggered**.

---

## Out-of-Scope Reminders (from CONTEXT)

The checker and auditor should flag any attempt to touch these in Phase 10:

- Font migration Geist → Inter **across the entire app** (Phase 7 already did this; AG Grid theme font update is the only font touch this phase).
- Real command palette with fuzzy search, grouped actions, recent history.
- Profile menu relocation from sidebar footer → navbar avatar dropdown.
- "Focused" / "All Apps" dual-mode sidebar nav (render disabled only).
- Mobile-specific AG Grid responsive layout.
- AG Grid Enterprise features.
- Any change to `packages/ui/src/shadcn/sidebar.tsx` (primitive is read-only).
- New modules, new routes, new nav items.
- Schema changes, RLS changes, loader contract changes.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
