---
phase: 09-app-shell-navbar-sidebar-drawer
plan: 05
type: execute
wave: 4
depends_on: [09-01, 09-02, 09-03, 09-04]
files_modified:
  - .planning/STATE.md
  - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md
requirements: [NAVBAR-01, NAVBAR-02, NAVBAR-03, NAVBAR-04, SIDEBAR-01, SIDEBAR-02, SIDEBAR-03, SIDEBAR-04, SIDEBAR-05, DRAWER-01, DRAWER-02, DRAWER-03, DRAWER-04, DRAWER-05]
autonomous: true
must_haves:
  truths:
    - "All 14 phase requirement grep assertions pass against the shipped code"
    - "Manual smoke checklist (D-34 items 1-5) documented as passed or failing items recorded"
    - "No regressions in typecheck, lint, or existing workspace routes"
    - "Pitfall 1 (closed shadcn Sheet on mobile) smoke-checked and resolved or logged"
    - "STATE.md updated to reflect Phase 9 completion"
  artifacts:
    - path: .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md
      provides: "Full phase verification report with grep results, a11y audit, and smoke checklist"
      min_lines: 40
    - path: .planning/STATE.md
      provides: "Updated to Phase 9 complete"
  key_links:
    - from: "09-VERIFICATION.md"
      to: "all 14 REQ-IDs"
      via: "grep command + pass/fail status"
      pattern: "NAVBAR|SIDEBAR|DRAWER"
---

<objective>
Execute the Phase 9 verification gate. Run the grep assertion suite against the shipped code,
perform the D-34 manual smoke checklist (desktop light/dark, mobile 375px, org switch, CRUD
Outlet), run the a11y audit (keyboard-only drawer open/close, focus management, screen reader
labels), check Pitfall 1 (closed shadcn Sheet on mobile), document findings in a VERIFICATION.md,
and update STATE.md to mark Phase 9 complete.

Purpose: Close the phase with recorded evidence that all 14 requirements are satisfied.
Output: `.planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md` + STATE.md bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-CONTEXT.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-VALIDATION.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-01-PLAN.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-02-PLAN.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-03-PLAN.md
@.planning/phases/09-app-shell-navbar-sidebar-drawer/09-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full grep assertion suite and typecheck/lint gate</name>
  <read_first>
    - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VALIDATION.md (per-task verification map — 22 tasks)
    - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-CONTEXT.md (D-33, D-34)
  </read_first>
  <files>(no code edits — verification only)</files>
  <action>
    Run each of these commands and record pass/fail in a scratch table. If any FAIL, STOP and
    report to the user; do NOT attempt to patch code in this plan (that is a follow-up gap
    closure).

    Gate 1 — Typecheck + Lint:
    - `pnpm typecheck`
    - `pnpm lint`

    Gate 2 — SIDEBAR grep assertions:
    - `rg "13.75rem" packages/ui/src/shadcn/sidebar.tsx` (SIDEBAR-01)
    - `rg "4.25rem" packages/ui/src/shadcn/sidebar.tsx` (SIDEBAR-01)
    - `rg "from-green-500 to-emerald-600" app/components/sidebar/module-sidebar-navigation.tsx` (SIDEBAR-02)
    - `rg "shadow-green-500/25" app/components/sidebar/module-sidebar-navigation.tsx` (SIDEBAR-02)
    - `rg "bg-green-50" app/components/sidebar/module-sidebar-navigation.tsx` (SIDEBAR-03)
    - `rg "text-green-700" app/components/sidebar/module-sidebar-navigation.tsx` (SIDEBAR-03)
    - `rg "border-l-2 border-green-200 ml-5 pl-3" app/components/sidebar/module-sidebar-navigation.tsx` (SIDEBAR-03)
    - `rg "PanelLeft" app/components/sidebar/workspace-sidebar.tsx` (SIDEBAR-04)
    - `rg "hidden md:" app/routes/workspace/layout.tsx` (SIDEBAR-05)
    - `rg "onNavigate\?: \(\) => void" app/components/sidebar/module-sidebar-navigation.tsx` (DRAWER-05 contract)
    - `rg "forceExpanded\?: boolean" app/components/sidebar/module-sidebar-navigation.tsx` (DRAWER-05 contract)

    Gate 3 — NAVBAR grep assertions:
    - `rg "renderTrigger" app/components/navbar-search.tsx` (NAVBAR-02 seam)
    - `rg "h-\[72px\]" app/components/workspace-shell/workspace-navbar.tsx` (NAVBAR-01)
    - `rg "from-green-500 to-emerald-600" app/components/workspace-shell/aloha-logo-square.tsx` (NAVBAR-01 gradient logo)
    - `rg "from '@aloha/ui/avatar'" app/components/workspace-shell/workspace-navbar.tsx` (NAVBAR-03)
    - `rg "md:hidden" app/components/workspace-shell/workspace-mobile-header.tsx` (NAVBAR-04 visibility)

    Gate 4 — DRAWER grep assertions:
    - `rg '"framer-motion"' package.json` (DRAWER-03 install)
    - `rg "bg-black/30" app/components/workspace-shell/workspace-mobile-drawer.tsx` (DRAWER-01)
    - `rg "type: 'spring', damping: 25, stiffness: 300" app/components/workspace-shell/workspace-mobile-drawer.tsx` (DRAWER-03)
    - `rg "ModuleSidebarNavigation" app/components/workspace-shell/workspace-mobile-drawer.tsx` (DRAWER-05)
    - `rg "forceExpanded" app/components/workspace-shell/workspace-mobile-drawer.tsx` (DRAWER-05 consumer)
    - `rg "onNavigate=\{onClose\}" app/components/workspace-shell/workspace-mobile-drawer.tsx` (DRAWER-04 leaf close)
    - `rg "role=\"dialog\"" app/components/workspace-shell/workspace-mobile-drawer.tsx` (a11y D-27)
    - `rg "aria-modal=\"true\"" app/components/workspace-shell/workspace-mobile-drawer.tsx` (a11y D-27)

    Gate 5 — Layout integration:
    - `rg "WorkspaceNavbar" app/routes/workspace/layout.tsx`
    - `rg "WorkspaceMobileHeader" app/routes/workspace/layout.tsx`
    - `rg "WorkspaceMobileDrawer" app/routes/workspace/layout.tsx`
    - `rg "return \{ workspace, layoutState, accountSlug \}" app/routes/workspace/layout.tsx` (loader contract preserved — success criterion #5)

    Gate 6 — Cleanup:
    - `test -f app/components/sidebar/mobile-navigation.tsx` (MUST exit non-zero — file deleted)
    - `! rg "SidebarTrigger" app/routes/workspace/layout.tsx` (old trigger gone)

    Record each in the VERIFICATION.md report (Task 3).
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; rg "13.75rem" packages/ui/src/shadcn/sidebar.tsx &amp;&amp; rg "4.25rem" packages/ui/src/shadcn/sidebar.tsx &amp;&amp; rg "from-green-500 to-emerald-600" app/components/sidebar/module-sidebar-navigation.tsx &amp;&amp; rg "bg-green-50" app/components/sidebar/module-sidebar-navigation.tsx &amp;&amp; rg "border-l-2 border-green-200 ml-5 pl-3" app/components/sidebar/module-sidebar-navigation.tsx &amp;&amp; rg "PanelLeft" app/components/sidebar/workspace-sidebar.tsx &amp;&amp; rg "h-\[72px\]" app/components/workspace-shell/workspace-navbar.tsx &amp;&amp; rg "renderTrigger" app/components/navbar-search.tsx &amp;&amp; rg "md:hidden" app/components/workspace-shell/workspace-mobile-header.tsx &amp;&amp; rg '"framer-motion"' package.json &amp;&amp; rg "bg-black/30" app/components/workspace-shell/workspace-mobile-drawer.tsx &amp;&amp; rg "type: 'spring', damping: 25, stiffness: 300" app/components/workspace-shell/workspace-mobile-drawer.tsx &amp;&amp; rg "role=\"dialog\"" app/components/workspace-shell/workspace-mobile-drawer.tsx &amp;&amp; rg "WorkspaceNavbar" app/routes/workspace/layout.tsx &amp;&amp; rg "WorkspaceMobileDrawer" app/routes/workspace/layout.tsx &amp;&amp; rg "return \{ workspace, layoutState, accountSlug \}" app/routes/workspace/layout.tsx &amp;&amp; ! test -f app/components/sidebar/mobile-navigation.tsx &amp;&amp; ! rg "SidebarTrigger" app/routes/workspace/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All grep commands in the Gate lists above return the expected match counts
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
    - `app/components/sidebar/mobile-navigation.tsx` does not exist
    - All 14 REQ-IDs have at least one passing grep assertion recorded
  </acceptance_criteria>
  <done>Automated gate passes; findings recorded for Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Manual smoke + a11y audit (human-assisted via pnpm dev)</name>
  <read_first>
    - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-CONTEXT.md (D-34, D-27, D-28)
    - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/09-app-shell-navbar-sidebar-drawer/09-RESEARCH.md §9 (Pitfalls 1, 5, 7)
  </read_first>
  <files>(no code edits — browser verification via pnpm dev)</files>
  <action>
    Start `pnpm dev` in the background and navigate to `/home/:account` with a valid session. Work
    through each of the D-34 checklist items and record pass/fail + observations:

    1. Desktop light mode (`/home/:account`, viewport ≥1024px, next-themes=light):
       - Navbar: 72px tall, gradient logo square left, "Aloha" wordmark, centered search button,
         avatar right.
       - Click search button → existing NavbarSearch dialog opens. Press Cmd+K → same dialog
         opens. Close dialog.
       - Sidebar: 220px wide, active module has gradient pill + shadow.
       - Sub-items under active module: active sub-item has `bg-green-50 text-green-700` chip +
         `border-green-200` left rail.
       - Click `PanelLeft` toggle → sidebar collapses to 68px. Reload page → still 68px (cookie
         persistence). Click again → expands to 220px. Reload → still 220px.

    2. Desktop dark mode (`next-themes` toggle → dark):
       - Navbar/sidebar borders + surfaces readable.
       - Active module gradient pill still looks correct on dark background.
       - Active sub-item `green-50` chip: if it looks harsh on dark slate, LOG IT AS A PHASE 10
         FOLLOW-UP in STATE.md. Do NOT fix here (D-11 defers dark-mode chip tuning to Phase 10).

    3. Mobile viewport (Chrome devtools 375×812 iPhone):
       - Desktop sidebar HIDDEN.
       - Compact mobile header visible with hamburger (left), `AlohaLogoSquare` + "Aloha"
         wordmark (center-left), avatar (right).
       - Tap hamburger → drawer slides in from left with spring physics; black/30 backdrop
         fades in.
       - Tap backdrop → drawer closes (reverse animation).
       - Tap hamburger again → drawer opens. Tap a leaf sub-item → drawer closes AND navigation
         occurs.
       - Open drawer again → press Escape key → drawer closes.
       - Open drawer → verify focus moved to first interactive nav element. Close drawer (any
         mechanism) → verify focus returns to hamburger button.

    4. Pitfall 1 smoke (closed shadcn Sheet on mobile):
       - At 375px viewport, open the elements panel. Find any DOM node from shadcn Sheet (usually
         a sibling of SidebarProvider root with `data-state="closed"`). Verify it does NOT
         intercept pointer events (click through to underlying content works). If it DOES block
         clicks or create a z-index conflict, log as a BLOCKER — follow-up fix is to move
         `SidebarProvider` to the desktop branch only.

    5. Org switch via sidebar profile menu (desktop):
       - Click profile menu at sidebar footer → switch org → verify page reloads and navigation
         updates. Loader contract must still work.

    6. CRUD list route regression:
       - Navigate to any HR module list (e.g., `/home/:account/hr/employees`) → AG Grid renders
         unchanged → Outlet content intact.

    7. Hamburger keyboard accessibility:
       - Tab through the mobile header → hamburger receives focus ring → press Enter → drawer
         opens → Escape → drawer closes → focus on hamburger.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - All 7 manual checklist sections executed and recorded (pass/fail/observations) in the
      VERIFICATION.md report (Task 3)
    - Any dark-mode green-50 harshness noted as a Phase 10 follow-up (NOT a Phase 9 blocker)
    - Any Pitfall 1 click-blocking evidence captured and either resolved or flagged as blocker
    - No regressions in org switch or CRUD Outlet
  </acceptance_criteria>
  <done>Manual verification evidence captured for the VERIFICATION.md report.</done>
</task>

<task type="auto">
  <name>Task 3: Write 09-VERIFICATION.md report and update STATE.md</name>
  <read_first>
    - Results from Task 1 (grep/typecheck/lint) and Task 2 (manual smoke)
    - .planning/STATE.md (current shape, for the in-place update)
    - .planning/ROADMAP.md (Phase 9 entry — flip to [x] complete)
  </read_first>
  <files>
    .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    1. Create `.planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md` with:
       - YAML frontmatter: `phase: 09-app-shell-navbar-sidebar-drawer`, `verified: <ISO date>`,
         `status: passed` (or `partial` if any blockers), `verified_by: claude`.
       - `## Grep Assertion Results` table: one row per REQ-ID (NAVBAR-01..04, SIDEBAR-01..05,
         DRAWER-01..05) with columns REQ-ID | Command | Result | Pass/Fail.
       - `## Typecheck / Lint Gate` section: exit codes for `pnpm typecheck` and `pnpm lint`.
       - `## Manual Smoke Results` table: one row per D-34 item (1-5 plus the Pitfall 1 check and
         keyboard a11y check) with observations.
       - `## A11y Audit` section: drawer role/aria-modal/aria-label confirmed; keyboard
         open/close via hamburger + Escape; focus-on-open + focus-return-on-close confirmed.
       - `## Phase 10 Follow-ups` section: log any dark-mode green-50 chip harshness or other
         deferred items.
       - `## Conclusion`: "Phase 9 complete — all 14 requirements satisfied" or "Phase 9 partial —
         blockers: [list]".

    2. Update `.planning/STATE.md`:
       - `stopped_at: Phase 9 complete`
       - `last_updated: <ISO>`
       - `progress.completed_phases: <current + 1>`
       - `progress.completed_plans: <current + 5>` (add the 5 plans from Phase 9)
       - Under `## Current Position`: bump Phase to 10, Plan to 1 of N, next action to
         `/gsd-plan-phase 10`.
       - Append row to the `Performance Metrics > By Phase` table for Phase 9.

    3. Update `.planning/ROADMAP.md` Phase 9 entry: change `[ ]` to `[x]`, mark plans complete,
       add `— completed <date>` to the phase header. Also update the Progress table at the bottom
       (Phase 9 row: 5/5 complete).

    4. Do NOT commit — the workflow runner handles the final commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md &amp;&amp; rg "Phase 9 complete|Phase 9 partial" .planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md &amp;&amp; rg "stopped_at: Phase 9 complete" .planning/STATE.md &amp;&amp; rg "\[x\] \*\*Phase 9" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-app-shell-navbar-sidebar-drawer/09-VERIFICATION.md` exists and is ≥40 lines
    - All 14 REQ-IDs appear in the VERIFICATION.md grep assertion table
    - `.planning/STATE.md` reflects Phase 9 completion
    - `.planning/ROADMAP.md` Phase 9 entry is marked `[x]`
    - `pnpm typecheck` and `pnpm lint` still exit 0 (documentation-only changes should not
      affect code)
  </acceptance_criteria>
  <done>Phase 9 closed. Evidence recorded. Ready for Phase 10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Verification / documentation only. No code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Repudiation | VERIFICATION.md as single source of audit trail | accept | Markdown under version control; git history preserves authorship and timestamps. |
| T-09-05-02 | Tampering | STATE.md / ROADMAP.md updates | accept | Plain markdown, reviewed by typecheck/lint gate passing unchanged. |

No code attack surface touched in this plan.
</threat_model>

<verification>
- Task 1 automated gate passes (all greps + typecheck + lint).
- Task 2 manual smoke recorded.
- Task 3 VERIFICATION.md + STATE.md + ROADMAP.md updated and committed.
</verification>

<success_criteria>
- All 14 REQ-IDs have a passing grep assertion recorded in 09-VERIFICATION.md.
- D-34 manual smoke checklist executed and recorded.
- A11y audit (dialog role, aria-modal, keyboard open/close, focus management) passes.
- Loader contract invariant confirmed (success criterion #5).
- STATE.md and ROADMAP.md reflect Phase 9 complete.
- Any dark-mode green-50 chip harshness logged as a Phase 10 follow-up, NOT fixed here.
</success_criteria>

<output>
After completion, create `.planning/phases/09-app-shell-navbar-sidebar-drawer/09-05-SUMMARY.md`.
</output>
