---
phase: 07-design-foundations
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/verify-wcag.mjs
  - package.json
  - DESIGN.md
autonomous: false
requirements: [DESIGN-04, DARK-01]
must_haves:
  truths:
    - "A Node script at scripts/verify-wcag.mjs checks all 12 foundation token pairs × 2 themes (24 assertions) against WCAG AA thresholds (4.5:1 body text, 3:1 UI/large) and exits non-zero on any failure."
    - "wcag-contrast@3.0.0 is a root devDependency."
    - "Running `node scripts/verify-wcag.mjs` exits 0 against the Plan 01 Aloha palette (with the --primary/--primary-foreground pair evaluated at 3:1 per D-14 Option C)."
    - "Existing routes (home + one sub-module) still render in both light and dark without layout breakage or missing CSS var warnings — verified manually in a checkpoint."
    - "DESIGN.md §WCAG Verification table is populated with actual ratios from the script run."
  artifacts:
    - path: "scripts/verify-wcag.mjs"
      provides: "WCAG AA verification for foundation token pairs"
      min_lines: 40
    - path: "package.json"
      provides: "wcag-contrast devDependency"
      contains: "wcag-contrast"
    - path: "DESIGN.md"
      provides: "Populated WCAG ratios table"
      contains: "4.5"
  key_links:
    - from: "scripts/verify-wcag.mjs"
      to: "hex values from DESIGN.md / shadcn-ui.css (hard-coded)"
      via: "Node import of wcag-contrast"
      pattern: "import.*wcag-contrast"
---

<objective>
Build a Node-based WCAG verification script that asserts the 12 foundation token pairs hit AA in both light and dark themes, run it, populate DESIGN.md's placeholder WCAG table with real ratios, and perform a manual shell smoke check (home + one sub-module in light and dark) to confirm Plan 01+02 didn't break any existing route visually.

Purpose: Closes DESIGN-04 (WCAG verified) and DARK-01 (dark palette passes AA at the foundation level per D-14). Provides a reusable script for Phase 10's full shell/AG Grid audit.

Output:
- `scripts/verify-wcag.mjs` — ESM Node script, wcag-contrast-powered, 24 assertions.
- `package.json` — `wcag-contrast@3.0.0` added to `devDependencies`.
- `DESIGN.md` — WCAG table updated with real ratios from the script run.
- Manual smoke checkpoint log captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-foundations/07-CONTEXT.md
@.planning/phases/07-design-foundations/07-RESEARCH.md
@.planning/phases/07-design-foundations/07-VALIDATION.md
@.planning/phases/07-design-foundations/07-01-SUMMARY.md
@.planning/phases/07-design-foundations/07-02-SUMMARY.md
@DESIGN.md
@app/styles/shadcn-ui.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wcag-contrast devDependency and create scripts/verify-wcag.mjs</name>
  <files>scripts/verify-wcag.mjs, package.json</files>
  <read_first>
    - package.json (to confirm devDependencies shape)
    - app/styles/shadcn-ui.css (Plan 01 output — source of truth for actual hex values)
    - DESIGN.md (Plan 01 output — WCAG placeholder table)
    - .planning/phases/07-design-foundations/07-CONTEXT.md (D-14 foundation-pairs-only scope)
    - .planning/phases/07-design-foundations/07-RESEARCH.md (§WCAG Verification Approach — full pair list and example script shape)
    - .planning/phases/07-design-foundations/07-VALIDATION.md (§Wave 0 Requirements)
  </read_first>
  <action>
    Step 1 — Install devDependency:

    ```
    pnpm add -wD wcag-contrast@3.0.0
    ```

    (The `-w` flag installs at the workspace root; `-D` marks it dev.)

    Step 2 — Create `scripts/verify-wcag.mjs` (new file). The script must:

    1. Be a standalone ESM Node file (no TypeScript, no Vite).
    2. Import `hex` from `wcag-contrast`.
    3. Hard-code the 12 foundation pairs × 2 themes = 24 assertions. Each entry has `{ name, theme, fg, bg, min }` where `min` is 4.5 (body text) or 3.0 (UI / large text).
    4. For each pair: compute `hex(fg, bg)`, compare against `min`, print a PASS/FAIL line with the ratio to 2 decimals.
    5. Exit with status 0 if all pass, 1 if any fail.
    6. Print a final markdown block suitable for pasting into DESIGN.md §WCAG Verification (a table with columns: Pair | Theme | Ratio | Min | Status).

    Pair list (24 entries — all hex values from Plan 01 shadcn-ui.css):

    Light (`:root`):
    - (1) `--foreground #0f172a` on `--background #f1f5f9` — min 4.5
    - (2) `--card-foreground #0f172a` on `--card #ffffff` — min 4.5
    - (3) `--popover-foreground #0f172a` on `--popover #ffffff` — min 4.5
    - (4) `--primary-foreground #ffffff` on `--primary #22c55e` — min 3.0 (UI/large per D-14 Option C)
    - (5) `--secondary-foreground #0f172a` on `--secondary #f1f5f9` — min 4.5
    - (6) `--muted-foreground #64748b` on `--background #f1f5f9` — min 4.5
    - (7) `--muted-foreground #64748b` on `--muted #f1f5f9` — min 4.5
    - (8) `--accent-foreground #0f172a` on `--accent #f1f5f9` — min 4.5
    - (9) `--destructive-foreground #ffffff` on `--destructive #dc2626` — min 3.0
    - (10) `--border #e2e8f0` on `--background #f1f5f9` — min 3.0
    - (11) `--ring #22c55e` on `--background #f1f5f9` — min 3.0
    - (12) `--sidebar-foreground #475569` on `--sidebar-background #ffffff` — min 4.5

    Dark (`.dark`):
    - (13) `--foreground #f8fafc` on `--background #0f172a` — min 4.5
    - (14) `--card-foreground #f8fafc` on `--card #1e293b` — min 4.5
    - (15) `--popover-foreground #f8fafc` on `--popover #1e293b` — min 4.5
    - (16) `--primary-foreground #052e16` on `--primary #4ade80` — min 3.0
    - (17) `--secondary-foreground #f8fafc` on `--secondary #334155` — min 4.5
    - (18) `--muted-foreground #94a3b8` on `--background #0f172a` — min 4.5
    - (19) `--muted-foreground #94a3b8` on `--muted #1e293b` — min 4.5
    - (20) `--accent-foreground #f8fafc` on `--accent #334155` — min 4.5
    - (21) `--destructive-foreground #ffffff` on `--destructive #ef4444` — min 3.0
    - (22) `--border #334155` on `--background #0f172a` — min 3.0
    - (23) `--ring #4ade80` on `--background #0f172a` — min 3.0
    - (24) `--sidebar-foreground #cbd5e1` on `--sidebar-background #0f172a` — min 4.5

    Script skeleton (write this literally, filling in all 24 pairs):

    ```js
    #!/usr/bin/env node
    // scripts/verify-wcag.mjs
    // Verifies WCAG AA contrast for Phase 7 foundation token pairs.
    // Runs 24 assertions (12 pairs × 2 themes). Exits 1 on any failure.
    import { hex } from 'wcag-contrast';

    const pairs = [
      // LIGHT
      { name: 'foreground/background',       theme: 'light', fg: '#0f172a', bg: '#f1f5f9', min: 4.5 },
      { name: 'card-foreground/card',        theme: 'light', fg: '#0f172a', bg: '#ffffff', min: 4.5 },
      // ... all 24 entries
    ];

    let failures = 0;
    const rows = [];
    for (const p of pairs) {
      const ratio = hex(p.fg, p.bg);
      const pass = ratio >= p.min;
      if (!pass) failures += 1;
      const status = pass ? 'PASS' : 'FAIL';
      console.log(`${pass ? '✓' : '✗'} [${p.theme}] ${p.name}: ${ratio.toFixed(2)}:1 (min ${p.min}) ${status}`);
      rows.push(`| ${p.name} | ${p.theme} | ${ratio.toFixed(2)}:1 | ${p.min}:1 | ${status} |`);
    }

    console.log('\n--- Markdown table for DESIGN.md §WCAG Verification ---\n');
    console.log('| Pair | Theme | Ratio | Min | Status |');
    console.log('|------|-------|-------|-----|--------|');
    for (const row of rows) console.log(row);

    if (failures > 0) {
      console.error(`\n${failures} WCAG failure(s). Review Plan 01 palette.`);
      process.exit(1);
    }
    console.log('\nAll 24 assertions PASS.');
    ```

    Step 3 — Run it: `node scripts/verify-wcag.mjs`. If any assertion fails:
    - Re-check the hex values in the script match `app/styles/shadcn-ui.css` exactly.
    - If the values match but contrast fails, the ONLY known risk pair is `#ffffff on #22c55e` (~2.75:1) which is scoped to 3:1 per D-14 Option C — if this fails 3:1 as well, STOP and escalate: the locked decision assumed it would clear 3:1 and a retune is needed. Do NOT silently change values — this is a decision-level concern.

    Step 4 — Do NOT touch any file outside `scripts/verify-wcag.mjs` and `package.json` in this task. DESIGN.md is updated in Task 2 after the script is green.
  </action>
  <verify>
    <automated>grep -q "wcag-contrast" package.json &amp;&amp; test -f scripts/verify-wcag.mjs &amp;&amp; grep -q "import { hex } from 'wcag-contrast'" scripts/verify-wcag.mjs &amp;&amp; node scripts/verify-wcag.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\"wcag-contrast\":" package.json` (devDependency present)
    - `grep -q "3.0.0" package.json` (pinned — either `3.0.0` or `^3.0.0`)
    - `test -f scripts/verify-wcag.mjs`
    - `grep -q "import { hex } from 'wcag-contrast'" scripts/verify-wcag.mjs`
    - Script has 24 pair entries: `grep -c "fg:" scripts/verify-wcag.mjs` returns at least 24
    - `grep -q "process.exit(1)" scripts/verify-wcag.mjs` (fails non-zero on errors)
    - `node scripts/verify-wcag.mjs` exits 0 (all 24 assertions pass)
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>verify-wcag.mjs exists, wcag-contrast installed, script prints 24 PASS lines + markdown table and exits 0. Quality gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Paste WCAG ratios into DESIGN.md §WCAG Verification</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (Plan 01 output — §WCAG Verification placeholder)
    - scripts/verify-wcag.mjs (just written — run it to capture fresh output)
  </read_first>
  <action>
    1. Run `node scripts/verify-wcag.mjs` and capture the stdout markdown-table block (everything between `--- Markdown table for DESIGN.md §WCAG Verification ---` and the final `All 24 assertions PASS.` line).

    2. Open `DESIGN.md` and locate the §WCAG Verification section (contains 24 "pending" entries from Plan 01 Task 1).

    3. REPLACE the placeholder table with the captured markdown table (columns: Pair | Theme | Ratio | Min | Status). Add a one-line header above the table:

    ```
    Verified via `node scripts/verify-wcag.mjs` on {YYYY-MM-DD}. 24/24 pass.
    ```

    4. Leave every other DESIGN.md section untouched.
  </action>
  <verify>
    <automated>grep -q "24/24 pass" DESIGN.md &amp;&amp; grep -q "| Pair | Theme | Ratio | Min | Status |" DESIGN.md &amp;&amp; grep -q "PASS" DESIGN.md &amp;&amp; ! grep -q "pending" DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "24/24 pass" DESIGN.md`
    - `grep -q "| Pair | Theme | Ratio | Min | Status |" DESIGN.md` (table header present)
    - `grep -c "PASS" DESIGN.md` returns at least 24 (one per row)
    - `! grep -qi "pending" DESIGN.md` (placeholder text removed from §WCAG Verification)
    - `! grep -qi "supabase" DESIGN.md` (sanity re-check)
    - `node scripts/verify-wcag.mjs` still exits 0 (no drift)
  </acceptance_criteria>
  <done>DESIGN.md §WCAG Verification has a real-ratio table with 24 entries and a verified-on date line. Script still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke check — home + register submodule in light and dark</name>
  <files>N/A — manual visual verification only, no file writes</files>
  <read_first>
    - .planning/phases/07-design-foundations/07-CONTEXT.md (D-16 manual smoke check requirement)
    - .planning/phases/07-design-foundations/07-RESEARCH.md (§Pitfall 3 shadow unlock safety check)
  </read_first>
  <what-built>
    Plans 01+02+03 Tasks 1-2: Aloha palette, Inter font, shadow unlock, WCAG-verified foundation tokens. Every existing route should render visually correct under the new tokens with zero layout breakage.
  </what-built>
  <action>
    Human operator performs a visual smoke check in a browser. Agent does NOT write any files in this task — only launches the dev server and waits for the operator's resume signal.

    Steps for the operator:

    1. Start dev server: `pnpm dev` (agent may launch this in background).
    2. Sign in as a test user.
    3. Load `/home/:account` (home dashboard). Confirm:
       - No console errors (open DevTools → Console).
       - No missing CSS var warnings.
       - Sidebar, top nav, main content area render with visible borders and no broken layout.
       - Body text is in Inter (DevTools → Computed → `font-family` on `body` shows `'Inter Variable'` before fallbacks).
       - DevTools → Network → filter `inter` — a woff2 request shows 200.
    4. Load one sub-module, e.g. `/home/:account/hr/employees` (or any register submodule). Confirm the AG Grid table renders, headers visible, rows render without overlap.
    5. Toggle the theme (header theme toggle) → switch to dark. Confirm:
       - Background goes slate-900 dark, not pure black.
       - Primary CTAs show green (green-400 in dark).
       - Sidebar/header/content contrasts all legible.
       - No FOUC / no un-themed flashes.
    6. Toggle back to light. Confirm baseline visual again.
    7. Optional: run `grep -rn "shadow-" app/ packages/ui/` to enumerate any shadow utilities that were previously locked to `none`. Note any surprises in the checkpoint reply (informational — fixes are Phase 8/9).

    Resume-signal options:
    - `approved` — all six checks pass.
    - `approved-with-notes: <text>` — pass but note latent shadow hits or minor visual surprises for Phase 8/9 retrospective.
    - `issues: <text>` — describe what broke so Plan 01 or 02 can be revised.
  </action>
  <how-to-verify>
    Operator performs steps 1-7 in action block above and types a resume signal.
  </how-to-verify>
  <verify>
    <automated>MANUAL — checkpoint resume signal is the verification. Supplementary automated re-runs: `pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; node scripts/verify-wcag.mjs`</automated>
  </verify>
  <acceptance_criteria>
    - Operator replies with `approved` or `approved-with-notes: ...`
    - `pnpm typecheck` still exits 0
    - `pnpm lint` still exits 0
    - `node scripts/verify-wcag.mjs` still exits 0
    - Any `issues: ...` reply blocks phase completion and triggers a revision of Plan 01 or 02
  </acceptance_criteria>
  <done>Operator has visually confirmed home + one sub-module render correctly in both light and dark themes with Inter font loaded, no layout breakage, no console errors, and approved the smoke check.</done>
  <resume-signal>Type `approved`, `approved-with-notes: ...`, or `issues: ...`</resume-signal>
</task>

</tasks>

<threat_model>
  <scope>
    One Node verification script (read-only, no network, no filesystem writes beyond stdout), one devDependency add (`wcag-contrast@3.0.0`), a docs paste into DESIGN.md, and a manual visual smoke check. No auth, data, routes, server changes.
  </scope>
  <threat id="T-7-03-01" severity="low">
    Supply-chain risk from `wcag-contrast@3.0.0` devDependency.
    Mitigation: Version pinned to 3.0.0 (verified via `npm view` in 07-RESEARCH.md 2026-04-10). ~10KB, MIT-licensed, widely used a11y utility. Dev-only dependency, never ships to production runtime. Lockfile diff reviewed in PR.
  </threat>
  <threat id="T-7-03-02" severity="info">
    Hard-coded hex values in `scripts/verify-wcag.mjs` could drift from `app/styles/shadcn-ui.css` if tokens are later retuned without updating the script.
    Mitigation: Phase 10 will likely re-run this script; a drift would show up as a PASS/FAIL mismatch vs the DESIGN.md table. For Phase 7, the script is authoritative because it produces the numbers that get pasted into DESIGN.md in Task 2. Script comment notes "update these hex values when shadcn-ui.css changes".
  </threat>
</threat_model>

<verification>
- `node scripts/verify-wcag.mjs` exits 0.
- `pnpm typecheck && pnpm lint` green.
- DESIGN.md has real WCAG ratios, no "pending".
- Manual checkpoint passes (all six visual checks green in both themes).
</verification>

<success_criteria>
- 24/24 WCAG AA foundation assertions pass (with `--primary/--primary-foreground` evaluated at 3:1 UI threshold per D-14 Option C).
- Existing routes still render without layout breakage in both themes.
- `scripts/verify-wcag.mjs` reusable by Phase 10 for the full shell + AG Grid audit.
- Phase 7 goal (Design Foundations) met: tokens, fonts, radius, shadows, and WCAG-verified dark palette ready for Phase 8 primitive restyle to build on.
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-foundations/07-03-SUMMARY.md` documenting:
- Final `node scripts/verify-wcag.mjs` output (24/24 PASS)
- Markdown table pasted into DESIGN.md (verbatim)
- Manual smoke check log (home + register submodule, light + dark)
- Any latent `shadow-*` utility hits found via `grep` (informational — for Phase 8/9)
- Any deviations from expected ratios (should be zero)
</output>
