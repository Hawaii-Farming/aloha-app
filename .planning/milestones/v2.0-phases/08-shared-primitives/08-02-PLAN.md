---
plan_id: 08-02
phase: 8
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui/src/shadcn/badge.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-04]
must_haves:
  truths:
    - "Badge base renders rounded-full pill: `rounded-full border-transparent px-3 py-1 text-xs font-medium`"
    - "Badge `default` variant retuned from `bg-primary text-primary-foreground` to `bg-muted text-muted-foreground` (fixes §9.1 #9 2.28:1 contrast failure)"
    - "Badge `destructive` retuned to `bg-semantic-red-bg text-semantic-red-fg` for semantic consistency"
    - "All 7 existing variants preserved (default, secondary, destructive, outline, success, warning, info) — no additions, no removals"
    - "pnpm typecheck and pnpm lint pass"
  artifacts:
    - path: packages/ui/src/shadcn/badge.tsx
      provides: "Pill-shaped Badge with semantic variants matching Aloha spec"
      contains: "rounded-full border-transparent px-3 py-1 text-xs font-medium"
  key_links:
    - from: packages/ui/src/shadcn/badge.tsx
      to: app/components/**/StatusBadgeRenderer.tsx
      via: "Badge used by AG Grid status cell renderer (central consumer)"
      pattern: "bg-semantic-(green|amber|blue|red)-bg"
---

<objective>
Restyle the Badge primitive (PRIM-04) to pill shape with semantic Aloha color variants. Retune `default` (fixes §9.1 #9 contrast failure) and `destructive` (semantic pastel for consistency). Preserve all 7 existing variants verbatim.

Purpose: Badge drives all status pills in AG Grid list views via `StatusBadgeRenderer`. Pill shape + semantic colors are a brand-visibility anchor.
Output: One edited file; no new variants; no dropped variants; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite badgeVariants cva block with pill base + retuned variants</name>
  <files>packages/ui/src/shadcn/badge.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/badge.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-09, D-17, D-18)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"6. badge.tsx (PRIM-04)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Badge recipe + §9.1 #9 R2 remediation)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/Badge.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/badge.tsx`, rewrite ONLY the `badgeVariants` cva call. Keep all imports, forwardRef, displayName, `BadgeProps` interface, and the component export byte-identical.

    The new cva base string is EXACTLY:
      `'inline-flex items-center rounded-full border-transparent px-3 py-1 text-xs font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2'`

    The variants object MUST contain exactly these 7 keys under `variant` with these exact class strings (do NOT add `danger` or `neutral` aliases — caller audit confirmed zero call sites):

      default: 'bg-muted text-muted-foreground'
      secondary: 'bg-muted text-muted-foreground'
      destructive: 'bg-semantic-red-bg text-semantic-red-fg'
      outline: 'bg-background text-foreground border border-border'
      success: 'bg-semantic-green-bg text-semantic-green-fg'
      warning: 'bg-semantic-amber-bg text-semantic-amber-fg'
      info: 'bg-semantic-blue-bg text-semantic-blue-fg'

    `defaultVariants` keeps `variant: 'default'`. Do NOT change the `Badge` component body, the `className` prop pass-through, or the `VariantProps<typeof badgeVariants>` type. Do NOT add `size` variants. Do NOT introduce hardcoded slate/gray/white/black classes anywhere.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rounded-full border-transparent px-3 py-1 text-xs font-medium" packages/ui/src/shadcn/badge.tsx` → match
    - `grep -q "bg-semantic-red-bg text-semantic-red-fg" packages/ui/src/shadcn/badge.tsx` → match
    - `grep -q "bg-semantic-green-bg text-semantic-green-fg" packages/ui/src/shadcn/badge.tsx` → match
    - `grep -q "bg-semantic-amber-bg text-semantic-amber-fg" packages/ui/src/shadcn/badge.tsx` → match
    - `grep -q "bg-semantic-blue-bg text-semantic-blue-fg" packages/ui/src/shadcn/badge.tsx` → match
    - `grep -q "bg-muted text-muted-foreground" packages/ui/src/shadcn/badge.tsx` → match (used by `default` and `secondary`)
    - `grep -q "bg-primary text-primary-foreground" packages/ui/src/shadcn/badge.tsx` → no match (old default removed — §9.1 #9 remediation)
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-white|bg-black" packages/ui/src/shadcn/badge.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/badge.tsx` → no matches
    - `pnpm typecheck` exits 0
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    Badge renders rounded-full pill; all 7 semantic variants present with exact class strings from 08-UI-SPEC §6; §9.1 #9 contrast failure remediated; typecheck+lint green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on a presentational primitive.

## STRIDE Threat Register

No new threat surface — className-only edit on a visual primitive. No auth, data handling, input parsing, or network surface affected. Existing threat model for the Badge primitive (none) unchanged. Note: §9.1 #9 contrast remediation is an accessibility fix (WCAG 2.2 AA), not a security mitigation — recorded here for traceability only.
</threat_model>

<verification>
- `pnpm typecheck` green
- `pnpm lint` green
- All grep acceptance criteria pass
- Manual smoke (phase end): status pills render on an HR module list route in both themes
</verification>

<success_criteria>
- PRIM-04 fully satisfied per 08-UI-SPEC §6
- §9.1 #9 contrast failure remediated (default no longer uses primary face)
- Single commit `feat(08-02): badge Aloha restyle` after typecheck+lint pass
- Zero variant additions, zero variant removals, zero prop contract changes
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-02-SUMMARY.md` capturing: file changed, variant diff table, grep verification results, typecheck+lint status.
</output>
