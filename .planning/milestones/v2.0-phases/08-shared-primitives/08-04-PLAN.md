---
plan_id: 08-04
phase: 8
plan: 4
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/ui/src/shadcn/avatar.tsx
autonomous: true
nyquist_compliant: true
requirements: [PRIM-05]
must_haves:
  truths:
    - "Avatar gains an additive optional `size` prop with variants sm/md/lg (default md = existing h-10 w-10 → zero visual regression for current callers)"
    - "AvatarFallback renders on gradient: `bg-gradient-to-br from-green-500 to-emerald-600 text-white font-medium`"
    - "`avatarVariants` is exported alongside the component (matches buttonVariants/badgeVariants pattern)"
    - "All existing Avatar, AvatarImage, AvatarFallback exports preserved"
    - "pnpm typecheck and pnpm lint pass"
  artifacts:
    - path: packages/ui/src/shadcn/avatar.tsx
      provides: "Avatar primitive with size variants + gradient initials fallback"
      contains: "from-green-500 to-emerald-600"
  key_links:
    - from: packages/ui/src/shadcn/avatar.tsx
      to: "existing Avatar callers (workspace header, etc.)"
      via: "default size 'md' = h-10 w-10 → byte-compatible with current unparameterized usage"
      pattern: "size: 'md'"
---

<objective>
Restyle the Avatar primitive (PRIM-05): add an additive, optional `size` variant prop (sm/md/lg, default md) via a new `avatarVariants` cva block, and change the AvatarFallback background from `bg-muted` to the Aloha gradient with white initials. Every existing call site remains byte-compatible because `md` = the current `h-10 w-10`.

Purpose: Avatar initials fallback is the second gradient anchor (pairs with Button primary). Adding `size` is the one approved additive prop change this phase.
Output: One edited file; one additive optional prop; one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-shared-primitives/08-CONTEXT.md
@.planning/phases/08-shared-primitives/08-UI-SPEC.md
@.planning/phases/08-shared-primitives/08-RESEARCH.md
@packages/ui/CLAUDE.md
@packages/ui/src/shadcn/avatar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add avatarVariants cva + gradient fallback</name>
  <files>packages/ui/src/shadcn/avatar.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/avatar.tsx (current state)
    - .planning/phases/08-shared-primitives/08-CONTEXT.md (D-02, D-06, D-10)
    - .planning/phases/08-shared-primitives/08-UI-SPEC.md §"7. avatar.tsx (PRIM-05)"
    - .planning/phases/08-shared-primitives/08-RESEARCH.md (Avatar recipe)
    - /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/shared/Avatar.tsx
    - packages/ui/CLAUDE.md
  </read_first>
  <action>
    In `packages/ui/src/shadcn/avatar.tsx`, make the following changes:

    1. Add an import: `import { cva, type VariantProps } from 'class-variance-authority';` (match the style already used in `button.tsx` / `badge.tsx`).

    2. Define and export a new `avatarVariants` cva:
       - Base: `'relative flex shrink-0 overflow-hidden rounded-full'`
       - Variants.size:
         - `sm: 'h-8 w-8 text-xs'`
         - `md: 'h-10 w-10 text-sm'`
         - `lg: 'h-12 w-12 text-base'`
       - defaultVariants: `{ size: 'md' }`

    3. Update the `Avatar` forwardRef component:
       - Extend the props type to include `VariantProps<typeof avatarVariants>` (i.e. add optional `size?: 'sm' | 'md' | 'lg'`).
       - Destructure `size` from props, pass to `avatarVariants({ size })`, and merge with incoming `className` via `cn()`.
       - Replace the current hardcoded className `'relative flex h-10 w-10 shrink-0 overflow-hidden rounded-full'` with `cn(avatarVariants({ size }), className)`.

    4. Update the `AvatarFallback` forwardRef className. Replace the current:
         `'bg-muted flex h-full w-full items-center justify-center rounded-full'`
       with EXACTLY:
         `'flex h-full w-full items-center justify-center rounded-full bg-gradient-to-br from-green-500 to-emerald-600 text-white font-medium'`

    5. `AvatarImage` is UNCHANGED. `displayName` assignments UNCHANGED. Existing `'use client'` directive UNCHANGED. Radix import UNCHANGED.

    6. Export `avatarVariants` alongside `Avatar`, `AvatarImage`, `AvatarFallback` in the existing export statement.

    Do NOT introduce hardcoded slate/gray/black classes (the `text-white` on the gradient is the documented brand-literal exception). Do NOT add `dark:` overrides.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from-green-500 to-emerald-600" packages/ui/src/shadcn/avatar.tsx` → match
    - `grep -q "bg-gradient-to-br from-green-500 to-emerald-600 text-white font-medium" packages/ui/src/shadcn/avatar.tsx` → match
    - `grep -q "avatarVariants" packages/ui/src/shadcn/avatar.tsx` → match (defined AND exported)
    - `grep -q "h-8 w-8 text-xs" packages/ui/src/shadcn/avatar.tsx` → match (sm)
    - `grep -q "h-10 w-10 text-sm" packages/ui/src/shadcn/avatar.tsx` → match (md)
    - `grep -q "h-12 w-12 text-base" packages/ui/src/shadcn/avatar.tsx` → match (lg)
    - `grep -q "size: 'md'" packages/ui/src/shadcn/avatar.tsx` → match (defaultVariants)
    - `grep -q "class-variance-authority" packages/ui/src/shadcn/avatar.tsx` → match
    - `grep -q "bg-muted flex h-full w-full" packages/ui/src/shadcn/avatar.tsx` → no match (old fallback removed)
    - `grep -E "bg-slate-|text-slate-|border-slate-|bg-gray-|text-gray-|text-black" packages/ui/src/shadcn/avatar.tsx` → no matches
    - `grep -E "dark:(bg|text|border|ring)-" packages/ui/src/shadcn/avatar.tsx` → no matches
    - `pnpm typecheck` exits 0 (verifies the new VariantProps extension is type-safe)
    - `pnpm lint` exits 0
  </acceptance_criteria>
  <done>
    Avatar supports optional size prop (default md = existing behavior); AvatarFallback renders on green gradient with white initials; `avatarVariants` exported; typecheck+lint green; all existing callers byte-compatible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure visual restyle on a presentational primitive.

## STRIDE Threat Register

No new threat surface — className-only edit plus one additive optional prop on a visual primitive. No auth, data handling, input parsing, or network surface affected. The new `size` prop is a closed enum (`sm | md | lg`) enforced at the TypeScript boundary via `VariantProps`; cva strips unknown values; no injection surface. Existing threat model for the Avatar primitive (none) unchanged.
</threat_model>

<verification>
- `pnpm typecheck` green (critical — validates additive prop type-safety and byte-compatibility with existing callers)
- `pnpm lint` green
- All grep acceptance criteria pass
- Manual smoke (phase end): workspace header avatar renders md size; gradient + white initials visible in both themes
</verification>

<success_criteria>
- PRIM-05 fully satisfied per 08-UI-SPEC §7
- One additive optional prop added; zero breaking changes (default `md` = prior `h-10 w-10`)
- `avatarVariants` exported to match the buttonVariants/badgeVariants convention
- Single commit `feat(08-04): avatar Aloha restyle` after typecheck+lint pass
</success_criteria>

<output>
After completion, the executor creates `.planning/phases/08-shared-primitives/08-04-SUMMARY.md` capturing: file changed, new variant table, additive prop rationale, grep results, typecheck+lint status.
</output>
