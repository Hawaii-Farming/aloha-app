---
phase: 10-ag-grid-theme-template-parity-dark-mode
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/components/ag-grid/__tests__/ag-grid-theme.test.ts
  - app/lib/workspace/__tests__/get-org-initials.test.ts
  - e2e/tests/phase10-grid-sizing.spec.ts
  - e2e/tests/phase10-toolbar-search.spec.ts
  - e2e/tests/phase10-theme-toggle.spec.ts
  - e2e/tests/phase10-dark-surfaces.spec.ts
  - e2e/tests/phase10-sidebar-parity.spec.ts
  - e2e/tests/phase10-navbar-toggle.spec.ts
  - e2e/tests/phase10-avatar-initials.spec.ts
  - e2e/tests/phase10-scrollbar.spec.ts
  - e2e/tests/phase10-bug-01-active-pill.spec.ts
  - e2e/tests/phase10-bug-02-palette-nav.spec.ts
  - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md
autonomous: false
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - DARK-02
  - DARK-03
  - PARITY-01
  - PARITY-03
  - PARITY-04
  - PARITY-05
  - BUG-01
  - BUG-02

must_haves:
  truths:
    - "Unit test file for get-org-initials exists and fails (function not yet created)"
    - "ag-grid-theme.test.ts asserts the new Aloha hex values (test currently red)"
    - "Every phase-10 E2E spec file exists with `test.fail()` or `test.skip.fail()` skeletons tagged by requirement"
    - "Manual BUG-01 + BUG-02 repros captured with exact steps and observed symptoms"
  artifacts:
    - path: "app/lib/workspace/__tests__/get-org-initials.test.ts"
      provides: "Unit test skeleton for PARITY-03 helper"
    - path: "app/components/ag-grid/__tests__/ag-grid-theme.test.ts"
      provides: "Updated hex assertions for GRID-01"
    - path: "e2e/tests/phase10-grid-sizing.spec.ts"
      provides: "GRID-02 failing regression test"
    - path: "e2e/tests/phase10-bug-01-active-pill.spec.ts"
      provides: "BUG-01 failing regression test"
    - path: "e2e/tests/phase10-bug-02-palette-nav.spec.ts"
      provides: "BUG-02 failing regression test"
    - path: ".planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md"
      provides: "Captured manual reproduction for BUG-01 and BUG-02"
  key_links:
    - from: "e2e/tests/phase10-*.spec.ts"
      to: "@phase10 tag"
      via: "test.describe.configure({ tag: '@phase10' })"
      pattern: "@phase10"
---

<objective>
Wave 0 test scaffolding + bug reproduction. This plan creates the failing tests and captures the manual reproductions that Waves 1-3 will turn green. No production code changes.

Purpose: Guarantee every requirement in Phase 10 has an automated verify target before any implementation lands (Nyquist compliance).
Output: Red test suite + BUG-REPRO.md evidence file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-CONTEXT.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-VALIDATION.md
@app/components/ag-grid/ag-grid-theme.ts
@app/components/ag-grid/__tests__/ag-grid-theme.test.ts
@e2e/playwright.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual repro BUG-01 + BUG-02 and capture evidence</name>
  <read_first>
    - app/components/sidebar/module-sidebar-navigation.tsx (lines 55-62, 100-234)
    - app/components/navbar-search.tsx (lines 85-110)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md §"BUG-01 Root Cause Deep Dive" and §"BUG-02 Root Cause Deep Dive"
  </read_first>
  <action>
    Start the app via `pnpm dev`. In Chrome DevTools open the Elements tab pinned to the sidebar and Console pinned.

    BUG-01 repro:
    1. Sign in, land on workspace home.
    2. Click a MODULE-level entry (not a sub-module) in the sidebar (e.g., HR Employee).
    3. Observe whether the green gradient pill (`bg-gradient-to-r from-green-500 to-emerald-600`) appears on the module row IMMEDIATELY.
    4. Then click a sub-module. Observe whether the pill NOW appears.
    5. In DevTools, copy the resolved `className` string on the module row in both states.
    6. Repeat in expanded AND collapsed sidebar modes. Note differences.

    BUG-02 repro:
    1. Press Cmd+K to open navbar search.
    2. Type a module name (e.g., "Employee") so the command palette shows both the module AND its sub-modules.
    3. Arrow-key or mouse-click specifically on the MODULE entry (not a sub-module).
    4. Press Enter / click. Observe: does URL change? does dialog close? check console for errors.
    5. Temporarily patch `navbar-search.tsx` `handleSelect` with `console.log('handleSelect', path)` (do not commit). Repeat.
    6. Record whether `handleSelect` fires, with what argument, and whether `navigate` commits.

    Write findings to `.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md` with sections: `## BUG-01 Observed`, `## BUG-01 Root Cause (confirmed)`, `## BUG-02 Observed`, `## BUG-02 Root Cause (confirmed)`. Attach screenshots if possible. Revert any temporary `console.log` patches before finishing.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md && grep -q "BUG-01 Observed" .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md && grep -q "BUG-02 Observed" .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-BUG-REPRO.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-BUG-REPRO.md exists with both BUG-01 and BUG-02 sections
    - Each bug has Observed + Root Cause (confirmed) subsections
    - No stray `console.log` patches left in `navbar-search.tsx`
    - `pnpm typecheck` green
  </acceptance_criteria>
  <done>Manual reproductions complete, root causes confirmed (or hypothesis updated), evidence file committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit test skeletons (get-org-initials + ag-grid-theme hex update)</name>
  <read_first>
    - app/components/ag-grid/ag-grid-theme.ts (current hexes)
    - app/components/ag-grid/__tests__/ag-grid-theme.test.ts (existing shape)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md §"AG Grid token-pair contract"
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md §"Pattern 5: Org Initials Helper"
  </read_first>
  <behavior>
    get-org-initials.test.ts:
    - `getOrgInitials('Hawaii Farming')` → `'HF'`
    - `getOrgInitials('Aloha')` → `'A'`
    - `getOrgInitials('hawaii  farming  corp')` → `'HF'` (two words max, collapses whitespace)
    - `getOrgInitials('', 'jean@example.com')` → `'J'`
    - `getOrgInitials(null, null)` → `'A'`
    - `getOrgInitials(undefined)` → `'A'`
    - `getOrgInitials('<script>alert(1)</script>')` → `'<'` or `'<S'` (function returns plain chars; React escapes on render) — assert no throw, length ≤ 2

    ag-grid-theme.test.ts (update existing assertions):
    - Light params must include exact hexes: `backgroundColor: '#ffffff'`, `foregroundColor: '#0f172a'`, `headerBackgroundColor: '#f1f5f9'`, `headerTextColor: '#475569'`, `borderColor: '#e2e8f0'`, `accentColor: '#22c55e'`, `rowHoverColor: '#f1f5f9'`, `selectedRowBackgroundColor: '#f0fdf4'`, `oddRowBackgroundColor: '#f8fafc'`.
    - Dark params must include: `backgroundColor: '#1e293b'`, `foregroundColor: '#f8fafc'`, `headerBackgroundColor: '#0f172a'`, `headerTextColor: '#cbd5e1'`, `borderColor: '#334155'`, `accentColor: '#4ade80'`, `rowHoverColor: '#334155'`, `oddRowBackgroundColor: '#1e293b'`.
    - `fontFamily` must contain `'Inter Variable'` (NOT `'Geist Variable'`).
    - Shared: `fontSize: 14`, `headerFontSize: 13`, `headerFontWeight: 500`, `rowVerticalPaddingScale: 1.6`.
  </behavior>
  <action>
    Create `app/lib/workspace/__tests__/get-org-initials.test.ts` as a Vitest unit test importing from `../get-org-initials` (file does not yet exist — import path is authoritative). Use `describe/it/expect`. The test file MUST fail with "module not found" at this stage — that's the Wave 0 red state.

    Update `app/components/ag-grid/__tests__/ag-grid-theme.test.ts` assertions to the Aloha hex set listed in `<behavior>`. Preserve the existing test structure; only change expected values. The test MUST fail after this change (current file uses Supabase hexes + `Geist Variable`).

    Do NOT modify `ag-grid-theme.ts` in this task — that's Plan 02's job.
  </action>
  <verify>
    <automated>pnpm vitest run app/lib/workspace/__tests__/get-org-initials.test.ts app/components/ag-grid/__tests__/ag-grid-theme.test.ts 2>&1 | grep -E "(FAIL|failed)" || (echo "Tests should be red in Wave 0" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/workspace/__tests__/get-org-initials.test.ts` exists with ≥ 6 `it(...)` cases matching `<behavior>`
    - `app/components/ag-grid/__tests__/ag-grid-theme.test.ts` asserts all hexes from `<behavior>` AND `'Inter Variable'`
    - Both test files FAIL when run (Wave 0 red state is expected)
    - `pnpm typecheck` green (test files are typesafe even when values fail)
  </acceptance_criteria>
  <done>Two unit test files exist in a failing state; execution in Plans 02 and 04 will turn them green.</done>
</task>

<task type="auto">
  <name>Task 3: Create all @phase10 Playwright E2E spec skeletons</name>
  <read_first>
    - e2e/playwright.config.ts
    - e2e/tests (any existing spec for reference on page-object usage)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md §"Validation Architecture"
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md (every Surface section for assertion contracts)
  </read_first>
  <action>
    Create ten Playwright spec files under `e2e/tests/` listed in `files_modified`. Each file:
    1. Uses `test.describe('@phase10 ...', () => { ... })` OR adds `test.info().annotations.push({ type: 'tag', description: '@phase10' })` so `--grep @phase10` selects them.
    2. Adds grep tags per `10-RESEARCH.md` table: `@grid-sizing`, `@toolbar-search`, `@theme-toggle`, `@dark-surfaces`, `@sidebar-parity`, `@navbar-toggle`, `@avatar-initials`, `@scrollbar`, `@bug-01-active-pill`, `@bug-02-palette-nav`.
    3. Contains one or more `test('...', async ({ page }) => { test.fail(); /* assertion */ })` cases describing the intended assertion. The `test.fail()` marker expects a current failure so Wave 0 is green-on-red.

    Spec contents (high-level assertions each file must contain; use `.toBe`/`.toHaveCSS` as appropriate):

    **phase10-grid-sizing.spec.ts** (GRID-02): Navigate to `/home/<account>/hr/hr_employee_register` (Register). Assert `[data-test="ag-grid-wrapper"]` (or first `.ag-root-wrapper`) `boundingBox().height > 300`. Repeat for Scheduler, Time Off, Payroll, Hours Comparison, Housing, Employee Review (loop array of 8 module paths).

    **phase10-toolbar-search.spec.ts** (GRID-03): Navigate to Register list view. Locate toolbar search input. Assert its computed `border-radius` equals `6px` (rounded-md) — NOT `16px` (rounded-2xl).

    **phase10-theme-toggle.spec.ts** (DARK-02): Snapshot `document.body.getBoundingClientRect()` and `navbar` + `aside` + `main` bounds. Toggle theme via profile menu or `localStorage.theme = 'dark'` + reload. Re-snapshot. Assert all bounds equal within 1px tolerance. Assert `page.on('console', ...)` caught no errors.

    **phase10-dark-surfaces.spec.ts** (DARK-03): Set dark mode. Assert `getComputedStyle(navbar).backgroundColor === 'rgb(30, 41, 59)'` (slate-800) AND differs from `getComputedStyle(body).backgroundColor` (slate-900 `rgb(15, 23, 42)`). Assert sidebar background also equals slate-800 and differs from body.

    **phase10-sidebar-parity.spec.ts** (PARITY-01, PARITY-04): Assert sidebar contains an element matching text `/^NAVIGATION$/i` AND `/^MODULES$/i`. Assert a `[role="separator"]` or `hr` is present between sections. Assert the "Focused" footer button exists with `aria-disabled="true"`. Assert at least one active sub-module has a parent with `border-l-2`. Assert sub-module row first-child has `margin-top >= 4px` computed.

    **phase10-navbar-toggle.spec.ts** (PARITY-02): Assert `nav > :first-child` (or the navbar container's first interactive child) has `data-test="workspace-navbar-sidebar-toggle"` OR contains a `PanelLeft` icon, verified by `locator('nav').locator(':scope > *').first()` matching the toggle.

    **phase10-avatar-initials.spec.ts** (PARITY-03): Assume signed-in demo org is "Hawaii Farming". Assert navbar avatar fallback text content equals `"HF"`. Assert sidebar-footer profile menu avatar also equals `"HF"`.

    **phase10-scrollbar.spec.ts** (PARITY-05): Evaluate `getComputedStyle(document.documentElement).scrollbarColor` contains `rgb(226, 232, 240)` (slate-200, light) OR `rgb(51, 65, 85)` (slate-700, dark). Assert it is NOT empty string or `'auto'`.

    **phase10-bug-01-active-pill.spec.ts** (BUG-01): Navigate to `/home/<account>/`. Click a module row (e.g., HR Employee). Immediately (before any sub-module click) assert the module row's computed background-image includes `linear-gradient` AND contains green hex. Repeat for initial load: go directly to `/home/<account>/hr/hr_employee_register` — assert the HR module row has the gradient without clicking anything.

    **phase10-bug-02-palette-nav.spec.ts** (BUG-02): Open command palette (`Meta+K`). Type `Employee`. Press Arrow/click the module-level entry (not a sub-module). Press Enter. Assert URL matches `/home/<account>/hr/hr_employee` (or the module's path). Assert dialog is closed.

    **phase10-theme-toggle.spec.ts** AND all others: use `test.fail()` at top of each test case so Wave 0 reports "expected failure = PASS". Waves 1-4 remove `test.fail()` as they fix each requirement.

    Use the demo account slug placeholder `${process.env.TEST_ACCOUNT_SLUG ?? 'aloha'}` — existing e2e tests show the convention; match it.
  </action>
  <verify>
    <automated>pnpm --filter e2e exec playwright test --grep @phase10 --list 2>&1 | grep -cE "phase10-" | awk '{ if ($1 < 10) exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - All 10 E2E spec files exist under `e2e/tests/`
    - `pnpm --filter e2e exec playwright test --grep @phase10 --list` lists ≥ 10 tests
    - Every spec uses `test.fail()` (or equivalent expected-failure marker) until corresponding fix lands
    - `pnpm typecheck` green across the e2e package
    - No spec imports production code that doesn't yet exist (use selectors + page interactions only)
  </acceptance_criteria>
  <done>Ten failing @phase10 Playwright specs committed, each tagged by requirement, ready for Waves 1-4 to unfail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → prod code | Test skeletons intentionally fail; no prod behavior changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Left-over `console.log` in `navbar-search.tsx` after Task 1 repro | mitigate | Acceptance criterion explicitly requires revert; git diff reviewed before commit |
| T-10-02 | DoS (self-inflicted) | Wave 0 tests stay red forever if Waves 1-4 skipped | mitigate | Phase completion gate requires 0 failing @phase10 tests |
</threat_model>

<verification>
Wave 0 is complete when:
- `pnpm vitest run app/lib/workspace/__tests__/get-org-initials.test.ts` fails with "module not found"
- `pnpm vitest run app/components/ag-grid/__tests__/ag-grid-theme.test.ts` fails with hex mismatches
- `pnpm --filter e2e exec playwright test --grep @phase10` reports every test as "expected failure" (green via `test.fail()`)
- `10-BUG-REPRO.md` contains confirmed root causes for BUG-01 and BUG-02
</verification>

<success_criteria>
- 12 test artifacts created (2 unit + 10 e2e) or updated
- Every Phase 10 requirement ID has at least one failing test pointing at it
- BUG-REPRO evidence committed
- No production code modified in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-01-SUMMARY.md`
</output>
