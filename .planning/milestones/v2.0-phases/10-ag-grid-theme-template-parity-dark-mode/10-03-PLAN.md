---
phase: 10-ag-grid-theme-template-parity-dark-mode
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - app/components/sidebar/module-sidebar-navigation.tsx
  - app/components/sidebar/workspace-sidebar.tsx
  - app/styles/shadcn-ui.css
  - app/styles/global.css
  - public/locales/en/common.json
autonomous: true
requirements:
  - PARITY-01
  - PARITY-04
  - PARITY-05
  - DARK-02
  - DARK-03

must_haves:
  truths:
    - "Sidebar displays a NAVIGATION section header and a MODULES section header in uppercase"
    - "A SidebarSeparator renders between sections"
    - "Sub-module rows have visible vertical margin (mt-1 mb-1) and a gap-1 within the accordion rail"
    - "The sidebar footer renders a disabled 'Focused' placeholder with LayoutGrid + ChevronLeft icons"
    - "In dark mode, the sidebar and navbar render on #1e293b, distinct from the page background #0f172a"
    - "Scrollbars are 6px thin with themed thumb in both light and dark modes"
  artifacts:
    - path: "app/styles/shadcn-ui.css"
      provides: "Dark-mode --sidebar-background fix"
      contains: "--sidebar-background: #1e293b"
    - path: "app/styles/global.css"
      provides: "Global themed scrollbar rules"
      contains: "::-webkit-scrollbar"
    - path: "app/components/sidebar/module-sidebar-navigation.tsx"
      provides: "NAVIGATION/MODULES headers, separator, accordion spacing, Focused footer placeholder"
    - path: "public/locales/en/common.json"
      provides: "i18n keys for shell.sidebar.nav_section/modules_section/focused_footer and shell.navbar.search_placeholder"
  key_links:
    - from: ".dark --sidebar-background"
      to: "bg-sidebar consumers (Shadcn sidebar primitive)"
      via: "CSS custom property"
      pattern: "var\\(--sidebar-background\\)"
    - from: "global.css ::-webkit-scrollbar"
      to: "every scrollable region"
      via: "global selector"
      pattern: "::-webkit-scrollbar"
---

<objective>
Wave 2: Structural sidebar parity with the prototype + one-line dark-mode surface fix + global themed scrollbars. Closes PARITY-01, PARITY-04, PARITY-05, DARK-02, DARK-03.

Purpose: Make the sidebar look like the prototype, make dark-mode chrome visible against the page, and theme scrollbars across the app.
Output: Restructured sidebar component, dark token fix, scrollbar CSS, i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md
@.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-CONTEXT.md
@DESIGN.md
@app/components/sidebar/module-sidebar-navigation.tsx
@app/components/sidebar/workspace-sidebar.tsx
@app/styles/shadcn-ui.css
@packages/ui/src/shadcn/sidebar.tsx
</context>

<interfaces>
From `@aloha/ui/shadcn/sidebar` (read-only — DO NOT modify):
```tsx
export { Sidebar, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupContent,
         SidebarGroupLabel, SidebarHeader, SidebarMenu, SidebarMenuButton, SidebarMenuItem,
         SidebarSeparator };
```

From `../aloha-design/prototype/src/components/layout/Sidebar.tsx` (canonical structure reference):
- Top row: "Navigation" label (prototype is title-case; UI-SPEC locks ALL CAPS for Aloha)
- Flex-col module list with chevron-dropdown buttons
- Footer: `LayoutGrid` + "Focused" label + `ChevronLeft`, border-t slate-100
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dark-mode --sidebar-background fix + global scrollbar CSS</name>
  <read_first>
    - app/styles/shadcn-ui.css (.dark block around line 134)
    - app/styles/global.css (entry point)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md §"Surface 4" and §"Surface 5"
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-RESEARCH.md §"Pattern 6: Dark-Mode Sidebar Background Fix" and §"Example 6"
  </read_first>
  <action>
    **Edit 1 — `app/styles/shadcn-ui.css` `.dark` block:**
    Locate the existing `--sidebar-background` and `--sidebar-border` declarations inside the `.dark` selector. Change:
    ```css
    --sidebar-background: #0f172a;   /* was colliding with --background */
    --sidebar-border: #1e293b;
    ```
    to:
    ```css
    --sidebar-background: #1e293b;   /* slate-800 — matches --card, elevated above --background */
    --sidebar-border: #334155;       /* slate-700 — matches --border, visible edge */
    ```
    Do NOT touch the `:root` (light) block. Do NOT introduce a `--color-chrome` token (CONTEXT D-17/D-17b supersession — UI-SPEC locks this one-line fix).

    **Edit 2 — `app/styles/global.css` (append to end):**
    ```css
    /* Phase 10 PARITY-05 — themed scrollbars */
    html {
      scrollbar-width: thin;
      scrollbar-color: var(--border) transparent;
    }
    *::-webkit-scrollbar {
      width: 6px;
      height: 6px;
    }
    *::-webkit-scrollbar-track {
      background: transparent;
    }
    *::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 3px;
    }
    *::-webkit-scrollbar-thumb:hover {
      background: var(--muted-foreground);
    }
    ```
    If `app/styles/global.css` does not exist, locate the CSS entry point actually imported in `app/root.tsx` (likely `app/styles/app.css` or `app/styles/shadcn-ui.css`) and append there instead. Update `files_modified` mental model but don't rename — just pick the correct file and document the choice in the commit message.

    **Edit 3 — centered navbar search trigger dark fallback (DARK-02):**
    In `app/components/workspace-shell/workspace-navbar.tsx` (or wherever the centered search trigger is defined), ensure the search trigger wrapper includes `dark:bg-slate-700` alongside its existing `bg-muted` to guarantee contrast against the post-fix slate-800 navbar surface. If this class is already present, no-op. This is the one literal dark override sanctioned by UI-SPEC Surface 3.
  </action>
  <verify>
    <automated>grep -n "sidebar-background: #1e293b" app/styles/shadcn-ui.css &amp;&amp; grep -n "::-webkit-scrollbar" app/styles/*.css &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `.dark` block has `--sidebar-background: #1e293b` and `--sidebar-border: #334155`
    - Scrollbar rules present in exactly one global CSS file
    - `pnpm --filter e2e exec playwright test --grep "@dark-surfaces|@scrollbar|@theme-toggle"` all pass (remove `test.fail()` markers)
    - Manual smoke: toggle dark mode, sidebar visually sits on slate-800 above slate-900 page; scrollbars are 6px and themed
    - `pnpm typecheck && pnpm lint` clean
  </acceptance_criteria>
  <done>Dark mode surfaces correct; global themed scrollbars live.</done>
</task>

<task type="auto">
  <name>Task 2: Port prototype sidebar structure onto module-sidebar-navigation.tsx</name>
  <read_first>
    - app/components/sidebar/module-sidebar-navigation.tsx (ENTIRE file — this is the primary edit target)
    - app/components/sidebar/workspace-sidebar.tsx (host that wraps it; check if NAVIGATION header + separator fit here or in module-sidebar-navigation)
    - ../aloha-design/prototype/src/components/layout/Sidebar.tsx (canonical structure lines 111-167)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md §"Surface 2 — Sidebar (PARITY-01, PARITY-04)"
    - packages/ui/src/shadcn/sidebar.tsx (read-only — confirm SidebarSeparator, SidebarFooter, SidebarGroupLabel slots exist)
  </read_first>
  <action>
    Port the following structural elements into `app/components/sidebar/module-sidebar-navigation.tsx` (or `workspace-sidebar.tsx` if it owns the group wrapper — decide after reading both). The expanded branch is the target for PARITY-01/04. BUG-01 consolidation lives in Plan 04 — do NOT touch the active-pill logic in this task.

    **Structure top → bottom:**

    1. **NAVIGATION section header** — a `SidebarGroupLabel` with classes `text-xs font-semibold uppercase tracking-wider text-muted-foreground px-3 pb-2 pt-3` and text `NAVIGATION`. Use i18n key `shell.sidebar.nav_section` (added in Task 3) via `useTranslation()`. If there is no separate "Navigation" sub-list (single-list app), this label sits directly above the module list.

    2. **MODULES section header** — second `SidebarGroupLabel` with the same style and text `MODULES`. If the current nav config only has one flat module list, render a single `NAVIGATION` label and reserve `MODULES` for future by rendering it only when `sortedModules.length > 0 && showSecondaryGroup` (default false — adds no visual noise). **OR** render both labels around the same list when the project convention accepts duplicate labelling. UI-SPEC explicitly says "interpret as a single `NAVIGATION` header above the module list, with MODULES header reserved for future" — follow that. So: render ONE `NAVIGATION` label as the active section header. MODULES header is documented in the commit message as reserved-for-future, NOT rendered in this task.

    3. **`SidebarSeparator`** — a single 1px `border-border` horizontal line above the module list OR between the header and the list, consuming `<SidebarSeparator />` from `@aloha/ui/shadcn/sidebar`.

    4. **Module rows (expanded branch)** — existing `SidebarGroupLabel` OR `SidebarMenuButton` row. **Do not modify the active-pill logic here** (Plan 04 owns BUG-01 consolidation). Only add the chevron visual polish:
       - Existing chevron already present per RESEARCH — verify it uses `transition-transform duration-200` and rotates `rotate-180` when `isOpen` is true.
       - Chevron color: inactive = `text-muted-foreground`; when module is active = inherit the pill text color.

    5. **Accordion sub-module container (expanded + open)** — wrapper classes:
       ```
       ml-5 border-l-2 border-green-200 dark:border-green-900/60 pl-3 flex flex-col gap-1 mt-1 mb-1
       ```
       This adds the missing vertical separation (PARITY-04): `mt-1 mb-1` + `gap-1`. The existing `ml-5 border-l-2 border-green-200 pl-3` stays. Add `dark:border-green-900/60` for dark-mode visibility.

    6. **Active sub-item chip — add dark-mode override:** existing classes `bg-green-50 text-green-700 font-medium rounded-lg`. Append `dark:bg-green-900/40 dark:text-green-200`. This closes the Phase 9 D-11 deferral.

    7. **`SidebarSeparator`** above the footer (only if the sidebar does not already have one at the bottom of the content area).

    8. **"Focused" footer placeholder** — add a `<SidebarFooter>` (or append inside the existing footer if one exists) containing:
       ```tsx
       <button
         type="button"
         aria-disabled="true"
         tabIndex={-1}
         className="flex w-full items-center gap-2 px-3 py-2 text-xs font-medium text-muted-foreground opacity-60 cursor-not-allowed"
       >
         <LayoutGrid className="h-4 w-4" />
         <span className="flex-1 text-left">{t('shell.sidebar.focused_footer')}</span>
         <ChevronLeft className="h-4 w-4" />
       </button>
       ```
       Import `LayoutGrid` and `ChevronLeft` from `lucide-react`. The button MUST have no click handler and MUST be `aria-disabled="true"`. This satisfies PARITY-01's "Focused footer" requirement as a visual placeholder (CONTEXT D-14 default).

    9. **Inline collapse affordance — explicitly OMITTED** (CONTEXT D-10). Add a code comment at the top of the file: `// Phase 10 D-10: inline collapse affordance is OMITTED — navbar PanelLeft toggle is the single source of truth for sidebar collapse.`

    **Constraints:**
    - Do NOT modify `packages/ui/src/shadcn/sidebar.tsx` (primitive is read-only).
    - Do NOT introduce `useEffect` — if you need derived state, use `useMemo`.
    - Do NOT alter the active-pill recipe (locked Phase 9 D-09) — gradient is still `bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25`.
    - Collapsed (icon-only) mode stays unchanged structurally; section headers and Focused footer hide when `state === 'collapsed'`.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; grep -n "NAVIGATION\|nav_section" app/components/sidebar/module-sidebar-navigation.tsx &amp;&amp; grep -n "LayoutGrid\|ChevronLeft" app/components/sidebar/module-sidebar-navigation.tsx &amp;&amp; grep -n "mt-1 mb-1" app/components/sidebar/module-sidebar-navigation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `NAVIGATION` label (via i18n) rendered above the module list
    - `SidebarSeparator` present between header and module list
    - Accordion container uses `ml-5 border-l-2 border-green-200 dark:border-green-900/60 pl-3 flex flex-col gap-1 mt-1 mb-1`
    - Active sub-item chip has `dark:bg-green-900/40 dark:text-green-200`
    - "Focused" footer placeholder exists with `aria-disabled="true"` and `LayoutGrid` + `ChevronLeft` icons
    - No `useEffect` added anywhere in the file
    - No edits to `packages/ui/src/shadcn/sidebar.tsx`
    - `pnpm --filter e2e exec playwright test --grep @sidebar-parity` passes (remove `test.fail()`)
    - `pnpm typecheck && pnpm lint` clean
  </acceptance_criteria>
  <done>Sidebar structurally matches the prototype on the existing Shadcn primitive.</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n keys for sidebar/navbar shell copy</name>
  <read_first>
    - public/locales/en/common.json (existing structure)
    - .planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-UI-SPEC.md §"Copywriting Contract" and §"i18n note"
  </read_first>
  <action>
    Add to `public/locales/en/common.json` under a new or existing `shell` namespace:
    ```json
    "shell": {
      "sidebar": {
        "nav_section": "NAVIGATION",
        "modules_section": "MODULES",
        "focused_footer": "Focused"
      },
      "navbar": {
        "search_placeholder": "Search for a module, sub-module, or action..."
      }
    }
    ```

    If a `shell` key already exists, merge under it. Do NOT remove or rename existing keys. Do NOT add other languages in this task (keep the change minimal — English only).
  </action>
  <verify>
    <automated>node -e "const j = require('./public/locales/en/common.json'); if (!j.shell?.sidebar?.nav_section) { console.error('missing nav_section'); process.exit(1); } if (!j.shell?.sidebar?.focused_footer) { console.error('missing focused_footer'); process.exit(1); } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `common.json` parses as valid JSON
    - `shell.sidebar.nav_section === "NAVIGATION"`
    - `shell.sidebar.modules_section === "MODULES"`
    - `shell.sidebar.focused_footer === "Focused"`
    - `shell.navbar.search_placeholder` present
    - `pnpm typecheck` green
  </acceptance_criteria>
  <done>i18n keys added; Task 2's `useTranslation()` call resolves correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn-ui.css .dark block | affects every dark-mode surface; wrong value collides with --background again |
| global.css ::-webkit-scrollbar | affects every scrollable region in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | DoS (visual) | Wrong hex re-colliding sidebar with page background in dark | mitigate | Acceptance: E2E @dark-surfaces asserts different `background-color` values |
| T-10-06 | Information Disclosure | i18n key namespace collision removing existing copy | mitigate | Acceptance: JSON parse + preserve existing keys via merge |
</threat_model>

<verification>
- `pnpm vitest run` all unit tests green
- `pnpm --filter e2e exec playwright test --grep "@sidebar-parity|@dark-surfaces|@scrollbar|@theme-toggle"` passes
- `pnpm typecheck && pnpm lint` clean
</verification>

<success_criteria>
- PARITY-01, PARITY-04, PARITY-05, DARK-02, DARK-03 all green via automated tests
- Sidebar visually matches prototype (section header, separator, spacing, footer)
- Dark mode shows elevated nav/sidebar on slate-800 above slate-900 page
- Themed 6px scrollbars in both modes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ag-grid-theme-template-parity-dark-mode/10-03-SUMMARY.md`
</output>
