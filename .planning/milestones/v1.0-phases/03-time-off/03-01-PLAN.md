---
phase: 03-time-off
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql
  - supabase/migrations/20260408000003_hr_time_off_request_rls.sql
autonomous: true
requirements: [TOFF-01]

must_haves:
  truths:
    - "SQL view app_hr_time_off_requests returns employee-joined columns for time off grid"
    - "RLS policies on hr_time_off_request restrict access to org members only"
    - "View includes NULL::DATE AS end_date for loadTableData compatibility"
  artifacts:
    - path: "supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql"
      provides: "SQL view joining hr_time_off_request with hr_employee, department, work auth, comp manager, requested_by, reviewed_by"
      contains: "CREATE OR REPLACE VIEW app_hr_time_off_requests"
    - path: "supabase/migrations/20260408000003_hr_time_off_request_rls.sql"
      provides: "RLS policies and GRANT for hr_time_off_request table"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "app_hr_time_off_requests view"
      to: "hr_time_off_request table"
      via: "JOIN hr_employee on hr_employee_id, requested_by, reviewed_by"
      pattern: "JOIN hr_employee"
---

<objective>
Create the SQL view and RLS policies for the Time Off submodule.

Purpose: The time off grid needs employee-joined data (name, photo, department, work auth status, comp manager, requested_by name, reviewed_by name) that the raw hr_time_off_request table doesn't provide. RLS policies are currently missing, creating a security gap.
Output: Two SQL migration files -- one for the view, one for RLS + GRANT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-time-off/03-CONTEXT.md
@.planning/phases/03-time-off/03-RESEARCH.md

@supabase/migrations/20260401000022_hr_time_off_request.sql
@supabase/migrations/20260408000001_update_ops_task_weekly_schedule_view.sql
@supabase/CLAUDE.md

<interfaces>
<!-- Schema columns from hr_time_off_request migration -->
From supabase/migrations/20260401000022_hr_time_off_request.sql:
- id UUID PK
- org_id TEXT NOT NULL
- hr_employee_id TEXT NOT NULL (FK hr_employee)
- start_date DATE NOT NULL
- return_date DATE
- non_pto_days NUMERIC
- pto_days NUMERIC
- sick_leave_days NUMERIC
- request_reason TEXT
- denial_reason TEXT
- notes TEXT
- status TEXT NOT NULL DEFAULT 'pending' CHECK (pending/approved/denied)
- requested_at TIMESTAMPTZ NOT NULL DEFAULT now()
- requested_by TEXT NOT NULL (FK hr_employee)
- reviewed_at TIMESTAMPTZ
- reviewed_by TEXT (FK hr_employee, nullable)
- is_deleted BOOLEAN NOT NULL DEFAULT false
- created_at, updated_at TIMESTAMPTZ

<!-- RLS pattern from supabase/CLAUDE.md -->
Standard org-scoped RLS:
```sql
CREATE POLICY "tablename_read" ON public.tablename
  FOR SELECT TO authenticated
  USING (EXISTS (
    SELECT 1 FROM public.hr_employee e
    WHERE e.org_id = tablename.org_id
      AND e.user_id = auth.uid()
      AND e.is_deleted = false
  ));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app_hr_time_off_requests SQL view migration</name>
  <files>supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql</files>
  <read_first>
    - supabase/migrations/20260401000022_hr_time_off_request.sql
    - supabase/migrations/20260408000001_update_ops_task_weekly_schedule_view.sql
    - supabase/migrations/20260401000020_hr_employee.sql
    - supabase/CLAUDE.md
  </read_first>
  <action>
Create SQL migration file `supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql` with a `CREATE OR REPLACE VIEW app_hr_time_off_requests` that:

1. Selects from `hr_time_off_request r` all domain columns: `r.id`, `r.org_id`, `r.hr_employee_id`, `r.start_date`, `r.return_date`, `r.pto_days`, `r.non_pto_days`, `r.sick_leave_days`, `r.request_reason`, `r.denial_reason`, `r.notes`, `r.status`, `r.requested_at`, `r.requested_by`, `r.reviewed_at`, `r.reviewed_by`, `r.is_deleted`, `r.created_at`, `r.updated_at`

2. JOINs `hr_employee e ON e.id = r.hr_employee_id` to get:
   - `e.first_name || ' ' || e.last_name AS full_name`
   - `e.preferred_name`
   - `e.profile_photo_url`

3. LEFT JOINs `hr_department d ON d.id = e.hr_department_id` for `d.name AS department_name`

4. LEFT JOINs `hr_work_authorization wa ON wa.id = e.hr_work_authorization_id` for `wa.name AS work_authorization_name`

5. LEFT JOINs `hr_employee cm ON cm.id = e.compensation_manager_id` for `cm.first_name || ' ' || cm.last_name AS compensation_manager_name`

6. JOINs `hr_employee req ON req.id = r.requested_by` for `req.first_name || ' ' || req.last_name AS requested_by_name`

7. LEFT JOINs `hr_employee rev ON rev.id = r.reviewed_by` for `rev.first_name || ' ' || rev.last_name AS reviewed_by_name`

8. Adds `NULL::DATE AS end_date` as a computed column for `loadTableData` compatibility (per D-01 through D-03, research pitfall 1)

9. GRANT SELECT on the view to `authenticated` role

Use LEFT JOIN for nullable FKs (department, work_authorization, compensation_manager, reviewed_by). Use INNER JOIN for non-null FKs (hr_employee_id, requested_by).
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && grep -c "CREATE OR REPLACE VIEW app_hr_time_off_requests" supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql && grep -c "NULL::DATE AS end_date" supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql && grep -c "LEFT JOIN" supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260408000002_app_hr_time_off_requests_view.sql
    - Contains `CREATE OR REPLACE VIEW app_hr_time_off_requests AS`
    - Contains `JOIN hr_employee e ON e.id = r.hr_employee_id` (INNER JOIN for non-null FK)
    - Contains `LEFT JOIN hr_department d ON d.id = e.hr_department_id`
    - Contains `LEFT JOIN hr_work_authorization wa ON wa.id = e.hr_work_authorization_id`
    - Contains `LEFT JOIN hr_employee cm ON cm.id = e.compensation_manager_id`
    - Contains `JOIN hr_employee req ON req.id = r.requested_by` (INNER JOIN for non-null FK)
    - Contains `LEFT JOIN hr_employee rev ON rev.id = r.reviewed_by`
    - Contains `NULL::DATE AS end_date`
    - Contains `e.first_name || ' ' || e.last_name AS full_name`
    - Contains `req.first_name || ' ' || req.last_name AS requested_by_name`
    - Contains `rev.first_name || ' ' || rev.last_name AS reviewed_by_name`
    - Contains `GRANT SELECT ON app_hr_time_off_requests TO authenticated`
  </acceptance_criteria>
  <done>SQL view migration creates app_hr_time_off_requests with all employee-joined columns, LEFT JOINs for nullable FKs, end_date compatibility column, and GRANT to authenticated</done>
</task>

<task type="auto">
  <name>Task 2: Create RLS policies for hr_time_off_request table</name>
  <files>supabase/migrations/20260408000003_hr_time_off_request_rls.sql</files>
  <read_first>
    - supabase/migrations/20260401000022_hr_time_off_request.sql
    - supabase/CLAUDE.md
  </read_first>
  <action>
Create SQL migration file `supabase/migrations/20260408000003_hr_time_off_request_rls.sql` that adds RLS to the hr_time_off_request table following the standard pattern from supabase/CLAUDE.md:

1. `ALTER TABLE hr_time_off_request ENABLE ROW LEVEL SECURITY;`

2. Read policy `hr_time_off_request_read`:
```sql
CREATE POLICY "hr_time_off_request_read" ON public.hr_time_off_request
  FOR SELECT TO authenticated
  USING (EXISTS (
    SELECT 1 FROM public.hr_employee e
    WHERE e.org_id = hr_time_off_request.org_id
      AND e.user_id = auth.uid()
      AND e.is_deleted = false
  ));
```

3. Insert policy `hr_time_off_request_write`:
```sql
CREATE POLICY "hr_time_off_request_write" ON public.hr_time_off_request
  FOR INSERT TO authenticated
  WITH CHECK (EXISTS (
    SELECT 1 FROM public.hr_employee e
    WHERE e.org_id = hr_time_off_request.org_id
      AND e.user_id = auth.uid()
      AND e.is_deleted = false
  ));
```

4. Update policy `hr_time_off_request_update`:
```sql
CREATE POLICY "hr_time_off_request_update" ON public.hr_time_off_request
  FOR UPDATE TO authenticated
  USING (EXISTS (
    SELECT 1 FROM public.hr_employee e
    WHERE e.org_id = hr_time_off_request.org_id
      AND e.user_id = auth.uid()
      AND e.is_deleted = false
  ));
```

5. `GRANT SELECT, INSERT, UPDATE ON public.hr_time_off_request TO authenticated;`

No DELETE grant -- use soft delete (is_deleted = true) per convention.
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && grep -c "CREATE POLICY" supabase/migrations/20260408000003_hr_time_off_request_rls.sql && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/20260408000003_hr_time_off_request_rls.sql && grep -c "GRANT" supabase/migrations/20260408000003_hr_time_off_request_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260408000003_hr_time_off_request_rls.sql
    - Contains `ALTER TABLE hr_time_off_request ENABLE ROW LEVEL SECURITY`
    - Contains `CREATE POLICY "hr_time_off_request_read"` with FOR SELECT TO authenticated
    - Contains `CREATE POLICY "hr_time_off_request_write"` with FOR INSERT TO authenticated
    - Contains `CREATE POLICY "hr_time_off_request_update"` with FOR UPDATE TO authenticated
    - All three policies use the `EXISTS (SELECT 1 FROM public.hr_employee e WHERE e.org_id = hr_time_off_request.org_id AND e.user_id = auth.uid() AND e.is_deleted = false)` pattern
    - Contains `GRANT SELECT, INSERT, UPDATE ON public.hr_time_off_request TO authenticated`
    - Does NOT contain DELETE grant
  </acceptance_criteria>
  <done>RLS policies and GRANT applied to hr_time_off_request table following standard org-scoped pattern from supabase/CLAUDE.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase API | Authenticated users query views via PostgREST |
| cross-org data | RLS must prevent org A users from seeing org B time off requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | hr_time_off_request table | mitigate | Add org-scoped RLS read policy (Task 2) -- currently NO RLS exists |
| T-03-02 | Tampering | hr_time_off_request table | mitigate | Add org-scoped RLS write/update policies (Task 2) |
| T-03-03 | Elevation of Privilege | hr_time_off_request table | accept | Module-level access control via hr_module_access enforced in app layer; RLS provides org-level isolation |
</threat_model>

<verification>
- `pnpm supabase:reset` completes without errors (migrations apply cleanly)
- `pnpm supabase:typegen` regenerates types including the new view
- View returns rows when queried: `SELECT * FROM app_hr_time_off_requests LIMIT 1;`
</verification>

<success_criteria>
- Both migration files exist and are syntactically valid SQL
- `pnpm supabase:reset` applies all migrations including the two new ones without error
- The view joins all required employee fields (full_name, profile_photo_url, department_name, work_authorization_name, compensation_manager_name, requested_by_name, reviewed_by_name)
- RLS policies prevent cross-org access
</success_criteria>

<output>
After completion, create `.planning/phases/03-time-off/03-01-SUMMARY.md`
</output>
