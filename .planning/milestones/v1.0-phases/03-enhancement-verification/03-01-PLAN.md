---
phase: 03-enhancement-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/styles/shadcn-ui.css
  - app/styles/global.css
  - app/styles/theme.css
  - packages/ui/src/shadcn/alert.tsx
  - packages/ui/src/shadcn/badge.tsx
autonomous: true
requirements: [ENHN-01, ENHN-04, ENHN-05, ENHN-06]

must_haves:
  truths:
    - "Translucent overlay tokens render in both light and dark themes with correct opacity"
    - "Semantic color tokens (red, amber, green, blue) provide bg/fg/border values in both themes"
    - "The .tech-label utility class produces uppercase monospace text at 12px with 1.2px letter-spacing"
    - "Alert and badge semantic variants use CSS variable tokens instead of hardcoded Tailwind colors"
  artifacts:
    - path: "app/styles/shadcn-ui.css"
      provides: "Translucent surface tokens, semantic color tokens, neutral gray gap fills"
      contains: "--glass-surface"
    - path: "app/styles/global.css"
      provides: "Tech-label utility class"
      contains: ".tech-label"
    - path: "app/styles/theme.css"
      provides: "Semantic color @theme entries for Tailwind class access"
      contains: "--color-semantic-red-bg"
    - path: "packages/ui/src/shadcn/alert.tsx"
      provides: "Alert variants using semantic tokens"
      contains: "semantic-green"
    - path: "packages/ui/src/shadcn/badge.tsx"
      provides: "Badge variants using semantic tokens"
      contains: "semantic-green"
  key_links:
    - from: "app/styles/shadcn-ui.css"
      to: "app/styles/theme.css"
      via: "@theme entries map CSS vars to Tailwind color tokens"
      pattern: "--color-semantic.*var\\(--semantic"
    - from: "packages/ui/src/shadcn/alert.tsx"
      to: "app/styles/theme.css"
      via: "Tailwind semantic color classes"
      pattern: "bg-semantic|text-semantic|border-semantic"
---

<objective>
Add all new CSS token layers for Phase 3: translucent surface overlays (ENHN-01), semantic color scale (ENHN-05), neutral gray gap fills (ENHN-06), and the monospace tech-label utility (ENHN-04). Then wire alert and badge components to use the new semantic color tokens.

Purpose: Completes the CSS token infrastructure so that all overlay surfaces, semantic states (error/warning/success/info), and technical label patterns are available as first-class design tokens.

Output: Updated shadcn-ui.css with new token blocks, global.css with tech-label utility, theme.css with @theme entries, and alert.tsx/badge.tsx consuming semantic tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enhancement-verification/03-CONTEXT.md
@.planning/phases/03-enhancement-verification/03-RESEARCH.md

<interfaces>
<!-- Current CSS token architecture in shadcn-ui.css -->
<!-- :root block (lines 10-76) and .dark block (lines 78-125) -->
<!-- Pattern: --token-name: oklch(L% C H); with optional /alpha for translucent -->
<!-- Existing alpha pattern: --supabase-green-border: oklch(47% 0.165 160 / 0.3) -->

<!-- theme.css @theme block maps CSS vars to Tailwind colors -->
<!-- Pattern: --color-{name}: var(--{name}); -->

<!-- alert.tsx CVA variants: default, destructive, success, warning, info -->
<!-- badge.tsx CVA variants: default, secondary, destructive, outline, success, warning, info -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add translucent surface tokens, neutral gray gaps, and tech-label utility</name>
  <files>app/styles/shadcn-ui.css, app/styles/global.css</files>
  <read_first>
    - app/styles/shadcn-ui.css (current token definitions — must see exact line structure)
    - app/styles/global.css (current @layer base body rule — must see structure before adding)
    - DESIGN.md (S2 "Surface & Overlay" and S2 neutral scale — source values)
    - .planning/phases/03-enhancement-verification/03-RESEARCH.md (pre-computed oklch values)
  </read_first>
  <action>
**In `app/styles/shadcn-ui.css` `:root` block** (after the `--sb-near-white` line, before `--background`), add:

```css
/* Neutral gray gap fills (per D-13, D-14) */
--sb-dark-gray: oklch(42% 0 none);
--sb-off-white: oklch(98.51% 0 none);

/* Translucent surface tokens (per D-01, D-02, D-03) */
--glass-surface: oklch(100% 0 none / 0.84);
--slate-alpha-wash: oklch(95.3% 0.026 229 / 0.109);

/* Semantic color tokens — light theme (per D-09, D-10, D-11, D-12) */
--semantic-red-bg: oklch(96.9% 0.015 17.0);
--semantic-red-fg: oklch(55.4% 0.197 24.9);
--semantic-red-border: oklch(79.3% 0.084 18.1);

--semantic-amber-bg: oklch(98.3% 0.019 87.6);
--semantic-amber-fg: oklch(57.8% 0.128 66.9);
--semantic-amber-border: oklch(82.4% 0.128 87.4);

--semantic-green-bg: oklch(98.0% 0.017 154.0);
--semantic-green-fg: oklch(50.6% 0.141 146.5);
--semantic-green-border: oklch(84.8% 0.076 151.1);

--semantic-blue-bg: oklch(97.3% 0.013 251.6);
--semantic-blue-fg: oklch(50.7% 0.167 254.9);
--semantic-blue-border: oklch(80.1% 0.082 255.5);
```

**In `app/styles/shadcn-ui.css` `.dark` block** (after the `--sidebar-ring` line, before closing `}`), add:

```css
/* Translucent surface tokens — dark overrides */
--glass-surface: oklch(28.1% 0 none / 0.84);
--slate-alpha-wash: oklch(27.9% 0.077 253 / 0.031);

/* Semantic color tokens — dark theme */
--semantic-red-bg: oklch(25.3% 0.065 20.0);
--semantic-red-fg: oklch(78.0% 0.128 22.1);
--semantic-red-border: oklch(35.3% 0.104 21.2);

--semantic-amber-bg: oklch(32.9% 0.073 65.9);
--semantic-amber-fg: oklch(85.4% 0.157 84.1);
--semantic-amber-border: oklch(42.0% 0.089 70.6);

--semantic-green-bg: oklch(25.6% 0.053 148.5);
--semantic-green-fg: oklch(78.0% 0.142 148.5);
--semantic-green-border: oklch(36.3% 0.090 146.6);

--semantic-blue-bg: oklch(26.3% 0.077 255.9);
--semantic-blue-fg: oklch(76.4% 0.126 249.5);
--semantic-blue-border: oklch(37.5% 0.116 254.7);
```

**In `app/styles/global.css`** — add `font-weight: 400;` to the existing `body` rule inside `@layer base` (per D-04, ENHN-02 base default). The body rule currently has `@apply bg-background text-foreground;` and `font-feature-settings` — add `font-weight: 400;` after the font-feature-settings line.

**In `app/styles/global.css`** — add a new `@layer utilities` block AFTER the `@layer base` block (before the closing of the file):

```css
@layer utilities {
  .tech-label {
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 1.2px;
    font-size: 0.75rem;
    line-height: 1.33;
  }
}
```

This implements D-07 and D-08 — the utility is additive and opt-in.
  </action>
  <verify>
    <automated>grep -c "glass-surface" app/styles/shadcn-ui.css | grep -q "2" && grep -c "semantic-red-bg" app/styles/shadcn-ui.css | grep -q "2" && grep -c "semantic-blue-border" app/styles/shadcn-ui.css | grep -q "2" && grep -c "sb-dark-gray" app/styles/shadcn-ui.css | grep -q "1" && grep -c "sb-off-white" app/styles/shadcn-ui.css | grep -q "1" && grep -c "tech-label" app/styles/global.css | grep -q "1" && grep -c "font-weight: 400" app/styles/global.css | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - shadcn-ui.css contains `--glass-surface` in both `:root` and `.dark` blocks (2 occurrences)
    - shadcn-ui.css contains `--slate-alpha-wash` in both blocks (2 occurrences)
    - shadcn-ui.css contains `--sb-dark-gray: oklch(42% 0 none)` in `:root`
    - shadcn-ui.css contains `--sb-off-white: oklch(98.51% 0 none)` in `:root`
    - shadcn-ui.css contains all 12 `--semantic-*` tokens in `:root` (red/amber/green/blue x bg/fg/border)
    - shadcn-ui.css contains all 12 `--semantic-*` tokens in `.dark`
    - global.css body rule contains `font-weight: 400;`
    - global.css contains `@layer utilities` block with `.tech-label` class
    - `.tech-label` class has `font-family: var(--font-mono)`, `text-transform: uppercase`, `letter-spacing: 1.2px`, `font-size: 0.75rem`, `line-height: 1.33`
  </acceptance_criteria>
  <done>All new CSS tokens (translucent surfaces, semantic colors, neutral gray gaps) are defined in both theme blocks, body weight default is set to 400, and tech-label utility class is available.</done>
</task>

<task type="auto">
  <name>Task 2: Register semantic tokens in theme.css and wire alert/badge components</name>
  <files>app/styles/theme.css, packages/ui/src/shadcn/alert.tsx, packages/ui/src/shadcn/badge.tsx</files>
  <read_first>
    - app/styles/theme.css (current @theme entries — must see pattern for adding new ones)
    - packages/ui/src/shadcn/alert.tsx (current CVA variant definitions for success/warning/info)
    - packages/ui/src/shadcn/badge.tsx (current CVA variant definitions for success/warning/info)
    - app/styles/shadcn-ui.css (confirm Task 1 tokens exist — verify before wiring)
  </read_first>
  <action>
**In `app/styles/theme.css`** — inside the `@theme` block, AFTER the `--color-chart-5` line (around line 44) and BEFORE the `--radius-radius` line, add semantic color token mappings:

```css
--color-semantic-red-bg: var(--semantic-red-bg);
--color-semantic-red-fg: var(--semantic-red-fg);
--color-semantic-red-border: var(--semantic-red-border);

--color-semantic-amber-bg: var(--semantic-amber-bg);
--color-semantic-amber-fg: var(--semantic-amber-fg);
--color-semantic-amber-border: var(--semantic-amber-border);

--color-semantic-green-bg: var(--semantic-green-bg);
--color-semantic-green-fg: var(--semantic-green-fg);
--color-semantic-green-border: var(--semantic-green-border);

--color-semantic-blue-bg: var(--semantic-blue-bg);
--color-semantic-blue-fg: var(--semantic-blue-fg);
--color-semantic-blue-border: var(--semantic-blue-border);
```

This enables first-class Tailwind usage like `bg-semantic-red-bg` instead of `bg-[var(--semantic-red-bg)]`. Follows research open question 1 recommendation.

**In `packages/ui/src/shadcn/alert.tsx`** — update the CVA `alertVariants` success, warning, and info variants to use semantic tokens. Keep `destructive` variant unchanged (it uses `--destructive` token for button consistency per research pitfall 3). Replace:

- `success` variant: change from `'border-green-600/50 text-green-600 dark:border-green-600 [&>svg]:text-green-600'` to `'border-semantic-green-border bg-semantic-green-bg text-semantic-green-fg [&>svg]:text-semantic-green-fg'`
- `warning` variant: change from `'border-orange-600/50 text-orange-600 dark:border-orange-600 [&>svg]:text-orange-600'` to `'border-semantic-amber-border bg-semantic-amber-bg text-semantic-amber-fg [&>svg]:text-semantic-amber-fg'`
- `info` variant: change from `'border-blue-600/50 text-blue-600 dark:border-blue-600 [&>svg]:text-blue-600'` to `'border-semantic-blue-border bg-semantic-blue-bg text-semantic-blue-fg [&>svg]:text-semantic-blue-fg'`

**In `packages/ui/src/shadcn/badge.tsx`** — update the CVA `badgeVariants` success, warning, and info variants to use semantic tokens. Replace:

- `success` variant: change from `'border-transparent bg-green-50 hover:bg-green-50 text-green-500 dark:bg-green-500/20 dark:hover:bg-green-500/20'` to `'border-transparent bg-semantic-green-bg text-semantic-green-fg'`
- `warning` variant: change from `'border-transparent bg-orange-50 hover:bg-orange-50 text-orange-500 dark:bg-orange-500/20 dark:hover:bg-orange-500/20'` to `'border-transparent bg-semantic-amber-bg text-semantic-amber-fg'`
- `info` variant: change from `'border-transparent bg-blue-50 hover:bg-blue-50 text-blue-500 dark:bg-blue-500/20 dark:hover:bg-blue-500/20'` to `'border-transparent bg-semantic-blue-bg text-semantic-blue-fg'`

Run `pnpm typecheck` and `pnpm format:fix` after changes.
  </action>
  <verify>
    <automated>grep -c "color-semantic" app/styles/theme.css | grep -q "12" && grep "semantic-green" packages/ui/src/shadcn/alert.tsx | grep -q "bg-semantic-green-bg" && grep "semantic-green" packages/ui/src/shadcn/badge.tsx | grep -q "bg-semantic-green-bg" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - theme.css @theme block contains 12 `--color-semantic-*` entries (4 families x 3 steps)
    - alert.tsx success variant contains `bg-semantic-green-bg text-semantic-green-fg border-semantic-green-border`
    - alert.tsx warning variant contains `bg-semantic-amber-bg text-semantic-amber-fg border-semantic-amber-border`
    - alert.tsx info variant contains `bg-semantic-blue-bg text-semantic-blue-fg border-semantic-blue-border`
    - alert.tsx destructive variant is UNCHANGED (still uses `--destructive` token)
    - badge.tsx success variant contains `bg-semantic-green-bg text-semantic-green-fg`
    - badge.tsx warning variant contains `bg-semantic-amber-bg text-semantic-amber-fg`
    - badge.tsx info variant contains `bg-semantic-blue-bg text-semantic-blue-fg`
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>Semantic color tokens are registered as first-class Tailwind colors, and alert/badge components consume them for theme-aware rendering in both light and dark modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply — this plan modifies only CSS variables and Tailwind class names in UI components. No authentication, data access, or user input processing involved.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | N/A | CSS tokens | accept | CSS-only changes with no security surface |
</threat_model>

<verification>
1. All new tokens present in both `:root` and `.dark` blocks of shadcn-ui.css
2. Tech-label utility class defined in global.css @layer utilities
3. 12 @theme entries in theme.css for semantic colors
4. Alert and badge components use semantic token classes instead of hardcoded Tailwind colors
5. `pnpm typecheck` passes
</verification>

<success_criteria>
- shadcn-ui.css has 2 translucent tokens, 24 semantic color tokens (12 per theme), and 2 neutral gray gap fills
- global.css has body font-weight: 400 and .tech-label utility
- theme.css has 12 --color-semantic-* entries
- alert.tsx and badge.tsx semantic variants use CSS variable tokens
- No hardcoded green-600/orange-600/blue-600 colors remain in alert/badge semantic variants
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhancement-verification/03-01-SUMMARY.md`
</output>
