---
phase: 06-housing-employee-review
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified: []
autonomous: false
requirements:
  - HOUS-01
  - HOUS-02
  - HOUS-03
  - HOUS-04
  - EREV-01
  - EREV-02
  - EREV-03
  - EREV-04
  - EREV-05
  - EREV-06

must_haves:
  truths:
    - "Database schema is pushed to hosted Supabase and all 4 migrations applied"
    - "Housing grid renders with data from app_hr_housing view"
    - "Employee review grid renders with data from app_hr_employee_reviews view"
    - "Application builds and typechecks successfully"
  artifacts: []
  key_links: []
---

<objective>
Push database schema to hosted Supabase and verify the complete phase: both housing and employee review submodules render and function correctly.

Purpose: Schema must be pushed to the live database for the views and tables to exist. Visual verification confirms end-to-end functionality.
Output: Schema pushed, both submodules verified working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-housing-employee-review/06-01-SUMMARY.md
@.planning/phases/06-housing-employee-review/06-02-SUMMARY.md
@.planning/phases/06-housing-employee-review/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push database schema and run typecheck + lint</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260411000001_org_site_max_beds.sql (verify migration exists)
    - supabase/migrations/20260411000002_app_hr_housing.sql (verify migration exists)
    - supabase/migrations/20260411000003_hr_employee_review.sql (verify migration exists)
    - supabase/migrations/20260411000004_app_hr_employee_reviews.sql (verify migration exists)
  </read_first>
  <action>
    Run the following commands in sequence:

    1. Push schema to hosted Supabase (per project convention -- uses hosted Supabase, not local Docker):
       ```bash
       pnpm supabase db push
       ```
       If this prompts for confirmation, it may need the `SUPABASE_ACCESS_TOKEN` environment variable set for non-interactive mode. If push fails due to interactive prompt, flag for manual intervention.

    2. Generate updated TypeScript types:
       ```bash
       pnpm supabase:typegen
       ```

    3. Run typecheck to verify all code compiles:
       ```bash
       pnpm typecheck
       ```

    4. Run format and lint:
       ```bash
       pnpm format:fix && pnpm lint:fix
       ```

    If `supabase db push` fails due to SQL errors in migrations, report the exact error and which migration file caused it.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm supabase db push` completes without SQL errors
    - `pnpm supabase:typegen` regenerates app/lib/database.types.ts with hr_employee_review table types
    - `pnpm typecheck` exits with code 0
    - `pnpm format:fix && pnpm lint:fix` exits with code 0
  </acceptance_criteria>
  <done>All 4 migrations applied to hosted Supabase. TypeScript types regenerated. Typecheck and lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of housing and employee review grids</name>
  <files></files>
  <action>
    Human visually verifies both housing and employee review submodules are functioning correctly in the browser.
  </action>
  <what-built>
    Complete housing and employee review submodules:
    1. Housing grid with max beds, tenant count, available beds columns
    2. Housing detail row showing tenant list on row click
    3. Housing create/edit form (name, max beds, notes)
    4. Employee review grid with score columns (color-coded 1-3), average, lock icon
    5. Year-Quarter filter for employee reviews
    6. Employee review create/edit form with 1-3 score selects
    7. Employee review detail row showing full review details
    8. Locked review edit prevention
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Navigate to the Housing submodule grid
       - Verify columns: Housing Name, Max Beds, Tenants, Available Beds
       - Click a housing row -- verify tenant list expands below with avatar, name, department, start date, work auth
       - Click Create -- verify side panel opens with name, max beds, notes fields
    3. Navigate to the Employee Review submodule grid
       - Verify columns: Employee (avatar+name), Dept, Quarter, Prod, Attend, Quality, Engage, Avg, Notes, Lead, Lock icon
       - Verify score cells are color-coded: 1=red, 2=amber, 3=green
       - Verify Average column shows 1 decimal (e.g., "2.3")
       - Change Year and Quarter filters -- verify grid data updates
       - Click Create -- verify side panel with employee dropdown, year, quarter select (Q1-Q4), 4 score selects (1-3), notes, lead dropdown, locked checkbox
       - Click a review row -- verify detail row expands showing all scores, notes, lead, dates, lock status
       - If any locked reviews exist, verify lock icon shows and edit form fields are disabled
    4. Both grids support search, column sort, and column resize
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — awaiting user approval"</automated>
  </verify>
  <acceptance_criteria>
    - Housing grid displays housing sites with Name, Max Beds, Tenants, Available Beds columns
    - Housing detail row shows tenant avatars, names, departments
    - Employee review grid shows scores with color coding (red/amber/green)
    - Year-Quarter filter changes grid data
    - User types "approved"
  </acceptance_criteria>
  <done>User approves visual verification of both housing and employee review submodules.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is verification-only; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Information Disclosure | database.types.ts | accept | Generated types contain schema structure only, no data; committed to repo is standard practice |
</threat_model>

<verification>
Schema pushed. Both grids render with correct data. Typecheck and lint pass. Human verifies visual correctness.
</verification>

<success_criteria>
- All 4 SQL migrations applied to hosted Supabase
- TypeScript types regenerated with hr_employee_review
- Both housing and employee review grids render correctly
- Score color coding, Year-Quarter filter, lock icon all work
- Detail rows expand with correct content
- Create/edit forms functional
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/06-housing-employee-review/06-04-SUMMARY.md`
</output>
