---
phase: 05-hours-comparison
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - HCMP-01
  - HCMP-02
  - HCMP-03
  - HCMP-04
  - HCMP-05

must_haves:
  truths:
    - "Database schema includes app_hr_hours_comparison view and it returns data"
    - "Hours Comparison grid renders in the browser with all columns visible"
    - "Pay period filter changes the displayed data"
    - "Variance cells are colored amber/red appropriately"
    - "Row expansion shows daily schedule breakdown"
  artifacts: []
  key_links: []
---

<objective>
Reset database schema to apply the new migration and visually verify the complete Hours Comparison submodule.

Purpose: Ensures the SQL view is applied to the running database (HCMP-05) and all UI requirements (HCMP-01 through HCMP-04) work end-to-end.

Output: Schema applied, visual confirmation of all requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hours-comparison/05-CONTEXT.md
@.planning/phases/05-hours-comparison/05-01-SUMMARY.md
@.planning/phases/05-hours-comparison/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Reset database schema to apply hours comparison view</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260410000001_app_hr_hours_comparison.sql
  </read_first>
  <action>
Run `pnpm supabase:reset` to apply all migrations including the new `app_hr_hours_comparison` view.

This is a BLOCKING task — the Hours Comparison grid cannot display data without the view existing in the database. The view is created by migration `20260410000001_app_hr_hours_comparison.sql`.

After reset completes, run `pnpm format:fix && pnpm lint:fix` to clean up any formatting issues from all modified files.

Then run `pnpm typecheck` to confirm everything compiles.
  </action>
  <verify>
    <automated>pnpm supabase:reset 2>&1 | tail -5 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm supabase:reset` completes without errors
    - `pnpm typecheck` passes
    - `pnpm lint:fix` passes (or only warnings)
  </acceptance_criteria>
  <done>Database schema includes app_hr_hours_comparison view; all code compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Hours Comparison submodule</name>
  <what-built>
Complete Hours Comparison submodule with:
- Comparison grid showing employee photo, name, department, scheduled hours, payroll hours, and variance
- Pay period filter defaulting to most recent period
- Variance highlighting (amber for any mismatch, red for >= 4 hours difference)
- Row-click detail expansion showing daily schedule breakdown
- Pinned totals row at bottom
- Search and CSV export
  </what-built>
  <how-to-verify>
1. Navigate to the Hours Comparison submodule in the HR module (the payroll_hours slug under the workspace)
2. **HCMP-01**: Verify the grid shows columns: employee photo, full name, department, scheduled hours, payroll hours, variance
3. **HCMP-02**: Verify the pay period dropdown is visible and defaults to the most recent period. Change the period and confirm the grid data updates.
4. **HCMP-04**: Check variance column — cells with any non-zero variance should be amber text, cells with >= 4 hours variance should be red bold text
5. **HCMP-03**: Click on an employee row to expand the detail. Verify it shows daily schedule entries with: date, day of week, department, task, start time, end time, hours
6. Verify the pinned bottom row shows TOTAL with summed scheduled hours, payroll hours, and variance
7. Test search: type an employee name and confirm filtering works
8. Test CSV export button
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only applies schema and verifies — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new code in this plan |
</threat_model>

<verification>
- `pnpm supabase:reset` succeeds (view created)
- `pnpm typecheck` passes
- Visual verification of all 5 HCMP requirements in browser
</verification>

<success_criteria>
- Database has app_hr_hours_comparison view
- All code compiles and passes lint
- Human confirms all HCMP requirements work in the browser
</success_criteria>

<output>
After completion, create `.planning/phases/05-hours-comparison/05-03-SUMMARY.md`
</output>
