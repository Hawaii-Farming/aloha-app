---
phase: 02-light-theme-component-theming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
  - app/styles/shadcn-ui.css
  - app/styles/global.css
  - packages/ui/src/shadcn/sonner.tsx
  - packages/ui/src/shadcn/sidebar.tsx
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - COMP-01
  - COMP-02
  - COMP-07
  - COMP-08

must_haves:
  truths:
    - "DESIGN.md contains a complete Light Mode Palette section with oklch values for every Shadcn semantic token"
    - "The :root block in shadcn-ui.css uses oklch values matching DESIGN.md light palette — no stock Shadcn var(--color-*) references remain for semantic tokens"
    - "The :root block includes --sidebar-* tokens for a light sidebar with green accent (per D-02)"
    - "Shadow nullification tokens exist in :root in global.css (matching the .dark shadow nullification)"
    - "Form input tokens (--input, --ring, --border) are set to Supabase-style values in :root"
    - "Toast/Sonner uses semantic palette tokens (bg-background, text-foreground, border-border) and renders correctly in both themes"
    - "Sidebar nav links use font-weight 500 (font-medium) for all items, not just active ones"
    - "Light mode green link tokens (--supabase-green-link, --supabase-green-border) are reviewed and adjusted for white backgrounds — darker green values for sufficient contrast"
  artifacts:
    - path: "DESIGN.md"
      provides: "Complete light palette specification"
      contains: "Light Mode Palette"
    - path: "app/styles/shadcn-ui.css"
      provides: "Supabase light palette CSS tokens in :root"
      contains: "oklch"
    - path: "app/styles/global.css"
      provides: "Shadow nullification for light theme"
      contains: "--shadow: none"
    - path: "packages/ui/src/shadcn/sonner.tsx"
      provides: "Toast theming with semantic tokens"
      contains: "bg-background"
    - path: "packages/ui/src/shadcn/sidebar.tsx"
      provides: "Sidebar nav links with font-medium on all items"
      contains: "font-medium"
  key_links:
    - from: "DESIGN.md"
      to: "app/styles/shadcn-ui.css"
      via: "oklch values copied from spec to :root block"
      pattern: "oklch\\("
    - from: "app/styles/shadcn-ui.css"
      to: "app/styles/theme.css"
      via: "var() references bridge tokens to Tailwind utilities"
      pattern: "var\\(--"
    - from: "packages/ui/src/shadcn/sonner.tsx"
      to: "app/styles/shadcn-ui.css"
      via: "Semantic Tailwind classes consume CSS tokens"
      pattern: "bg-background"
---

<objective>
Define the complete Supabase light palette in DESIGN.md, then implement it as oklch CSS tokens in the :root block of shadcn-ui.css. Also add shadow nullification to :root in global.css, verify toast/sonner theming, apply sidebar nav weight-500, and review light-mode green link tokens.

Purpose: Establishes the light theme foundation — every semantic token gets its Supabase light equivalent, completing the dual-theme color system started in Phase 1. Also ensures toast, sidebar, and link components properly consume the new tokens. COMP-02 (form input theming) is delivered by the --input, --ring, and --border token overrides in Task 2 — form components (input, select, checkbox, radio, textarea) use these semantic tokens and inherit the light palette automatically.

Output: Updated DESIGN.md with light palette spec, fully-themed :root block in shadcn-ui.css, shadow nullification in global.css, verified sonner.tsx, sidebar font-weight applied, light-mode green link tokens adjusted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-light-theme-component-theming/02-CONTEXT.md
@.planning/phases/02-light-theme-component-theming/02-RESEARCH.md

<interfaces>
<!-- Current :root token structure in shadcn-ui.css (lines 10-76) — executor replaces values in-place -->
<!-- Current .dark token structure (lines 78-121) — reference for mirroring pattern -->
<!-- theme.css bridges tokens to Tailwind via var() — no changes needed there -->

From app/styles/shadcn-ui.css :root (current stock values to replace):
```css
--background: var(--color-white);
--foreground: var(--color-neutral-950);
--card: var(--color-white);
--card-foreground: var(--color-neutral-950);
--popover: var(--color-white);
--popover-foreground: var(--color-neutral-950);
--primary: var(--color-neutral-950);
--primary-foreground: var(--color-white);
--secondary: oklch(96.76% 0.0013 286.38);
--secondary-foreground: oklch(21.03% 0.0318 264.65);
--muted: oklch(96.71% 0.0029 264.54);
--muted-foreground: oklch(55.13% 0.0233 264.36);
--accent: oklch(96.76% 0.0013 286.38);
--accent-foreground: oklch(21.03% 0.0318 264.65);
--destructive: var(--color-red-500);
--destructive-foreground: var(--color-white);
--border: var(--color-gray-100);
--input: var(--color-gray-200);
--ring: var(--color-neutral-800);
--sidebar-background: var(--color-neutral-50);
--sidebar-foreground: oklch(37.05% 0.012 285.8);
--sidebar-primary: var(--color-neutral-950);
--sidebar-primary-foreground: var(--color-white);
--sidebar-accent: var(--color-neutral-100);
--sidebar-accent-foreground: var(--color-neutral-950);
--sidebar-border: var(--border);
--sidebar-ring: var(--color-blue-500);
```

From app/styles/shadcn-ui.css :root (green tokens — shared, need light-mode review):
```css
--supabase-green: oklch(73.5% 0.158 162);
--supabase-green-link: oklch(71.2% 0.184 160);
--supabase-green-border: oklch(73.5% 0.158 162 / 0.3);
```

From app/styles/global.css (shadow nullification — dark only, need :root equivalent):
```css
.dark {
  --shadow: none;
  --shadow-sm: none;
  --shadow-md: none;
  --shadow-lg: none;
  --shadow-xl: none;
  --shadow-2xl: none;
}
```

From packages/ui/src/shadcn/sonner.tsx (toast theming — uses semantic tokens):
```typescript
toastOptions: {
  classNames: {
    toast:
      'group toast group-[.toaster]:bg-background group-[.toaster]:text-foreground group-[.toaster]:border-border group-[.toaster]:shadow-lg',
    description: 'group-[.toast]:text-muted-foreground',
    actionButton:
      'group-[.toast]:bg-primary group-[.toast]:text-primary-foreground',
    cancelButton:
      'group-[.toast]:bg-muted group-[.toast]:text-muted-foreground',
  },
},
```

From packages/ui/src/shadcn/sidebar.tsx (SidebarMenuButton — line 535):
```typescript
'peer/menu-button ... text-left text-sm outline-hidden ... data-[active=true]:font-medium ...'
```
Note: font-medium is only on data-[active=true]. COMP-01 requires weight-500 on ALL nav links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add complete light palette specification to DESIGN.md</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (full file — understand existing structure, dark palette section, component stylings)
    - app/styles/shadcn-ui.css (see current .dark block oklch values as reference for light counterparts)
    - .planning/phases/02-light-theme-component-theming/02-RESEARCH.md (light palette token table at lines 193-214, sidebar tokens at lines 219-233)
  </read_first>
  <action>
Add a new section to DESIGN.md titled "### Light Mode Palette" inside the "## 2. Color Palette and Roles" section, after the existing dark mode content. Per D-01, mirror Supabase Studio's dashboard light theme. Per D-03, this spec must be complete before any CSS is touched. Per D-04, use different green intensities per theme.

The section must contain two tables:

**Light Mode Semantic Tokens:**

| Token | oklch Value | Hex Approx | Notes |
|-------|------------|------------|-------|
| --background | oklch(99% 0 none) | ~#fafafa | Near-white canvas |
| --foreground | oklch(12% 0 none) | ~#171717 | Near-black text |
| --card | oklch(100% 0 none) | #ffffff | Pure white cards |
| --card-foreground | oklch(12% 0 none) | ~#171717 | Same as foreground |
| --popover | oklch(100% 0 none) | #ffffff | Pure white |
| --popover-foreground | oklch(12% 0 none) | ~#171717 | Same as foreground |
| --primary | oklch(12% 0 none) | ~#171717 | Near-black CTA |
| --primary-foreground | oklch(99% 0 none) | ~#fafafa | White text on primary |
| --secondary | oklch(96% 0 none) | ~#f0f0f0 | Light gray secondary |
| --secondary-foreground | oklch(12% 0 none) | ~#171717 | Dark text |
| --muted | oklch(96% 0 none) | ~#f0f0f0 | Light gray muted bg |
| --muted-foreground | oklch(45% 0 none) | ~#6b6b6b | Medium gray text |
| --accent | oklch(96% 0 none) | ~#f0f0f0 | Light gray accent |
| --accent-foreground | oklch(12% 0 none) | ~#171717 | Dark text |
| --destructive | var(--color-red-500) | - | Keep Tailwind red |
| --destructive-foreground | var(--color-white) | - | White on red |
| --border | oklch(89% 0 none) | ~#e2e2e2 | Light gray border |
| --input | oklch(85% 0 none) | ~#d4d4d4 | Slightly darker input border |
| --ring | oklch(47% 0.165 160) | ~#1d9e65 | Darker green focus ring (D-04) |

**Light Mode Sidebar Tokens (D-02: light sidebar):**

| Token | oklch Value | Notes |
|-------|------------|-------|
| --sidebar-background | oklch(99% 0 none) | Near-white, matches page bg |
| --sidebar-foreground | oklch(35% 0 none) | Dark-gray nav text |
| --sidebar-primary | oklch(47% 0.165 160) | Darker green for active items (D-04) |
| --sidebar-primary-foreground | oklch(99% 0 none) | White text on green |
| --sidebar-accent | oklch(95% 0.01 160) | Very subtle green wash for hover/active bg |
| --sidebar-accent-foreground | oklch(30% 0 none) | Dark text on accent |
| --sidebar-border | oklch(89% 0 none) | Same as --border |
| --sidebar-ring | oklch(47% 0.165 160) | Green focus ring |

**Light Mode Green Accent Note:**
Add a paragraph explaining per D-04 that light mode uses a darker green (oklch(47% 0.165 160) approx #1d9e65) while dark mode uses a brighter green (oklch(73.5% 0.158 162) approx #3ecf8e). The --supabase-green, --supabase-green-link, and --supabase-green-border tokens in :root remain as the dark-mode defaults; light-mode-specific overrides for --ring and --sidebar-primary use the darker green.

**Light Mode Link Color Hierarchy (COMP-07):**
Add a sub-section documenting the 4 link styles for light mode:
1. **Green branded link**: Uses --supabase-green-link — for light mode, override in :root to oklch(47% 0.165 160) (darker green for white bg contrast)
2. **Primary link**: Uses text-foreground with underline — oklch(12% 0 none)
3. **Secondary link**: Uses text-muted-foreground — oklch(45% 0 none)
4. **Muted link**: Uses text-muted-foreground with opacity — oklch(45% 0 none) at lower opacity

Also add a note about shadow removal: "Light theme uses the same border-depth system as dark — all --shadow-* tokens are set to `none` in both themes."

Mark the light palette section status as "Complete" (removing any "Active" or "Incomplete" markers).
  </action>
  <verify>
    <automated>grep -c "Light Mode Palette" DESIGN.md && grep -c "oklch(99% 0 none)" DESIGN.md && grep -c "oklch(47% 0.165 160)" DESIGN.md && grep -c "Link Color Hierarchy" DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains the heading "### Light Mode Palette"
    - DESIGN.md contains "oklch(99% 0 none)" for --background light value
    - DESIGN.md contains "oklch(12% 0 none)" for --foreground light value
    - DESIGN.md contains "oklch(47% 0.165 160)" for light mode green
    - DESIGN.md contains "--sidebar-background" in the light sidebar tokens table
    - DESIGN.md contains "oklch(95% 0.01 160)" for --sidebar-accent
    - DESIGN.md contains "Link Color Hierarchy" sub-section with 4 link styles
    - No "Active" or "Incomplete" status markers on the light palette section
  </acceptance_criteria>
  <done>DESIGN.md has a complete Light Mode Palette section with oklch values for every semantic token and sidebar token, link color hierarchy for light mode, matching the Supabase Studio dashboard aesthetic with darker green accents for light mode</done>
</task>

<task type="auto">
  <name>Task 2: Override :root tokens with Supabase light palette, adjust green link tokens, and add shadow nullification</name>
  <files>app/styles/shadcn-ui.css, app/styles/global.css</files>
  <read_first>
    - DESIGN.md (read the Light Mode Palette section just created in Task 1 — source of truth for all values)
    - app/styles/shadcn-ui.css (current :root block lines 10-76 to replace, .dark block lines 78-121 as pattern reference)
    - app/styles/global.css (current .dark shadow nullification at lines 49-56 — need to add :root equivalent)
    - app/styles/theme.css (verify var() bridge still works — no changes needed)
  </read_first>
  <action>
**In app/styles/shadcn-ui.css**, replace the semantic token values in the `:root` block (lines 32-75) with oklch values from the DESIGN.md light palette spec. Keep the font variables (lines 11-13), brand tokens (lines 15-19), and neutral scale (lines 21-30) unchanged — they are shared across themes. Only replace the semantic tokens:

Replace these lines:
```css
--background: var(--color-white);
--foreground: var(--color-neutral-950);
```
With:
```css
--background: oklch(99% 0 none);
--foreground: oklch(12% 0 none);
```

Full replacement list for :root semantic tokens:
```css
--background: oklch(99% 0 none);
--foreground: oklch(12% 0 none);

--card: oklch(100% 0 none);
--card-foreground: oklch(12% 0 none);

--popover: oklch(100% 0 none);
--popover-foreground: oklch(12% 0 none);

--primary: oklch(12% 0 none);
--primary-foreground: oklch(99% 0 none);

--secondary: oklch(96% 0 none);
--secondary-foreground: oklch(12% 0 none);

--muted: oklch(96% 0 none);
--muted-foreground: oklch(45% 0 none);

--accent: oklch(96% 0 none);
--accent-foreground: oklch(12% 0 none);

--destructive: var(--color-red-500);
--destructive-foreground: var(--color-white);

--border: oklch(89% 0 none);
--input: oklch(85% 0 none);
--ring: oklch(47% 0.165 160);
```

Keep --radius and --chart-* tokens unchanged.

Replace sidebar tokens in :root (per D-02 light sidebar):
```css
--sidebar-background: oklch(99% 0 none);
--sidebar-foreground: oklch(35% 0 none);
--sidebar-primary: oklch(47% 0.165 160);
--sidebar-primary-foreground: oklch(99% 0 none);
--sidebar-accent: oklch(95% 0.01 160);
--sidebar-accent-foreground: oklch(30% 0 none);
--sidebar-border: oklch(89% 0 none);
--sidebar-ring: oklch(47% 0.165 160);
```

**COMP-02 — Form input theming (per D-01):** The --input, --ring, and --border token overrides above are what theme form inputs. The form components (input.tsx, select.tsx, checkbox.tsx, radio-group.tsx, textarea.tsx) all use semantic classes (border-input, ring-ring, bg-background, text-foreground) that resolve to these :root tokens. No component file changes are needed for COMP-02 — the token-driven approach delivers form input theming automatically.

**COMP-07 Light mode green link tokens:** Also adjust the --supabase-green-link and --supabase-green-border values in the :root block for light-mode contrast. The bright dark-mode green (oklch(71.2%)) does not have sufficient contrast on white backgrounds. Update:
```css
--supabase-green-link: oklch(47% 0.165 160);
--supabase-green-border: oklch(47% 0.165 160 / 0.3);
```
Then in the `.dark` block, add these tokens to preserve the original bright green values:
```css
--supabase-green-link: oklch(71.2% 0.184 160);
--supabase-green-border: oklch(73.5% 0.158 162 / 0.3);
```
This gives the 4-level link hierarchy proper per-theme contrast: green branded links use darker green in light mode, brighter green in dark mode.

**In app/styles/global.css**, add :root shadow nullification inside the existing `@layer base` block, BEFORE the `.dark` block. Add:
```css
:root {
  --shadow: none;
  --shadow-sm: none;
  --shadow-md: none;
  --shadow-lg: none;
  --shadow-xl: none;
  --shadow-2xl: none;
}
```

This goes inside `@layer base { ... }` right before the existing `.dark { --shadow: none; ... }` block (around line 49).
  </action>
  <verify>
    <automated>grep "oklch(99% 0 none)" app/styles/shadcn-ui.css | head -3 && grep "oklch(47% 0.165 160)" app/styles/shadcn-ui.css | head -3 && grep -A1 ":root" app/styles/global.css | grep "shadow" && grep "supabase-green-link" app/styles/shadcn-ui.css</automated>
  </verify>
  <acceptance_criteria>
    - app/styles/shadcn-ui.css :root block contains "--background: oklch(99% 0 none)"
    - app/styles/shadcn-ui.css :root block contains "--foreground: oklch(12% 0 none)"
    - app/styles/shadcn-ui.css :root block contains "--ring: oklch(47% 0.165 160)"
    - app/styles/shadcn-ui.css :root block contains "--sidebar-primary: oklch(47% 0.165 160)"
    - app/styles/shadcn-ui.css :root block contains "--sidebar-accent: oklch(95% 0.01 160)"
    - app/styles/shadcn-ui.css :root block contains "--supabase-green-link: oklch(47% 0.165 160)" (darker for light mode)
    - app/styles/shadcn-ui.css .dark block contains "--supabase-green-link: oklch(71.2% 0.184 160)" (bright for dark mode)
    - app/styles/shadcn-ui.css :root block does NOT contain "var(--color-white)" for --background
    - app/styles/shadcn-ui.css :root block does NOT contain "var(--color-neutral-950)" for --foreground
    - app/styles/global.css contains ":root" with "--shadow: none" inside @layer base
    - app/styles/global.css still contains ".dark" with "--shadow: none"
    - app/styles/shadcn-ui.css .dark block existing tokens (lines 78-121) are unchanged except for added green link tokens
    - app/styles/shadcn-ui.css font variables (lines 11-13), brand tokens (lines 15-19), neutral scale (lines 21-30) are unchanged
  </acceptance_criteria>
  <done>The :root block has complete Supabase light palette in oklch, sidebar tokens set for light sidebar with green accents, form input tokens themed via --input/--ring/--border for COMP-02, green link tokens adjusted per-theme for COMP-07 link hierarchy, and global.css has shadow nullification for both :root and .dark</done>
</task>

<task type="auto">
  <name>Task 3: Verify sonner toast theming and apply sidebar nav weight-500</name>
  <files>packages/ui/src/shadcn/sonner.tsx, packages/ui/src/shadcn/sidebar.tsx</files>
  <read_first>
    - packages/ui/src/shadcn/sonner.tsx (current toast classNames — verify semantic tokens)
    - packages/ui/src/shadcn/sidebar.tsx (line ~535 SidebarMenuButton class string — find data-[active=true]:font-medium)
    - app/styles/shadcn-ui.css (verify :root tokens from Task 2 are in place — bg-background, text-foreground, border-border will resolve to light palette)
  </read_first>
  <action>
**COMP-08 — Sonner toast theming:**
Review packages/ui/src/shadcn/sonner.tsx. The current classNames already use semantic tokens (bg-background, text-foreground, border-border, text-muted-foreground, bg-primary, bg-muted). These tokens will automatically resolve to the light palette values set in Task 2. Verify that:
1. The `theme` prop correctly passes the next-themes value to Sonner
2. No hardcoded color classes exist (no bg-white, bg-black, text-gray-*, etc.)
3. The shadow-lg on the toast class is intentional (elevated toast over page content) and should NOT be removed
4. If any hardcoded colors are found, replace them with semantic token classes

If sonner.tsx already uses all semantic tokens correctly (which it appears to based on current code), document this verification and make no changes. If any adjustments are needed, make them.

**COMP-01 — Sidebar nav link weight-500:**
In packages/ui/src/shadcn/sidebar.tsx, find the SidebarMenuButton class string (around line 535). Currently it has `data-[active=true]:font-medium` which only applies font-weight 500 to active items. Per COMP-01, ALL sidebar nav links should use weight-500.

Change: Add `font-medium` to the base class string of SidebarMenuButton (not conditional on active state). Keep `data-[active=true]:font-medium` as well (it becomes redundant but harmless, or remove it for cleanliness since the base already applies font-medium).

The SidebarMenuButton class string currently starts with:
```
'peer/menu-button ring-sidebar-ring hover:bg-sidebar-accent ...'
```
Add `font-medium` to this base string:
```
'peer/menu-button font-medium ring-sidebar-ring hover:bg-sidebar-accent ...'
```
And optionally remove the now-redundant `data-[active=true]:font-medium`.

Do NOT modify any other SidebarMenu* components — only SidebarMenuButton needs the weight change.
  </action>
  <verify>
    <automated>grep "font-medium" packages/ui/src/shadcn/sidebar.tsx | grep "peer/menu-button" && grep -c "bg-background" packages/ui/src/shadcn/sonner.tsx && grep -c "bg-white\|bg-black\|text-gray" packages/ui/src/shadcn/sonner.tsx</automated>
  </verify>
  <acceptance_criteria>
    - packages/ui/src/shadcn/sidebar.tsx SidebarMenuButton base class contains "font-medium" (not just on data-[active=true])
    - packages/ui/src/shadcn/sonner.tsx uses only semantic token classes (bg-background, text-foreground, etc.)
    - packages/ui/src/shadcn/sonner.tsx has zero hardcoded color classes (bg-white, bg-black, text-gray-*)
    - packages/ui/src/shadcn/sonner.tsx preserves shadow-lg on toast (intentional elevation)
    - TypeScript compilation passes for both files
  </acceptance_criteria>
  <done>Sonner toast verified to use semantic palette tokens (auto-themed via :root/:dark). Sidebar nav links all use font-weight 500 via font-medium base class on SidebarMenuButton.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS token values | Static CSS values in source files — no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CSS tokens | accept | CSS variables are client-visible by design; no secrets in color tokens |
| T-02-02 | Denial of Service | global.css shadow override | accept | Static CSS; no runtime computation; cannot be abused |
</threat_model>

<verification>
1. `grep -c "var(--color-white)" app/styles/shadcn-ui.css` returns 0 (for semantic tokens — destructive-foreground may still use var(--color-white))
2. `grep -c "oklch" app/styles/shadcn-ui.css` returns at least 30 (dark + light combined)
3. `grep "shadow: none" app/styles/global.css | wc -l` returns at least 2 (:root + .dark)
4. Light Mode Palette section exists in DESIGN.md
5. `grep "supabase-green-link" app/styles/shadcn-ui.css | wc -l` returns at least 2 (:root + .dark)
6. `grep "font-medium" packages/ui/src/shadcn/sidebar.tsx | grep "peer/menu-button"` returns a match
7. `grep "bg-background" packages/ui/src/shadcn/sonner.tsx` returns a match
</verification>

<success_criteria>
- DESIGN.md has a complete, clearly structured Light Mode Palette section with link color hierarchy
- All :root semantic tokens in shadcn-ui.css use oklch format (no stock Shadcn defaults)
- Sidebar tokens in :root are configured for a light sidebar with green accents
- Green link tokens have per-theme values (darker in light, brighter in dark) for COMP-07
- Shadow nullification exists in both :root and .dark
- The .dark block is untouched from Phase 1 except for added green link token overrides
- Sonner toast uses semantic tokens and renders correctly in both themes
- All sidebar nav links use font-weight 500
- Form inputs themed via token inheritance (COMP-02) — no component changes needed
</success_criteria>

<output>
After completion, create `.planning/phases/02-light-theme-component-theming/02-01-SUMMARY.md`
</output>
