---
phase: 01-foundation-dark-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/styles/global.css
  - app/styles/shadcn-ui.css
  - app/styles/theme.css
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-08
must_haves:
  truths:
    - "Body text renders in Geist Sans font family"
    - "Monospace elements render in Geist Mono font family"
    - "Dark mode shows Supabase neutral palette — off-white text on #171717 background, #2e2e2e borders"
    - "Green accent tokens are available as CSS custom properties"
    - "Sidebar primary accent uses Supabase green in dark mode"
  artifacts:
    - path: "app/styles/global.css"
      provides: "Font CSS imports before Tailwind"
      contains: "@fontsource-variable/geist"
    - path: "app/styles/shadcn-ui.css"
      provides: "Supabase dark palette tokens + font variables + green accent tokens + neutral scale"
      contains: "--supabase-green"
    - path: "app/styles/theme.css"
      provides: "Font mono mapping in @theme"
      contains: "--font-mono"
  key_links:
    - from: "app/styles/global.css"
      to: "@fontsource-variable/geist/wght.css"
      via: "CSS @import"
      pattern: "@import '@fontsource-variable/geist/wght.css'"
    - from: "app/styles/theme.css"
      to: "app/styles/shadcn-ui.css"
      via: "--font-sans and --font-mono var() references"
      pattern: "--font-sans: var\\(--font-sans\\)"
    - from: "app/styles/shadcn-ui.css .dark block"
      to: "all Shadcn components"
      via: "CSS variable consumption"
      pattern: "--sidebar-primary: oklch\\(73\\.5%"
---

<objective>
Install Geist fonts, override the full Shadcn dark palette with Supabase oklch values, define green accent tokens, and add the Supabase neutral scale as custom properties.

Purpose: Establish the complete CSS token foundation so dark mode renders with the Supabase palette. This is the core of Phase 1 — every visual change flows from these token overrides.
Output: Three modified CSS files with fonts installed, dark palette overridden, and green/neutral tokens defined.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-dark-theme/01-CONTEXT.md
@.planning/phases/01-foundation-dark-theme/01-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- Current file structures the executor will modify -->

From app/styles/global.css (lines 7-8 — import section):
```css
/* Tailwind CSS */
@import 'tailwindcss';
```
Font imports must go BEFORE this line.

From app/styles/shadcn-ui.css (line 11 — current font-sans):
```css
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
```
No --font-mono exists yet. Must be added.

From app/styles/theme.css (line 52 — font-sans in @theme):
```css
--font-sans: -apple-system, var(--font-sans);
```
The `-apple-system, ` prefix must be removed. `--font-mono` line must be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Geist fonts and wire font variables</name>
  <files>app/styles/global.css, app/styles/shadcn-ui.css, app/styles/theme.css</files>
  <read_first>
    - app/styles/global.css
    - app/styles/shadcn-ui.css
    - app/styles/theme.css
    - .planning/phases/01-foundation-dark-theme/01-RESEARCH.md (Pattern 1, Pattern 4, Pitfall 1, Pitfall 5)
  </read_first>
  <action>
1. Install font packages (per D-04):
```bash
pnpm add @fontsource-variable/geist @fontsource-variable/geist-mono
```

2. Edit `app/styles/global.css` — add two font imports BEFORE the `@import 'tailwindcss'` line (per D-05, Pitfall 4):
```css
@import '@fontsource-variable/geist/wght.css';
@import '@fontsource-variable/geist-mono/wght.css';

/* Tailwind CSS */
@import 'tailwindcss';
```
Keep all other lines unchanged.

3. Edit `app/styles/shadcn-ui.css` `:root` block — replace the existing `--font-sans` line (line 11) and add `--font-mono` and `--font-heading` (per D-05, D-06):
```css
--font-sans: 'Geist Variable', 'Helvetica Neue', Helvetica, Arial, sans-serif;
--font-mono: 'Geist Mono Variable', 'Source Code Pro', Menlo, monospace;
--font-heading: var(--font-sans);
```
The old value `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"` is fully replaced.

4. Edit `app/styles/theme.css` `@theme` block (per D-06, Pitfall 1, Pitfall 5):
- Line 52: change `--font-sans: -apple-system, var(--font-sans);` to `--font-sans: var(--font-sans);`
- Line 53: change `--font-heading: var(--font-heading);` stays as-is
- Add new line after font-heading: `--font-mono: var(--font-mono);`

The resulting font section in `@theme` should be:
```css
--font-sans: var(--font-sans);
--font-heading: var(--font-heading);
--font-mono: var(--font-mono);
```
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && grep -q "@fontsource-variable/geist/wght.css" app/styles/global.css && grep -q "@fontsource-variable/geist-mono/wght.css" app/styles/global.css && grep -q "'Geist Variable'" app/styles/shadcn-ui.css && grep -q "'Geist Mono Variable'" app/styles/shadcn-ui.css && grep -q "\-\-font-mono: var(--font-mono)" app/styles/theme.css && ! grep -q "\-apple-system" app/styles/theme.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/styles/global.css` contains `@import '@fontsource-variable/geist/wght.css'` BEFORE `@import 'tailwindcss'`
    - `app/styles/global.css` contains `@import '@fontsource-variable/geist-mono/wght.css'` BEFORE `@import 'tailwindcss'`
    - `app/styles/shadcn-ui.css` `:root` contains `--font-sans: 'Geist Variable', 'Helvetica Neue', Helvetica, Arial, sans-serif;`
    - `app/styles/shadcn-ui.css` `:root` contains `--font-mono: 'Geist Mono Variable', 'Source Code Pro', Menlo, monospace;`
    - `app/styles/theme.css` contains `--font-sans: var(--font-sans);` with NO `-apple-system` prefix anywhere in the file
    - `app/styles/theme.css` contains `--font-mono: var(--font-mono);`
    - `pnpm ls @fontsource-variable/geist` shows the package is installed
    - `pnpm ls @fontsource-variable/geist-mono` shows the package is installed
  </acceptance_criteria>
  <done>Geist and Geist Mono fonts are installed, imported in global.css before Tailwind, declared as --font-sans and --font-mono in shadcn-ui.css :root, and mapped through theme.css @theme with no -apple-system prefix interfering.</done>
</task>

<task type="auto">
  <name>Task 2: Override dark palette tokens, define green accents and neutral scale</name>
  <files>app/styles/shadcn-ui.css</files>
  <read_first>
    - app/styles/shadcn-ui.css
    - .planning/phases/01-foundation-dark-theme/01-RESEARCH.md (Complete Shadcn Dark Token Mapping section, Pattern 2, Pattern 3)
    - DESIGN.md
  </read_first>
  <action>
1. Edit `app/styles/shadcn-ui.css` `:root` block — ADD (do not replace existing light tokens) the following custom properties after the `--font-heading` line and before `--background` (per D-07, D-14):

```css
/* Supabase brand tokens (per D-07) */
--supabase-green: oklch(73.5% 0.158 162);
--supabase-green-link: oklch(71.2% 0.184 160);
--supabase-green-border: oklch(73.5% 0.158 162 / 0.3);

/* Supabase neutral scale (per D-14) */
--sb-near-black: oklch(7.84% 0 none);
--sb-dark: oklch(12.55% 0 none);
--sb-dark-border: oklch(18.84% 0 none);
--sb-border-dark: oklch(23.53% 0 none);
--sb-border-mid: oklch(27.45% 0 none);
--sb-border-light: oklch(28.63% 0 none);
--sb-charcoal: oklch(33.33% 0 none);
--sb-mid-gray: oklch(60% 0 none);
--sb-light-gray: oklch(74.9% 0 none);
--sb-near-white: oklch(94.51% 0 none);
```

Note: `--supabase-green-border` uses oklch with alpha channel syntax `oklch(L% C H / alpha)` instead of the rgba format in DESIGN.md — this is the oklch-native equivalent per D-01.

2. REPLACE the entire `.dark {}` block (lines 60-103) with the Supabase dark palette (per D-01, D-02, D-03, D-08, D-09, D-13):

```css
.dark {
    --background: oklch(12.55% 0 none);
    --foreground: oklch(98.04% 0 none);

    --card: oklch(12.55% 0 none);
    --card-foreground: oklch(98.04% 0 none);

    --popover: oklch(12.55% 0 none);
    --popover-foreground: oklch(98.04% 0 none);

    --primary: oklch(98.04% 0 none);
    --primary-foreground: oklch(12.55% 0 none);

    --secondary: oklch(23.53% 0 none);
    --secondary-foreground: oklch(98.04% 0 none);

    --muted: oklch(23.53% 0 none);
    --muted-foreground: oklch(60% 0 none);

    --accent: oklch(23.53% 0 none);
    --accent-foreground: oklch(98.04% 0 none);

    --destructive: var(--color-red-700);
    --destructive-foreground: var(--color-white);

    --border: oklch(23.53% 0 none);
    --input: oklch(27.45% 0 none);
    --ring: oklch(73.5% 0.158 162);

    --chart-1: var(--color-blue-600);
    --chart-2: var(--color-emerald-400);
    --chart-3: var(--color-orange-400);
    --chart-4: var(--color-purple-500);
    --chart-5: var(--color-pink-500);

    --sidebar-background: oklch(12.55% 0 none);
    --sidebar-foreground: oklch(60% 0 none);
    --sidebar-primary: oklch(73.5% 0.158 162);
    --sidebar-primary-foreground: oklch(12.55% 0 none);
    --sidebar-accent: oklch(23.53% 0 none);
    --sidebar-accent-foreground: oklch(98.04% 0 none);
    --sidebar-border: var(--border);
    --sidebar-ring: oklch(73.5% 0.158 162);
}
```

Key decisions reflected:
- `--primary` stays neutral off-white (per D-08) — NOT green
- `--sidebar-primary` uses Supabase green (per D-09)
- `--destructive` and `--destructive-foreground` keep Tailwind var() refs (per D-03)
- Chart tokens kept as-is (deferred to Phase 3)
- `--sidebar-foreground` set to mid-gray `oklch(60% 0 none)` for muted inactive nav items (per RESEARCH.md recommendation)
- All non-Tailwind-ref tokens use oklch format (per D-01)
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && grep -q "\-\-supabase-green: oklch(73.5% 0.158 162)" app/styles/shadcn-ui.css && grep -q "\-\-sb-dark: oklch(12.55% 0 none)" app/styles/shadcn-ui.css && grep -q "\-\-sidebar-primary: oklch(73.5% 0.158 162)" app/styles/shadcn-ui.css && grep -q "\-\-foreground: oklch(98.04% 0 none)" app/styles/shadcn-ui.css && grep -q "\-\-ring: oklch(73.5% 0.158 162)" app/styles/shadcn-ui.css && grep -q "\-\-destructive: var(--color-red-700)" app/styles/shadcn-ui.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `app/styles/shadcn-ui.css` `:root` contains `--supabase-green: oklch(73.5% 0.158 162)`
    - `app/styles/shadcn-ui.css` `:root` contains `--supabase-green-link: oklch(71.2% 0.184 160)`
    - `app/styles/shadcn-ui.css` `:root` contains `--supabase-green-border: oklch(73.5% 0.158 162 / 0.3)`
    - `app/styles/shadcn-ui.css` `:root` contains all 10 `--sb-*` neutral scale tokens (--sb-near-black through --sb-near-white)
    - `.dark` block `--background` is `oklch(12.55% 0 none)` (not `var(--color-neutral-900)`)
    - `.dark` block `--foreground` is `oklch(98.04% 0 none)` (not `var(--color-white)`)
    - `.dark` block `--border` is `oklch(23.53% 0 none)` (#2e2e2e equivalent)
    - `.dark` block `--primary` is `oklch(98.04% 0 none)` (neutral, NOT green — per D-08)
    - `.dark` block `--sidebar-primary` is `oklch(73.5% 0.158 162)` (green — per D-09)
    - `.dark` block `--destructive` is `var(--color-red-700)` (kept per D-03)
    - `.dark` block `--ring` is `oklch(73.5% 0.158 162)` (green focus ring)
    - `.dark` block `--sidebar-foreground` is `oklch(60% 0 none)` (muted nav text)
    - No `var(--color-neutral-*)` references remain in `.dark` block except for chart tokens and destructive
  </acceptance_criteria>
  <done>The .dark block in shadcn-ui.css contains the complete Supabase palette in oklch format. Green accent tokens and neutral scale custom properties are defined in :root. Primary stays neutral, sidebar uses green, destructive keeps Tailwind red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan modifies CSS files and installs fonts only — no user input, no network requests, no data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | npm packages | accept | @fontsource-variable packages are MIT-licensed, widely used, installed from npm registry — standard supply chain risk accepted |
</threat_model>

<verification>
1. `pnpm typecheck` passes (no type errors from font/CSS changes)
2. `pnpm build` succeeds (Vite bundles CSS with new imports)
3. All grep checks in task verify blocks pass
</verification>

<success_criteria>
- Geist and Geist Mono fonts are installed and wired through all three CSS layers
- The .dark block contains 100% Supabase oklch values (no legacy var(--color-neutral-*) except charts/destructive)
- Green accent tokens (--supabase-green, --supabase-green-link, --supabase-green-border) exist in :root
- Supabase neutral scale (10 --sb-* tokens) exists in :root
- No -apple-system prefix in theme.css
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-dark-theme/01-01-SUMMARY.md`
</output>
