---
quick_id: 260410-sl6
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/sidebar/module-sidebar-navigation.tsx
  - app/components/sidebar/workspace-sidebar.tsx
autonomous: true
requirements:
  - SL6-01
  - SL6-02
  - SL6-03
must_haves:
  truths:
    - "Module labels render in sentence case (not UPPERCASE)"
    - "Module rows visually match the prototype: rounded-xl pill, 18px icon, 15px font-medium label, green→emerald gradient pill when active"
    - "Sub-items render as text-only rows inside a green-200 left rail (no icons)"
    - "No separator divider is drawn between module rows"
    - "Sub-menu chevron sits inline at the end of the module row (not an absolute-positioned sibling button) and rotates 180deg when expanded"
    - "Expanded sidebar width stays 13.75rem (220px), collapsed stays 4.25rem (68px) — matching the prototype"
    - "Sidebar pulls modules and sub-modules from the existing workspace loader (AppNavModule / AppNavSubModule) — no data source change"
    - "NAVIGATION + MODULES section labels remain rendered above the module list (Phase 10 e2e contract — do not remove)"
  artifacts:
    - path: "app/components/sidebar/module-sidebar-navigation.tsx"
      provides: "Prototype-parity module row + sub-menu rendering"
      contains: "text-[15px] font-medium"
    - path: "app/components/sidebar/workspace-sidebar.tsx"
      provides: "Sidebar shell with section labels + footer matching prototype"
      contains: "text-[11px]"
  key_links:
    - from: "workspace-sidebar.tsx"
      to: "module-sidebar-navigation.tsx"
      via: "modules + subModules props from workspace loader"
      pattern: "ModuleSidebarNavigation"
    - from: "module-sidebar-navigation.tsx"
      to: "workspace loader data"
      via: "AppNavModule / AppNavSubModule types"
      pattern: "AppNavModule"
---

<objective>
Make the aloha-app workspace sidebar visually and structurally match the aloha-design
prototype sidebar (`/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/layout/Sidebar.tsx`),
as shown on http://localhost:5176/filtered-table.

Purpose: The prototype is the canonical design source (memory: reference_aloha_design_prototype).
Phase 10 shipped the green-gradient active pill and DESIGN.md tokens, but the current
sidebar drifted from the prototype in several concrete ways: module labels are UPPERCASE,
labels are missing the 15px/font-medium sizing, the chevron is an absolute-positioned
sibling button (creates a "pr-9" reserved gutter), sub-items render with icons the
prototype doesn't have, and SidebarSeparators are drawn between every module row.

Output: Two edited files, no new files, no new dependencies, no data-model changes.
Sidebar continues to be driven by the existing workspace loader (AppNavModule / AppNavSubModule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@./DESIGN.md
@.planning/STATE.md

# Reference prototype (read-only — DO NOT edit)
@/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/components/layout/Sidebar.tsx
@/Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype/src/data/roles.ts

# Files being edited
@app/components/sidebar/workspace-sidebar.tsx
@app/components/sidebar/module-sidebar-navigation.tsx

# Primitive (read-only — do not edit the shadcn primitive)
@packages/ui/src/shadcn/sidebar.tsx

<interfaces>
<!-- Workspace data shapes the sidebar consumes. Do NOT change these. -->

From app/lib/workspace/types.ts (already in use):
```ts
type AppNavModule = {
  module_id: string;
  module_slug: string;
  display_name: string;
  display_order: number;
  // ...
};

type AppNavSubModule = {
  sub_module_id: string;
  sub_module_slug: string;
  module_slug: string;
  display_name: string;
  display_order: number;
  // ...
};
```

Prototype NavItem shape (for reference only — NOT imported):
```ts
interface NavItem {
  id: string;
  label: string;
  icon: string;
  route: string;
  subItems?: { id: string; label: string }[];
  subParam?: string;
}
```

Mapping: prototype `NavItem.label` → `AppNavModule.display_name`,
prototype `subItems[].label` → `AppNavSubModule.display_name`,
prototype `route` → `/home/${account}/${module_slug}` (existing pattern).
</interfaces>
</context>

<gap_analysis>
Concrete differences between prototype Sidebar.tsx and current implementation:

| # | Aspect | Prototype | Current aloha-app | Action |
|---|--------|-----------|-------------------|--------|
| 1 | Module label case | sentence case | `uppercase` class | Remove `uppercase` |
| 2 | Module label typography | `text-[15px] font-medium` | shadcn default (14px) | Add `text-[15px] font-medium` |
| 3 | Module row padding | `py-2.5 px-3` | `px-3 py-2 pr-9` | `py-2.5 px-3`, drop `pr-9` |
| 4 | Module icon size | `size={18}` (18px) | `h-4 w-4` (16px) | Use `h-[18px] w-[18px]` |
| 5 | Chevron structure | inline child of button, `size={14}` | absolute sibling button `pr-9` gutter | Move chevron INSIDE SidebarMenuButton as last flex child |
| 6 | Chevron color when active | `text-white/70` | `text-white` | `text-white/70` |
| 7 | Separator between modules | NONE (`flex flex-col gap-0.5`) | `SidebarSeparator` every row | Remove `{index > 0 && <SidebarSeparator />}` |
| 8 | Module gap | `gap-0.5` | default | Wrap list with `gap-0.5` container |
| 9 | Sub-item icons | NONE (text only) | `SubModuleIcon` rendered | Remove sub-module icons |
| 10 | Sub-item typography | `text-sm px-2.5 py-1.5 rounded-lg` active `font-medium` | mostly matches but uses tokens | Match prototype classes exactly |
| 11 | Sub-item inactive color | `text-slate-500 hover:bg-slate-100 hover:text-slate-700` | `text-muted-foreground` | Keep tokens (design.md says tokens), but ensure visual parity |
| 12 | Sub-item rail gap | `gap-0.5` | `gap-1` | `gap-0.5` |
| 13 | NAVIGATION label size | `text-xs` (11-12px), `tracking-wider` | `text-xs font-semibold tracking-wider` | Keep (close enough; Phase 10 locked) |
| 14 | Footer button | `rounded-xl py-2.5 text-[15px] font-medium` | `text-xs py-2` | Match prototype sizing |
| 15 | Collapsed mode (icon) row | `px-0 justify-center` | shadcn handles | OK — keep shadcn behavior |

Items 13 is intentionally a no-op (Phase 10 e2e contract locked both NAVIGATION + MODULES
labels plus SidebarSeparator between them — DO NOT remove).

Items 1-12, 14 are in scope for this plan.
</gap_analysis>

<tasks>

<task type="auto">
  <name>Task 1: Restyle module rows + sub-items in module-sidebar-navigation.tsx to match prototype</name>
  <files>app/components/sidebar/module-sidebar-navigation.tsx</files>
  <action>
Edit `app/components/sidebar/module-sidebar-navigation.tsx` to match the prototype sidebar. Apply ALL of the following — read the task carefully, these are concrete class/structure edits:

1. **Remove the SidebarSeparator between modules.** Delete the line `{index > 0 && <SidebarSeparator className="mx-0" />}`. The prototype does NOT draw separators between module rows. Also remove the trailing `<SidebarSeparator className="mx-0" />` at the very end of the returned fragment. Drop the `SidebarSeparator` import if it becomes unused.

2. **Remove `uppercase` from module labels** in BOTH the expanded and collapsed branches. The prototype renders module names in sentence case (`display_name` as-is).

3. **Module row classes (expanded branch SidebarMenuButton):** change the current
   ```
   'rounded-xl px-3 py-2 pr-9 uppercase'
   ```
   to
   ```
   'rounded-xl px-3 py-2.5 gap-3 text-[15px] font-medium'
   ```
   Keep the existing active-state gradient classes (`bg-gradient-to-r from-green-500 to-emerald-600 text-white shadow-lg shadow-green-500/25 dark:from-green-400 dark:to-emerald-500 dark:text-green-950`). Keep inactive: `text-foreground hover:bg-muted bg-transparent`.

4. **Module icon size:** change `className: 'h-4 w-4 shrink-0'` to `className: 'h-[18px] w-[18px] shrink-0'` in BOTH the expanded AND collapsed branches (the collapsed-icon branch should keep 18px too — the prototype uses `size={18}` in both modes).

5. **Move the chevron INSIDE the SidebarMenuButton** as the last flex child (the prototype renders the chevron inline, NOT as an absolute-positioned sibling button):
   - Delete the entire `<CollapsibleTrigger asChild><button className="absolute top-1/2 right-2 ...">...</button></CollapsibleTrigger>` block and the `relative` class on the wrapping `SidebarMenuItem`.
   - Wrap the module row in `<CollapsibleTrigger asChild>` so clicking the whole row toggles expansion BUT the `<Link>` inside `asChild` still navigates. Since both nav + toggle fire on the same click, preserve the existing UX: clicking a module link navigates to the module AND toggles its sub-items (matching the prototype behavior at Sidebar.tsx lines 34-44).
   - Approach: DO NOT wrap SidebarMenuButton in CollapsibleTrigger. Instead, leave `SidebarMenuButton asChild` with the `<Link>`, and place the chevron inside the Link as the last child after the label `<span>`. The chevron should be a plain `<svg>` (or lucide `ChevronDown`) with classes `h-3.5 w-3.5 shrink-0 transition-transform duration-200` and `isOpen && 'rotate-180'`. Color: `isModuleActive ? 'text-white/70 dark:text-green-950/70' : 'text-muted-foreground'`.
   - BUT: the prototype's chevron ALSO acts as the toggle. Because the whole row already toggles on click via the existing `toggleModule` logic, we need to keep that wiring. Replace the current dual-child pattern with this single click path:
     * Remove the CollapsibleTrigger wrapper entirely.
     * Change the Collapsible to controlled-only (`open={isOpen}`, no `onOpenChange` trigger child).
     * Add `onClick={() => { onNavigate?.(); toggleModule(mod.module_slug); }}` to the `<Link>` so navigating a module also toggles its sub-items (matches prototype Sidebar.tsx `onClick` at lines 35-44).
     * Render the chevron only when `children.length > 0` as a trailing `<ChevronDown>` inside the Link, AFTER the label span.

6. **Sub-item rail wrapper:** change `gap-1` to `gap-0.5` on the `<div data-sidebar="menu-sub">` wrapper. Keep all other classes (the `ml-5 pl-3 border-l-2 border-green-200 dark:border-green-900/60` rail).

7. **Sub-item rows — remove icons.** In the `children.map((sm, subIndex) => ...)` loop:
   - DELETE the `const SubModuleIcon = getSubModuleIcon(sm.sub_module_slug);` line.
   - DELETE the `createElement(SubModuleIcon, { className: 'h-4 w-4 shrink-0' })` call inside the Link.
   - The Link should contain ONLY the `<span className="truncate capitalize">{sm.display_name}</span>`. The prototype sub-items are text-only.
   - Remove the `capitalize` class — prototype renders sub labels as-provided. Just `<span className="truncate">`.

8. **Sub-item typography:** change the active/inactive classes on the sub SidebarMenuButton to match prototype exactly:
   - Active: `'rounded-lg bg-green-50 px-2.5 py-1.5 text-sm font-medium text-green-700 dark:bg-green-900/40 dark:text-green-200'`
   - Inactive: `'rounded-lg bg-transparent px-2.5 py-1.5 text-sm text-muted-foreground hover:bg-muted hover:text-foreground'`

9. **Collapsed (icon-mode) branch:** Also update the collapsed-branch sub-items inside the popover — remove `SubModuleIcon` usage and render text-only (match the prototype which has no sub-items in collapsed mode, but our collapsed popover should still work; remove icons to stay consistent). If the collapsed-branch sub-items already look acceptable with icons and you determine the current behavior is correct for icon-mode UX, leave them as-is and note it in the done criteria. Preference: remove icons for consistency.

10. **Imports cleanup:** remove `getSubModuleIcon` from the `module-icons.config` import if no longer used. Remove `SidebarSeparator` import if no longer used. Add `ChevronDown` import from `lucide-react` if used for the inline chevron (replacing the inline SVG).

Do NOT:
- Change the data source. Continue reading `modules` and `subModules` from props.
- Remove the NAVIGATION/MODULES section labels in workspace-sidebar.tsx (Phase 10 e2e contract).
- Remove the active-state gradient or dark-mode variants (Phase 10 locked).
- Modify `packages/ui/src/shadcn/sidebar.tsx`.
- Introduce framer-motion (prototype uses it but DESIGN.md constraints say no new libraries for this project; we use plain CSS transitions).
- Add useEffect.

Run `pnpm typecheck` after editing to confirm no type errors.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- `module-sidebar-navigation.tsx` has NO `uppercase` class on the module row
- Module row SidebarMenuButton uses `text-[15px] font-medium gap-3 py-2.5 px-3 rounded-xl` (no `pr-9`)
- Module icon renders at 18px (`h-[18px] w-[18px]`)
- Chevron is INSIDE the Link (inline flex child), not an absolute sibling button
- NO `SidebarSeparator` rendered between module rows or at the list end
- Sub-items render text-only (no `SubModuleIcon` import used)
- Sub-item rail uses `gap-0.5`
- `pnpm typecheck` passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Align workspace-sidebar.tsx section labels + footer to prototype sizing</name>
  <files>app/components/sidebar/workspace-sidebar.tsx</files>
  <action>
Edit `app/components/sidebar/workspace-sidebar.tsx` to tighten visual parity with the prototype section-header + footer styling. Keep all Phase 10 locked decisions (both NAVIGATION and MODULES labels must remain rendered; SidebarEdgeToggle stays; avatar row stays).

1. **Section label typography — match prototype `text-[11px]` exactly.** Change both `SidebarGroupLabel` className from `text-xs` to `text-[11px]`. The prototype uses `text-[11px] font-medium text-slate-400 uppercase tracking-wider`. Keep `font-semibold` or change to `font-medium` — prefer `font-medium` to match prototype. Keep `tracking-wider uppercase px-3` and the `group-data-[collapsible=icon]:hidden` modifier.

2. **Module list gap wrapper.** The current implementation renders `<ModuleSidebarNavigation />` directly inside SidebarContent — each module row becomes a top-level block with its own SidebarGroup. Wrap the ModuleSidebarNavigation call in a `<div className="flex flex-col gap-0.5 px-3">` to give the module list the prototype's tight `gap-0.5` rhythm AND the prototype's `px-3` horizontal padding. Verify the wrapper does not break the SidebarGroup / SidebarMenu semantics required by the shadcn primitive — if it does, instead add a className with `px-3` to the existing SidebarContent wrapping div without breaking the collapsed-icon mode. Safer alternative: add `className="gap-0.5"` semantics by adjusting `ModuleSidebarNavigation`'s top-level wrapper in Task 1 instead. Pick whichever keeps collapsed-icon mode visually stable.

3. **Footer button typography.** Update the disabled "Focused" placeholder button classes from
   ```
   'text-muted-foreground flex w-full cursor-not-allowed items-center gap-2 px-3 py-2 text-xs font-medium opacity-60'
   ```
   to
   ```
   'text-muted-foreground flex w-full cursor-not-allowed items-center gap-3 px-3 py-2.5 text-[15px] font-medium rounded-xl opacity-60'
   ```
   This matches the prototype's `py-2.5 rounded-xl text-[15px] font-medium gap-3` footer button (prototype Sidebar.tsx lines 154-165). Keep the LayoutGrid + label + ChevronLeft children and the `aria-disabled` / `tabIndex={-1}` attrs.

4. **Footer avatar row.** The prototype's footer does NOT contain an avatar row (the prototype avatar lives in the top Header). However, Phase 10 PARITY-03 locked the sidebar-footer avatar as a deviation for the e2e contract — KEEP IT. Do not remove the avatar row. Only update spacing to match the footer button rhythm: change `px-3 py-2` to `px-3 py-2.5` and `gap-2` to `gap-3`. Keep `data-test="workspace-sidebar-profile"`.

5. **Sidebar border.** The prototype uses `border-r border-slate-200` (slate-200 is `--border` already). The current Sidebar uses `border-border border-r` — this is correct via tokens. Leave as-is.

6. **Do NOT** change the `md:top-[72px] md:h-[calc(100svh-72px)]` offset — the aloha-app has a separate sticky navbar above the sidebar (structurally different from the prototype's flex-column AppLayout). This is intentional and correct.

7. **Do NOT** remove the NAVIGATION label, the SidebarSeparator between NAVIGATION and MODULES labels, or the MODULES label. Phase 10 e2e contract.

Run `pnpm typecheck` and visually inspect classes for typos.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- Both SidebarGroupLabel elements use `text-[11px]` (not `text-xs`)
- Footer disabled button uses `py-2.5 text-[15px] font-medium gap-3 rounded-xl`
- Avatar row spacing updated to `py-2.5 gap-3`
- NAVIGATION + MODULES labels and the separator between them STILL render (Phase 10 contract)
- Top offset `md:top-[72px] md:h-[calc(100svh-72px)]` is unchanged
- `pnpm typecheck` passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual parity check — sidebar vs prototype screenshot</name>
  <what-built>
Sidebar restyled to match the aloha-design prototype:
- Module rows: sentence case, 15px medium, 18px icon, py-2.5 rounded-xl pill
- Active module: green→emerald gradient pill with white text + shadow
- Chevron inline at end of row, rotates on expand
- Sub-items: text-only, no icons, green-50 active pill, green-200 left rail
- No separators between module rows
- Footer disabled "Focused" button styled like prototype (py-2.5 rounded-xl)
  </what-built>
  <how-to-verify>
1. Start dev servers in parallel:
   - Aloha app: `pnpm dev` (http://localhost:5173)
   - Prototype:  cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-design/prototype && pnpm dev (should start on 5176)
2. Open prototype at http://localhost:5176/filtered-table (select "Manager" or "Owner" role from the top toolbar so the full sidebar is visible).
3. Open aloha-app at http://localhost:5173, sign in, navigate to any module (e.g. HR > Employees).
4. Side-by-side compare:
   - [ ] Module row height, padding, font weight, font size appear identical
   - [ ] Active module pill: green→emerald gradient, white label, soft green shadow
   - [ ] Module icons are 18px (not 16px — slightly larger than before)
   - [ ] Chevron sits at the END of the module row (not in a right gutter), rotates when expanded
   - [ ] Sub-items are TEXT ONLY (no icons) inside a thin green-200 vertical rail
   - [ ] Active sub-item has a soft green-50 background + green-700 text
   - [ ] NO horizontal divider line between module rows
   - [ ] Section labels "NAVIGATION" and "MODULES" still render (Phase 10 contract)
   - [ ] Footer "Focused" placeholder button matches prototype sizing
5. Toggle dark mode (top-right toggle) — verify:
   - [ ] Dark mode still respects DESIGN.md tokens (slate-900 canvas, slate-800 card, etc.)
   - [ ] Active gradient brightens to green-400 → emerald-500 in dark mode
   - [ ] Active sub-item uses `bg-green-900/40 text-green-200`
6. Click a module with sub-items — chevron should rotate 180deg and sub-rail should appear.
7. Click the navbar PanelLeft toggle to collapse the sidebar — verify icon-only mode still works and icons are centered.
  </how-to-verify>
  <resume-signal>Type "approved" or list specific visual deviations still present</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes cleanly
- `pnpm lint:fix` and `pnpm format:fix` run without complaints (run manually before commit)
- Visual parity checkpoint (Task 3) approved by user
</verification>

<success_criteria>
- Module rows visually match the prototype (sentence case, 15px medium, 18px icons, inline chevron, no uppercase, no absolute chevron gutter)
- Sub-items are text-only with the green-200 rail and green-50 active pill
- No SidebarSeparator between module rows
- Footer button + avatar row sizing matches prototype rhythm
- Phase 10 locked items preserved: NAVIGATION + MODULES section labels, SidebarEdgeToggle, avatar footer row, dark-mode gradient/sub-item variants
- Data source unchanged — sidebar still driven by `AppNavModule` + `AppNavSubModule` from workspace loader
- No new dependencies, no new files, no changes to `packages/ui/src/shadcn/sidebar.tsx`
</success_criteria>

<output>
After completion, if the user wants a commit, stage and commit:
- `app/components/sidebar/module-sidebar-navigation.tsx`
- `app/components/sidebar/workspace-sidebar.tsx`

Suggested commit message:
`style(sidebar): match aloha-design prototype — inline chevron, 15px labels, text-only sub-items, drop uppercase`
</output>
