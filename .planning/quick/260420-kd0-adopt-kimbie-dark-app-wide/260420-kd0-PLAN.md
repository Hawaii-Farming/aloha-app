---
phase: quick-260420-kd0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/styles/shadcn-ui.css
  - DESIGN.md
  - UI-RULES.md
autonomous: true
requirements:
  - KD0-01  # Swap .dark CSS vars to Kimbie Dark palette
  - KD0-02  # Preserve Aloha brand tokens (--primary, --primary-foreground, --ring, --gradient-primary) and keep :root byte-identical
  - KD0-03  # Align DESIGN.md §1 / §2 / §9 dark sections with the new palette
  - KD0-04  # Spot-check UI-RULES.md for cool/slate phrasing in dark-mode context
must_haves:
  truths:
    - "Toggling to dark mode renders warm Kimbie earth surfaces (background #15100a, cards #1e1710) instead of cool slate-900"
    - "Toggling to light mode is visually identical before and after (byte-identical :root block)"
    - "Primary CTAs, focus rings, and brand gradient still render as Aloha emerald on Kimbie surfaces"
    - "DESIGN.md §2 dark foundation tokens table matches app/styles/shadcn-ui.css .dark block value-for-value"
    - "DESIGN.md §1 and §8 no longer describe dark mode as 'slate-900/800/700' — new phrasing describes Kimbie warm earth tones"
    - "DESIGN.md §9 WCAG table has recomputed ratios for every pair that uses a dark token; any new failures are entered in §9.1 with a proposed remediation"
  artifacts:
    - path: app/styles/shadcn-ui.css
      provides: "Kimbie Dark .dark token block (warm bg/surface/border, preserved emerald brand)"
      contains: "--background: #15100a"
    - path: DESIGN.md
      provides: "Rewritten §1 dark-mode paragraph, §2 dark foundation tokens table, §2 dark semantic triples, §8 derivation table, §9 WCAG pairs for dark"
      contains: "Kimbie"
    - path: UI-RULES.md
      provides: "Spot-checked prose — any cool/slate references in dark-mode contexts either updated or explicitly documented as unchanged"
      contains: "UI Rules"
  key_links:
    - from: "app/styles/shadcn-ui.css .dark block"
      to: "DESIGN.md §2 Dark mode foundation tokens table"
      via: "value-for-value token match"
      pattern: "--background: #15100a"
    - from: "app/styles/shadcn-ui.css .dark --primary / --primary-foreground / --ring / --gradient-primary"
      to: "current values on main (unchanged brand identity)"
      via: "byte-identical preservation of the four brand tokens"
      pattern: "--primary: #4ade80"
    - from: "app/styles/shadcn-ui.css :root (light) block"
      to: "current values on main (untouched — HARD REQUIREMENT)"
      via: "byte-identical preservation of lines 11-79"
      pattern: "--background: #f1f5f9"
---

<objective>
Adopt the Kimbie Dark palette app-wide for dark mode. Swap the warm earth-tone token values from `.planning/sketches/themes/kimbie-dark.css` into the live `.dark` block in `app/styles/shadcn-ui.css`, then align `DESIGN.md` (the token source of truth) and spot-check `UI-RULES.md` (the behavior companion) to match.

Purpose: The v2.0 Aloha Design System Retheme shipped with a slate-based dark mode. The sketch lab has vetted a Kimbie Dark warm earth-tone variant that reads better and feels more distinct from generic Shadcn dark themes. This quick task promotes that sketch to production without altering light mode, without breaking Shadcn primitives, and without disturbing the Aloha emerald brand moment.

Output:
- Rewritten `.dark` block in `app/styles/shadcn-ui.css` using canonical Kimbie values
- Rewritten dark-mode sections of `DESIGN.md` (§1 atmosphere, §2 foundation + semantic triples, §8 derivation rules, §9 WCAG pairs)
- Spot-checked `UI-RULES.md` — updated or confirmed unchanged

Hard constraints:
- Light mode (`:root` block, lines 11-79 of `shadcn-ui.css`) MUST be byte-identical before and after
- `--primary` (`#4ade80`), `--primary-foreground` (`#052e16`), `--ring` (`#4ade80`), and `--gradient-primary` MUST retain their current values
- Each task is committed atomically per gsd-executor convention
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@DESIGN.md
@UI-RULES.md
@app/styles/shadcn-ui.css
@.planning/sketches/themes/kimbie-dark.css

<!-- Reference only (do not modify): -->
<!-- app/styles/theme.css — forwards vars, no palette values -->
<!-- app/components/ag-grid/ag-grid-theme.ts — OUT OF SCOPE (TS file, see <scope_notes>) -->
</context>

<scope_notes>
### In scope
- `app/styles/shadcn-ui.css` — `.dark` block only
- `DESIGN.md` — all dark-mode prose, tables, and WCAG pairs
- `UI-RULES.md` — spot-check only

### Explicitly out of scope (for this plan)
1. **AG Grid theme TS file (`app/components/ag-grid/ag-grid-theme.ts`) and its test (`app/components/ag-grid/__tests__/ag-grid-theme.test.ts`).** Per `UI-RULES.md §Tables > Table theme`, AG Grid v35's `themeQuartz.withParams()` does not resolve CSS vars, so the dark AG Grid hex values (`#1e293b`, `#0f172a`, `#334155`, `#f8fafc`, `#cbd5e1`, `#4ade80`, `#334155`) are hardcoded in TS and asserted by a Vitest assertion. Changing them is a TS edit + test update, which this plan's constraint ("token-only changes + doc rewrites. No TS, no JSX") explicitly forbids.

   **User-visible consequence:** AG Grid dark tables will continue to render slate-900/800/700 chrome while the rest of the app renders Kimbie warm earth. This is a known, documented mismatch. Task 2 records this in `DESIGN.md §2` and Task 3 records it in `UI-RULES.md §Tables > Table theme`. Executor MUST NOT silently expand scope to the AG Grid TS file.

   **Follow-up:** a separate quick task (proposed slug: `kd0-aggrid-dark-sync`) should swap the AG Grid TS hex values + update the test assertion. Not this plan.

2. **`supabase/` SQL and `ops_task_weekly_schedule` tz bug (#13).** That fix touches SQL views + a React renderer — no CSS overlap. No conflict, no coordination needed.

3. **Light mode (`:root` block).** HARD REQUIREMENT per task input: byte-identical before and after.
</scope_notes>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite .dark block in app/styles/shadcn-ui.css with Kimbie palette</name>
  <files>app/styles/shadcn-ui.css</files>
  <action>
Rewrite ONLY lines 81-142 (the `.dark { ... }` block) of `app/styles/shadcn-ui.css` using the canonical Kimbie values below. Do NOT touch lines 1-80 (:root / light mode) or line 143 (closing brace of `@layer base`). The :root block MUST be byte-identical before and after — verify with `git diff --stat app/styles/shadcn-ui.css` showing only insertions/deletions inside the .dark block.

Preserve these four tokens EXACTLY as currently set (they are already Kimbie-compatible and locked as brand identity):
  --primary: #4ade80;
  --primary-foreground: #052e16;
  --ring: #4ade80;
  --gradient-primary: linear-gradient(135deg, #4ade80, #10b981);

Replace all other `.dark` token values with these Kimbie values (sourced verbatim from the palette table in the planning input, cross-checked against `.planning/sketches/themes/kimbie-dark.css` .dark block):

```css
.dark {
  --background: #15100a;
  --foreground: #d3af86;

  --card: #1e1710;
  --card-foreground: #d3af86;

  --popover: #1e1710;
  --popover-foreground: #d3af86;

  --primary: #4ade80;           /* PRESERVED — Aloha brand green-400 */
  --primary-foreground: #052e16; /* PRESERVED — green-950 */

  --secondary: #332618;
  --secondary-foreground: #d3af86;

  --muted: #1e1710;
  --muted-foreground: #a57a4c;

  --accent: #332618;
  --accent-foreground: #d3af86;

  --destructive: #dc3958;
  --destructive-foreground: #ffffff;

  --border: #332618;
  --input: #4a3620;
  --ring: #4ade80;              /* PRESERVED — focus ring stays emerald */

  --gradient-primary: linear-gradient(135deg, #4ade80, #10b981); /* PRESERVED */

  --chart-1: #4ade80;
  --chart-2: #10b981;
  --chart-3: #51bdb2;            /* Kimbie teal — replaces slate-blue #60a5fa */
  --chart-4: #f79a32;            /* Kimbie yellow-orange — replaces amber #fbbf24 */
  --chart-5: #d3af86;            /* Kimbie cream — replaces purple #c084fc */

  --semantic-red-bg: rgb(220 57 88 / 0.15);
  --semantic-red-fg: #f87171;    /* keep brighter red-400 for foreground legibility on warm bg */
  --semantic-red-border: rgb(220 57 88 / 0.30);

  --semantic-amber-bg: rgb(247 154 50 / 0.15);
  --semantic-amber-fg: #f79a32;
  --semantic-amber-border: rgb(247 154 50 / 0.30);

  --semantic-green-bg: rgb(136 155 74 / 0.18);
  --semantic-green-fg: #a7c05a;  /* brightened Kimbie olive #889b4a for legibility */
  --semantic-green-border: rgb(136 155 74 / 0.35);

  --semantic-blue-bg: rgb(81 189 178 / 0.14);
  --semantic-blue-fg: #51bdb2;
  --semantic-blue-border: rgb(81 189 178 / 0.30);

  --sidebar-background: #15100a;
  --sidebar-foreground: #a57a4c;
  --sidebar-primary: #4ade80;
  --sidebar-primary-foreground: #052e16;
  --sidebar-accent: #332618;     /* Kimbie warm border tone — cohesive with --secondary / --border */
  --sidebar-accent-foreground: #d3af86;
  --sidebar-border: #1e1710;     /* matches --card so sidebar edge reads as subtle lift, same pattern as previous slate-800 edge */
  --sidebar-ring: #4ade80;
}
```

Notes for the executor:
- The `--chart-*` tokens are not called out in the planning input palette table, but they are dark-mode tokens and MUST be repalettized to stay coherent. Chosen values use Kimbie's own accent hues (teal, yellow-orange, cream) so chart surfaces feel native to the warm dark canvas instead of snapping back to slate-cool.
- The `--semantic-red-fg` stays at `#f87171` (Tailwind red-400) rather than the Kimbie `#dc3958` / option-to-brighten, because `#dc3958` on `rgb(220 57 88 / 0.15)` self-wash is low contrast; the red-400 fg preserves legibility. This is a deliberate Kimbie-adjacent choice noted in the DESIGN.md update (Task 2) and the executor SUMMARY.
- The `--semantic-green-fg` uses `#a7c05a` (brightened Kimbie olive) — the planner-provided range allowed `#889b4a` or `#a7c05a`, and the brighter tone is chosen for WCAG legibility on the warm canvas.
- The `--sidebar-accent` uses `#332618` (matches --border / --secondary / --accent) rather than a warm orange wash. Rationale: consistency with the rest of the .dark block's muted earth tones; if the warm wash `rgba(247,154,50,0.10)` is desired later, it's a one-line tweak and a separate visual-polish task.

After edit:
1. Save file
2. Run `pnpm typecheck` (expected: no new errors — this is a CSS-only change)
3. Run `pnpm format:fix` to ensure Prettier/stylelint alignment on the block
4. Commit atomically: `refactor(quick-260420-kd0): swap .dark tokens to Kimbie Dark palette`
  </action>
  <verify>
    <automated>git diff app/styles/shadcn-ui.css | grep -E '^[+-]' | grep -vE '^[+-]{3}' | awk '/^[+-]/ &amp;&amp; NR&lt;=162' &gt; /dev/null &amp;&amp; grep -q '    --background: #15100a;' app/styles/shadcn-ui.css</automated>
  </verify>
  <done>
- `.dark` block in `app/styles/shadcn-ui.css` contains `--background: #15100a;`, `--foreground: #d3af86;`, `--card: #1e1710;`, `--border: #332618;`, `--sidebar-background: #15100a;`
- `--primary: #4ade80;`, `--primary-foreground: #052e16;`, `--ring: #4ade80;`, and `--gradient-primary: linear-gradient(135deg, #4ade80, #10b981);` are UNCHANGED from prior commit (verifiable via `git log -p app/styles/shadcn-ui.css` on the four lines)
- `:root` block (lines 11-79) is byte-identical to prior commit (verifiable via `git diff HEAD~1 -- app/styles/shadcn-ui.css` showing zero changes inside `:root`)
- `pnpm typecheck` passes
- Atomic commit created
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dark-mode sections of DESIGN.md to describe Kimbie</name>
  <files>DESIGN.md</files>
  <action>
Update the following sections of `DESIGN.md` to reflect the Kimbie Dark palette. Each edit below is scoped and the line numbers reference the current file on disk (as of the read snapshot).

**A. §1 Visual Theme & Atmosphere — dark-mode paragraph (line 11)**

Current:
> Dark mode is **hand-authored**, not algorithmically derived. Backgrounds drop to slate-900 (`#0f172a`) with slate-800 cards and slate-700 borders; the green primary brightens to green-400 (`#4ade80`) to preserve brand vividness on dark surfaces. Every foundation token pair is designed to meet WCAG AA (4.5:1 normal text, 3:1 large text / UI components) in both themes.

Rewrite to:
> Dark mode is **hand-authored** against the Kimbie Dark palette — warm earth tones inspired by the Kimbie Dark VS Code theme (Heiko Cavelier). Backgrounds drop to a deep warm brown (`#15100a`) with slightly lifted card surfaces (`#1e1710`) and muted cocoa borders (`#332618`); foreground text is a warm cream (`#d3af86`) and muted copy a softer tan (`#a57a4c`). The Aloha brand moment is preserved intentionally: `--primary`, `--primary-foreground`, `--ring`, and `--gradient-primary` stay at their emerald values (`#4ade80` / green-950 / gradient `#4ade80 → #10b981`) so the brand CTA still reads as Aloha green on the Kimbie warm canvas. Every foundation token pair is re-verified against WCAG AA (4.5:1 normal text, 3:1 UI components); any new miss is documented in §9.1.

Also update the bullet at line 19:
- Before: "Hand-authored dark palette (slate-900/800/700, green-400/emerald-500) with WCAG AA verified pairs"
- After: "Hand-authored Kimbie Dark palette (warm `#15100a` / `#1e1710` / `#332618` earth tones, preserved emerald `#4ade80` brand) with WCAG AA re-verified pairs — see §9"

**B. §2 Neutrals — add a Kimbie note (after line 56)**

Append to the "Neutrals — Slate scale" subsection a short paragraph:
> **Dark-mode exception:** As of the Kimbie Dark adoption (quick-260420-kd0), the `.dark` foundation tokens use a Kimbie warm-earth palette instead of the slate scale. The slate hex values in the table above apply to `:root` (light mode) only. See the "Dark mode foundation tokens" table below for the Kimbie values.

**C. §2 Semantic colors (bg/fg/border triples) — rewrite the four dark-mode rows (lines 62-67)**

Replace the dark-mode columns of each row with these Kimbie values. Light-mode columns are UNCHANGED.

| Semantic | Dark bg | Dark fg | Dark border |
|----------|---------|---------|-------------|
| red      | `rgb(220 57 88 / 0.15)` | `#f87171` | `rgb(220 57 88 / 0.30)` |
| amber    | `rgb(247 154 50 / 0.15)` | `#f79a32` | `rgb(247 154 50 / 0.30)` |
| green    | `rgb(136 155 74 / 0.18)` | `#a7c05a` | `rgb(136 155 74 / 0.35)` |
| blue     | `rgb(81 189 178 / 0.14)` | `#51bdb2` | `rgb(81 189 178 / 0.30)` |

Update the paragraph on line 60 from "Dark values use 15% / 30% alpha washes of the 500-level color plus a brightened 400-level foreground." to:
> Dark values use 14-18% alpha washes of the Kimbie accent hue plus a brightened foreground. The red foreground is held at Tailwind red-400 (`#f87171`) instead of Kimbie's own `#dc3958` because `#dc3958` on its self-wash is low-contrast; all others use the Kimbie accent directly. The green foreground brightens Kimbie olive (`#889b4a → #a7c05a`) for legibility on the warm canvas.

**D. §2 Dark mode foundation tokens table (lines 105-136) — full rewrite of the Value column**

Replace every row's Value and Notes to match the new `.dark` block from Task 1:

| Token | Value | Notes |
|-------|-------|-------|
| `--background` | `#15100a` | Kimbie deep warm brown — canonical page canvas |
| `--foreground` | `#d3af86` | Kimbie cream body text |
| `--card` | `#1e1710` | Slightly lifted warm surface |
| `--card-foreground` | `#d3af86` | Kimbie cream |
| `--popover` | `#1e1710` | Matches `--card` |
| `--popover-foreground` | `#d3af86` | Kimbie cream |
| `--primary` | `#4ade80` | green-400 — **PRESERVED** brand emerald |
| `--primary-foreground` | `#052e16` | green-950 — **PRESERVED** |
| `--secondary` | `#332618` | Kimbie muted cocoa — elevated secondary surface |
| `--secondary-foreground` | `#d3af86` | Kimbie cream |
| `--muted` | `#1e1710` | Matches `--card` |
| `--muted-foreground` | `#a57a4c` | Kimbie tan — secondary copy |
| `--accent` | `#332618` | Kimbie muted cocoa |
| `--accent-foreground` | `#d3af86` | Kimbie cream |
| `--destructive` | `#dc3958` | Kimbie red |
| `--destructive-foreground` | `#ffffff` | White |
| `--border` | `#332618` | Kimbie muted cocoa hairline |
| `--input` | `#4a3620` | Kimbie border-strong — input outline |
| `--ring` | `#4ade80` | green-400 — **PRESERVED** focus ring |
| `--gradient-primary` | `linear-gradient(135deg, #4ade80, #10b981)` | **PRESERVED** brand gradient |
| `--sidebar-background` | `#15100a` | Kimbie deep warm brown — cohesive with canvas |
| `--sidebar-foreground` | `#a57a4c` | Kimbie tan |
| `--sidebar-primary` | `#4ade80` | green-400 — **PRESERVED** |
| `--sidebar-primary-foreground` | `#052e16` | green-950 — **PRESERVED** |
| `--sidebar-accent` | `#332618` | Kimbie muted cocoa — hover/active wash |
| `--sidebar-accent-foreground` | `#d3af86` | Kimbie cream |
| `--sidebar-border` | `#1e1710` | Matches `--card` — subtle lift edge |
| `--sidebar-ring` | `#4ade80` | green-400 — **PRESERVED** |

**E. §2 Chart token note (if present) / otherwise append after the dark foundation table**

Append a short subsection:
> **Chart tokens (dark mode):** `--chart-1: #4ade80` (emerald), `--chart-2: #10b981` (emerald-500, gradient end), `--chart-3: #51bdb2` (Kimbie teal), `--chart-4: #f79a32` (Kimbie yellow-orange), `--chart-5: #d3af86` (Kimbie cream). Repalettized so chart surfaces stay native to the Kimbie canvas instead of snapping back to the slate-cool defaults. Light-mode chart tokens are unchanged.

**F. §8 Dark Mode — derivation rules table (lines 225-238)**

Rewrite the table rows that reference slate values:

| `:root` role | `.dark` adaptation |
|--------------|--------------------|
| Backgrounds (slate-50/100 canvas, white cards) | Kimbie deep warm brown (`#15100a`) canvas, warm cocoa cards (`#1e1710`) |
| Body text (slate-900) | Kimbie cream (`#d3af86`) |
| Muted text (slate-500) | Kimbie tan (`#a57a4c`) |
| Primary (green-500 solid) | **PRESERVED** green-400 `#4ade80` — brand identity held across palette swap |
| Primary gradient (`#22c55e → #059669`) | **PRESERVED** `#4ade80 → #10b981` |
| Primary foreground (white) | **PRESERVED** green-950 (`#052e16`) |
| Borders (slate-200/300) | Kimbie muted cocoa `#332618`, border-strong `#4a3620` |
| Sidebar surfaces (white, green-50 accent) | Kimbie canvas `#15100a`, muted cocoa accent `#332618`, cream accent-fg `#d3af86` |
| Destructive (red-600) | Kimbie red `#dc3958` |
| Semantic bg tiles (e.g. red-50) | 14-18% alpha washes of Kimbie accent hues (see §2 Semantic colors) |

Also update the preceding sentence:
- Before: "Dark is **hand-authored**, not runtime-derived. Derivation rules applied when writing the `.dark` block:"
- After: "Dark is **hand-authored** against the Kimbie Dark palette, not runtime-derived. Derivation rules applied when writing the `.dark` block (emerald brand tokens are explicitly preserved — see the **PRESERVED** rows below):"

**G. §9 WCAG AA Verification — recompute dark pairs (lines 241-300)**

Recompute and update every row in the WCAG table whose theme is `dark` (lines 261-272 range). Keep the light rows UNCHANGED.

Use these measured ratios (computed against the new Kimbie .dark block — executor should re-run `node scripts/verify-wcag.mjs` if that script exists, otherwise compute inline via standard WCAG 2.x relative-luminance formula):

Spot-check values the planner already computed (executor should verify, not trust):
- `#d3af86` on `#15100a` — cream on deep brown — ~9.5:1 (PASS for 4.5:1)
- `#a57a4c` on `#15100a` — tan on deep brown — ~4.8:1 (PASS for 4.5:1, TIGHT — note as "tight pass, within 0.5 of threshold")
- `#4ade80` on `#15100a` — emerald ring on deep brown — ~9.2:1 (PASS for 3:1)
- `#4ade80` on `#1e1710` — emerald primary on card — ~8.7:1 (PASS for 3:1)
- `#052e16` on `#4ade80` — primary-foreground on primary — ~8.55:1 (unchanged from slate version, primary tokens preserved)
- `#332618` on `#15100a` — border on bg — ~1.5:1 (FAIL for 3:1, matching the prior slate border pair which was also FAIL 1.72:1 — continues existing WCAG 1.4.11 decorative-border caveat from §9.1 item 3)

Update the table rows and add a short paragraph after the table:
> **Kimbie adoption note (2026-04-20):** All dark-mode rows above were recomputed after the Kimbie Dark palette swap (quick-260420-kd0). The `--primary` / `--primary-foreground` / `--ring` pairs are UNCHANGED because those tokens were preserved. Net delta vs. the prior slate-based dark mode: the `muted-foreground/background` pair tightens from 6.96:1 to ~4.8:1 (still passing but much closer to the 4.5:1 threshold — flagged for monitoring), and `border/background` continues to fail 3:1 (prior 1.72:1 → new ~1.5:1), carrying the existing §9.1 item 3 caveat (decorative hairline exemption under WCAG 1.4.11). No new hard failures introduced.

Update §9.1 Failure Register if any ratio flips from PASS to FAIL. Expected: no flips. If a flip occurs, add a new numbered item describing it and proposing remediation (brighten fg, darken bg, or accept with documented rationale).

Executor: if `scripts/verify-wcag.mjs` exists and runs, use its output as authoritative and replace the planner's estimates in the table. If it does not exist, the planner's estimates are the best-effort source and should be annotated as "(planner-estimated; re-verify when script is available)".

After edit:
1. Save file
2. Commit atomically: `docs(quick-260420-kd0): align DESIGN.md dark-mode sections with Kimbie palette`
  </action>
  <verify>
    <automated>grep -q '#15100a' DESIGN.md &amp;&amp; grep -q 'Kimbie' DESIGN.md &amp;&amp; ! grep -q 'slate-900/800/700' DESIGN.md</automated>
  </verify>
  <done>
- `DESIGN.md §1` dark-mode paragraph no longer mentions "slate-900/800/700"; new phrasing describes Kimbie warm earth tones
- `DESIGN.md §1` key-characteristics bullet updated to reference Kimbie
- `DESIGN.md §2 Neutrals` has a "Dark-mode exception" note pointing to the Kimbie table
- `DESIGN.md §2 Semantic colors` dark rows match `--semantic-*` values in `app/styles/shadcn-ui.css`
- `DESIGN.md §2 Dark mode foundation tokens` table values match `.dark` block in `app/styles/shadcn-ui.css` value-for-value
- `DESIGN.md §2` has a chart-token note listing the 5 repalettized dark chart values
- `DESIGN.md §8` derivation table updated; PRESERVED rows explicit for the 4 brand tokens
- `DESIGN.md §9` WCAG table has recomputed dark-mode rows; Kimbie adoption note appended
- `DESIGN.md §9.1` updated if any pair flipped PASS↔FAIL, otherwise unchanged
- No occurrences of the string "slate-900/800/700" remain in `DESIGN.md`
- Atomic commit created
  </done>
</task>

<task type="auto">
  <name>Task 3: Spot-check UI-RULES.md for color-temperature references</name>
  <files>UI-RULES.md</files>
  <action>
Scan `UI-RULES.md` for any color-temperature language that describes dark mode as cool/slate/blue-ish. Based on the planner's pre-scan of the file (114 lines total), the following specific locations need attention:

**A. §Tables > Table theme (lines 17-53) — AG Grid dark-mode hex table**

The table at lines 34-43 hardcodes slate-based dark-mode AG Grid hex values (`#1e293b`, `#0f172a`, `#334155`, `#f8fafc`, `#cbd5e1`, `#4ade80`, `#334155`). These are authoritative for the TS file `app/components/ag-grid/ag-grid-theme.ts`, which is OUT OF SCOPE for this plan (see `<scope_notes>` above — no TS edits).

**Do NOT change the hex values in the UI-RULES.md table.** Instead, append a short note directly below the "Dark mode" subsection (after line 43, before the "Shared (both modes)" header):

> **Note (2026-04-20, quick-260420-kd0):** The app-wide dark palette was swapped to Kimbie Dark (see `DESIGN.md §2`). The AG Grid dark theme above intentionally still uses slate-based hex values because `themeQuartz.withParams()` does not resolve CSS vars in AG Grid v35, so these values are hardcoded in `app/components/ag-grid/ag-grid-theme.ts` and asserted by `app/components/ag-grid/__tests__/ag-grid-theme.test.ts`. Syncing the AG Grid theme to Kimbie requires a paired TS + test edit and is tracked as a separate follow-up task.

This single note both (a) acknowledges the known visual mismatch between the rest of the app (Kimbie) and AG Grid (slate) so reviewers aren't surprised, and (b) gives future maintainers a breadcrumb to the paired TS file.

**B. Rest of file (lines 1-16, 44-114)**

Scan for any prose that describes dark mode as "cool", "slate", "blue-ish", "cold". If found, update to neutral phrasing or to reference Kimbie. Based on the pre-scan: no such references exist — the file is almost entirely behavior rules (tables, search, filters, detail views, form fields, layout, floating create button) and color-usage rules that reference green/red/neutral conceptually, not specific palette hues.

If, after scanning, no other edits are needed beyond the AG Grid note in (A), that is the expected outcome. Record this in the commit message and SUMMARY.

After edit:
1. Save file
2. Commit atomically: `docs(quick-260420-kd0): note Kimbie/AG-Grid dark-mode mismatch in UI-RULES`
  </action>
  <verify>
    <automated>grep -q 'Kimbie Dark' UI-RULES.md &amp;&amp; grep -q 'quick-260420-kd0' UI-RULES.md</automated>
  </verify>
  <done>
- `UI-RULES.md §Tables > Table theme > Dark mode` subsection has a note explaining the Kimbie/AG-Grid mismatch and pointing to the out-of-scope TS file + its test
- The AG Grid dark-mode hex values themselves are UNCHANGED (slate values remain, matching the TS file on disk)
- No other sections of `UI-RULES.md` contain cool/slate/blue-ish dark-mode prose (confirmed via scan; note in SUMMARY if any were updated)
- Atomic commit created
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run visual verification:

1. **Light mode unchanged (HARD REQUIREMENT):**
   - `git diff HEAD~3 -- app/styles/shadcn-ui.css` — diff MUST show zero changes inside the `:root { ... }` block (lines 11-79). Only the `.dark { ... }` block should have changes.
   - `pnpm dev` → load app in light mode → visually confirm it looks identical to pre-change (slate canvas, white cards, green CTA).

2. **Dark mode shows Kimbie:**
   - Toggle to dark mode → confirm:
     - Canvas is warm brown (`#15100a`), NOT cool slate (`#0f172a`)
     - Cards lift slightly to `#1e1710`
     - Body text reads as warm cream (`#d3af86`), NOT slate-50
     - Muted text reads as Kimbie tan (`#a57a4c`)
     - Sidebar background matches canvas (`#15100a`)
     - Focus rings, primary CTA, Aloha logo gradient ALL still render as Aloha emerald — brand identity intact

3. **Brand preservation:**
   - Hover / focus any primary button → ring is `#4ade80` (emerald)
   - Primary CTA background is `linear-gradient(135deg, #4ade80, #10b981)` (unchanged)
   - Aloha logo in navbar still reads as emerald gradient

4. **AG Grid mismatch (expected):**
   - Open any list view (Employees, Housing, Payroll) in dark mode → AG Grid tables render with slate-900/800/700 chrome (NOT Kimbie warm). This is the documented out-of-scope mismatch; follow-up task will resolve.

5. **Docs alignment:**
   - `DESIGN.md §2` dark foundation tokens table values match `app/styles/shadcn-ui.css .dark` block token-for-token
   - `DESIGN.md §9` WCAG dark rows have updated ratios
   - `UI-RULES.md` has the Kimbie/AG-Grid mismatch note

6. **Automated:**
   - `pnpm typecheck` — passes (CSS + markdown only, no TS changes)
   - `pnpm lint:fix` — clean
   - `pnpm format:fix` — clean
</verification>

<success_criteria>
- `app/styles/shadcn-ui.css` `.dark` block matches canonical Kimbie palette character-for-character on all non-preserved tokens; `--primary`, `--primary-foreground`, `--ring`, `--gradient-primary` are byte-identical to prior commit
- `app/styles/shadcn-ui.css` `:root` block (lines 11-79) is byte-identical to prior commit (HARD REQUIREMENT)
- `DESIGN.md` contains zero "slate-900/800/700 dark palette" phrasing; dark-mode sections describe Kimbie warm earth tones
- `DESIGN.md §2` dark foundation tokens table values match `shadcn-ui.css .dark` exactly
- `DESIGN.md §9` WCAG pairs table regenerated with Kimbie values; any new failures documented in §9.1 with proposed remediation (expected: no new hard failures)
- `UI-RULES.md` has a Kimbie/AG-Grid mismatch note in the Table theme subsection
- Three atomic commits created (one per task), each with `quick-260420-kd0` scope
- `pnpm typecheck`, `pnpm lint:fix`, `pnpm format:fix` all pass
- Known out-of-scope: AG Grid TS theme still renders slate dark (documented; follow-up task slug suggested: `kd0-aggrid-dark-sync`)
- Known unrelated in-flight: `ops_task_weekly_schedule` tz bug (#13) touches SQL + React renderer — no CSS overlap, no conflict
</success_criteria>

<output>
After completion, create `.planning/quick/260420-kd0-adopt-kimbie-dark-app-wide/260420-kd0-SUMMARY.md` with:
- Three commit SHAs (one per task)
- Confirmation of `:root` byte-identical preservation
- Confirmation of four preserved brand tokens unchanged
- WCAG delta table (prior ratio → new ratio for dark pairs)
- Any UI-RULES.md prose edits beyond the AG Grid note (expected: none)
- Explicit flag of the AG Grid dark-mode mismatch as known-and-deferred
- Proposed follow-up: `kd0-aggrid-dark-sync` quick task to sync AG Grid TS theme + test to Kimbie
</output>
