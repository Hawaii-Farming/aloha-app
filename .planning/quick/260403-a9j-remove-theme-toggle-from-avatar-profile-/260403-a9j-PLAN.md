---
phase: quick
plan: 260403-a9j
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/user-profile-dropdown.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Avatar/profile dropdown no longer contains a theme toggle option"
    - "Theme toggle in navbar still works independently"
    - "No unused imports remain in the file"
  artifacts:
    - path: "app/components/user-profile-dropdown.tsx"
      provides: "Profile dropdown without theme toggle"
  key_links: []
---

<objective>
Remove the redundant theme toggle from the avatar/profile dropdown menu.

Purpose: A standalone ModeToggle button was added to the top navbar, making the theme toggle inside the avatar dropdown redundant.
Output: Cleaned up UserProfileDropdown component with no theme-related code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/user-profile-dropdown.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove theme toggle from UserProfileDropdown</name>
  <files>app/components/user-profile-dropdown.tsx</files>
  <action>
In `app/components/user-profile-dropdown.tsx`:

1. Remove the `Palette` import from the `lucide-react` import line (keep `LogOut`, `Settings`, `Users`).
2. Remove the `import { useTheme } from 'next-themes'` import entirely.
3. Remove the `const { setTheme, theme } = useTheme()` line from inside the component.
4. Remove the entire theme toggle block (lines 66-73):
   ```
   {featuresFlagConfig.enableThemeToggle ? (
     <DropdownMenuItem onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}>
       <Palette className="mr-2 h-4 w-4" />
       <Trans i18nKey={'common:theme'} />
     </DropdownMenuItem>
   ) : null}
   ```
5. Also remove the `featuresFlagConfig` import (`import featuresFlagConfig from '~/config/feature-flags.config'`) ONLY if it is not used anywhere else in the file after removing the theme toggle block. Check for other usages first.
6. Run `pnpm typecheck` and `pnpm lint:fix` to confirm no errors.
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck && pnpm lint:fix</automated>
  </verify>
  <done>
- UserProfileDropdown has no theme toggle menu item
- No unused imports (Palette, useTheme, featuresFlagConfig if unused)
- TypeScript compiles without errors
- Lint passes
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm lint:fix` passes
- Grep for "useTheme\|Palette\|setTheme" in user-profile-dropdown.tsx returns no results
</verification>

<success_criteria>
- The avatar/profile dropdown menu no longer shows a theme toggle option
- The file has no unused imports related to theming
- TypeScript and lint checks pass
</success_criteria>

<output>
After completion, create `.planning/quick/260403-a9j-remove-theme-toggle-from-avatar-profile-/260403-a9j-SUMMARY.md`
</output>
