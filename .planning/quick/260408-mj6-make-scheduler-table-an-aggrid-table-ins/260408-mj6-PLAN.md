---
phase: quick
plan: 260408-mj6
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260409000001_ops_task_weekly_schedule_add_names.sql
  - app/components/ag-grid/cell-renderers/schedule-day-renderer.tsx
  - app/components/ag-grid/scheduler-list-view.tsx
autonomous: true
must_haves:
  truths:
    - "Scheduler grid has checkbox column, avatar column, and employee cell with name + badges matching Register pattern"
    - "Day-of-week columns render time ranges as styled pills instead of plain text"
    - "Toolbar has column visibility dropdown, CSV export, and search alongside existing week nav and dept filter"
    - "Column state persists to localStorage and restores on reload"
  artifacts:
    - path: "supabase/migrations/20260409000001_ops_task_weekly_schedule_add_names.sql"
      provides: "View update exposing first_name, last_name, preferred_name"
    - path: "app/components/ag-grid/cell-renderers/schedule-day-renderer.tsx"
      provides: "Custom cell renderer for day-of-week time range pills"
    - path: "app/components/ag-grid/scheduler-list-view.tsx"
      provides: "Enhanced scheduler grid with Register-matching features"
  key_links:
    - from: "scheduler-list-view.tsx"
      to: "employee-cell-renderer.tsx"
      via: "EmployeeCellRenderer in colDefs"
      pattern: "EmployeeCellRenderer"
    - from: "scheduler-list-view.tsx"
      to: "schedule-day-renderer.tsx"
      via: "ScheduleDayRenderer in day colDefs"
      pattern: "ScheduleDayRenderer"
    - from: "scheduler-list-view.tsx"
      to: "column-state.ts"
      via: "saveColumnState/restoreColumnState"
      pattern: "saveColumnState|restoreColumnState"
---

<objective>
Enhance the scheduler AG Grid table to match the Register's visual quality and feature set: checkbox selection column, avatar + employee cell renderer with name/badges, styled day-of-week pill renderers, column visibility dropdown, CSV export, search toolbar, and column state persistence.

Purpose: The scheduler grid currently renders plain text for employee names and day columns. This brings it to visual parity with the Register grid's polished look and feature set.
Output: Enhanced scheduler-list-view.tsx with all Register-pattern features, plus a new ScheduleDayRenderer and a SQL migration for name fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/ag-grid/ag-grid-list-view.tsx (Register pattern reference)
@app/components/ag-grid/scheduler-list-view.tsx (current implementation)
@app/components/ag-grid/cell-renderers/employee-cell-renderer.tsx
@app/components/ag-grid/cell-renderers/badge-cell-renderer.tsx
@app/components/ag-grid/cell-renderers/avatar-renderer.tsx
@app/components/ag-grid/column-state.ts
@app/components/ag-grid/csv-export-button.tsx
@app/components/ag-grid/row-class-rules.ts

<interfaces>
From ag-grid-list-view.tsx (Register):
- CHECKBOX_COL: ColDef with headerCheckboxSelection, checkboxSelection, pinned left, maxWidth 50
- AVATAR_COL: ColDef with AvatarRenderer, pinned left, 60px
- ColumnVisibilityDropdown: component accepting { gridApi: GridApi | null, colDefs: ColDef[] }
- Column state: onGridReady -> restoreColumnState, onColumnMoved/Resized/SortChanged/Visible -> debouncedSaveState
- DataTableToolbar from @aloha/ui/data-table-toolbar with searchValue, onSearchChange, actionSlot

From employee-cell-renderer.tsx:
- Expects row data: { first_name, last_name, preferred_name, gender, wc, hr_department_name, hr_work_authorization_name }
- The scheduler view currently has department_name and work_authorization_name (not hr_ prefixed) — need field aliases in colDef or view update

From badge-cell-renderer.tsx:
- BadgeCellRenderer for task column — hash-based colored pill

From column-state.ts:
- saveColumnState(subModuleSlug, api), restoreColumnState(subModuleSlug, api)

From ops_task_weekly_schedule view:
- Currently exposes: full_name, hr_employee_id, profile_photo_url, department_name, work_authorization_name, task, sunday-saturday, total_hours, is_over_ot_threshold
- Need to ADD: first_name, last_name, preferred_name (already in GROUP BY, just not selected)
- Need to ALIAS: department_name -> hr_department_name, work_authorization_name -> hr_work_authorization_name (to match EmployeeCellRenderer interface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SQL view and create ScheduleDayRenderer</name>
  <files>
    supabase/migrations/20260409000001_ops_task_weekly_schedule_add_names.sql
    app/components/ag-grid/cell-renderers/schedule-day-renderer.tsx
  </files>
  <action>
1. Create migration `20260409000001_ops_task_weekly_schedule_add_names.sql` that does `CREATE OR REPLACE VIEW ops_task_weekly_schedule AS ...` copying the existing view definition from `20260408000001_update_ops_task_weekly_schedule_view.sql` but adding these SELECT columns:
   - `e.first_name` (already in GROUP BY)
   - `e.last_name` (already in GROUP BY)
   - `e.preferred_name` (add to GROUP BY)
   - Rename `department_name` alias to `hr_department_name` and `work_authorization_name` alias to `hr_work_authorization_name` so the output matches EmployeeCellRenderer's expected interface
   - Keep `full_name` as well for backward compat

2. Create `schedule-day-renderer.tsx` — a custom AG Grid cell renderer for day-of-week columns:
   - Import `CustomCellRendererProps` from `ag-grid-react`
   - If value is null/empty, render a subtle dash (`<span className="text-muted-foreground/40">-</span>`)
   - If value is a time range string like "09:00 - 17:00", render it as a styled pill:
     - Use Tailwind classes for a compact pill: `inline-flex items-center rounded-md px-2 py-0.5 text-[11px] font-medium`
     - Background: `bg-primary/10` with `text-primary` for the text (using theme tokens, works dark+light)
     - Calculate hours from the time range: parse "HH:MM - HH:MM", compute difference. If >= 8 hours, use `bg-primary/10 text-primary`. If < 6 hours (short shift), use `bg-amber-500/10 text-amber-600 dark:text-amber-400`. Otherwise use `bg-muted text-muted-foreground`.
   - Export as named export `ScheduleDayRenderer`

After creating the migration, run `pnpm supabase:reset` to apply it.
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm supabase:reset && pnpm typecheck</automated>
  </verify>
  <done>SQL view returns first_name, last_name, preferred_name, hr_department_name, hr_work_authorization_name. ScheduleDayRenderer renders time range pills with color coding by shift length.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance scheduler-list-view with Register patterns</name>
  <files>
    app/components/ag-grid/scheduler-list-view.tsx
  </files>
  <action>
Rewrite the schedule-mode column definitions and toolbar in scheduler-list-view.tsx to match the Register (ag-grid-list-view.tsx) pattern. Keep the existing week navigation, department filter, history toggle, detail rows, and create panel logic intact.

**Add imports:**
- `EmployeeCellRenderer` from `~/components/ag-grid/cell-renderers/employee-cell-renderer`
- `BadgeCellRenderer` from `~/components/ag-grid/cell-renderers/badge-cell-renderer`
- `ScheduleDayRenderer` from `~/components/ag-grid/cell-renderers/schedule-day-renderer`
- `CsvExportButton` from `~/components/ag-grid/csv-export-button`
- `saveColumnState`, `restoreColumnState` from `~/components/ag-grid/column-state`
- `DataTableToolbar` from `@aloha/ui/data-table-toolbar`
- `Columns3` from `lucide-react`
- `DropdownMenu`, `DropdownMenuCheckboxItem`, `DropdownMenuContent`, `DropdownMenuTrigger` from `@aloha/ui/dropdown-menu`
- AG Grid types: `GridApi`, `GridReadyEvent`, `ColumnMovedEvent`, `ColumnResizedEvent`, `SortChangedEvent`, `ColumnVisibleEvent`, `SelectionChangedEvent`

**Add state:**
- `gridApi` via useState (set in onGridReady)
- `searchValue` for quick filter
- `saveDebounceRef` and `searchDebounceRef` as useRef

**Add CHECKBOX_COL and AVATAR_COL** constants (same as ag-grid-list-view.tsx):
- CHECKBOX_COL: headerCheckboxSelection, checkboxSelection, maxWidth 50, pinned left, lockPosition true
- AVATAR_COL: AvatarRenderer, profile_photo_url field, 60px, pinned left, lockPosition true

**Update colDefs** (schedule mode):
- Remove the standalone avatar and full_name/department/work_auth columns
- Insert CHECKBOX_COL first, then AVATAR_COL
- Add Employee column: field `first_name`, cellRenderer `EmployeeCellRenderer`, minWidth 180, sortable true, filter true, pinned left — this renders multi-line name + department badge + work auth badge automatically from the row data
- Add Task column: field `task`, cellRenderer `BadgeCellRenderer`, minWidth 120, sortable true, filter true
- Day columns (Sun-Sat): add `cellRenderer: ScheduleDayRenderer` to each, keep minWidth 100
- Total Hrs: keep as-is with numericColumn type

**Extract `dataColDefs`** (columns after CHECKBOX_COL and AVATAR_COL) into a separate useMemo for the ColumnVisibilityDropdown.

**Add column state persistence** (same pattern as ag-grid-list-view.tsx):
- `handleGridReady`: set gridApi, call `restoreColumnState('scheduler', api)`
- `debouncedSaveState`: debounced `saveColumnState('scheduler', api)` with 300ms timeout
- `handleColumnMoved`, `handleColumnResized`, `handleSortChanged`, `handleColumnVisible`: call debouncedSaveState
- Pass all these as props to the schedule-mode AgGridWrapper

**Add row selection:**
- `rowSelection="multiple"`, `suppressRowClickSelection={true}` on AgGridWrapper
- `onSelectionChanged` handler that captures selected IDs (for future bulk actions)

**Add quickFilterText** prop to AgGridWrapper bound to `searchValue`.

**Restructure toolbar:**
Replace the current custom toolbar div with a two-row layout:
- Top row (keep as-is): Week navigation (prev/today/next + week label) on left, dept filter + history toggle + create button on right
- Below that: `DataTableToolbar` with search, actionSlot containing ColumnVisibilityDropdown and CsvExportButton. Do NOT include showInactive toggle (scheduler has no inactive concept). The search placeholder should be "Search scheduler...".

**Create ColumnVisibilityDropdown** inline (copy from ag-grid-list-view.tsx) or import if it gets extracted — for now, copy the component inline within the same file (same pattern as Register).

**Row height:** Increase default row height to accommodate the multi-line EmployeeCellRenderer. Set `rowHeight={52}` on AgGridWrapper for schedule mode (detail rows keep their 200px via getRowHeight).

Run `pnpm format:fix && pnpm lint:fix` after changes.
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck && pnpm format:fix && pnpm lint:fix</automated>
  </verify>
  <done>Scheduler grid renders with checkbox column, avatar, multi-line employee cell with badges, BadgeCellRenderer on task, ScheduleDayRenderer pills on day columns, column visibility dropdown, CSV export, search bar, and column state persistence. Week nav, dept filter, history, detail rows, and create panel still work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. The SQL view is read-only and already secured by RLS via hr_employee membership. No new endpoints or user inputs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I (Info Disclosure) | ops_task_weekly_schedule view | accept | View already filtered by org_id + hr_employee RLS; adding first_name/last_name exposes no new data beyond what full_name already showed |
</threat_model>

<verification>
1. `pnpm supabase:reset` completes without errors (migration applies cleanly)
2. `pnpm typecheck` passes
3. `pnpm dev` — navigate to scheduler submodule, verify:
   - Checkbox column appears pinned left
   - Avatar column shows profile photos
   - Employee column shows multi-line name with department and work auth badges
   - Task column shows colored badge pill
   - Day columns show styled time range pills (color-coded by shift length) or dashes for empty
   - Column visibility dropdown toggles columns
   - CSV export downloads file
   - Search filters rows
   - Column reordering/resizing persists on page reload
   - Week navigation still works
   - Department filter still works
   - History toggle still works
   - Detail row expansion still works
   - Create button still opens side panel
   - OT warning row highlighting still works
</verification>

<success_criteria>
- Scheduler grid is visually consistent with Register grid (checkbox, avatar, employee cell with badges, badge task column)
- Day columns render as styled pills instead of plain text
- Toolbar includes search, column visibility, CSV export alongside existing scheduler controls
- Column state persists to localStorage
- All existing scheduler functionality preserved (week nav, dept filter, history, detail rows, create panel, OT warnings)
</success_criteria>

<output>
After completion, create `.planning/quick/260408-mj6-make-scheduler-table-an-aggrid-table-ins/260408-mj6-SUMMARY.md`
</output>
