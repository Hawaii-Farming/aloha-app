---
phase: quick
plan: 260403-a68
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/ai/ai-chat-button.tsx
  - app/components/workspace-navbar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "AI button in navbar uses a Zap (lightning bolt) icon instead of Lightbulb"
    - "Theme toggle button appears in the navbar next to the AI button"
    - "Theme toggle opens a dropdown with light/dark/system options"
  artifacts:
    - path: "app/components/ai/ai-chat-button.tsx"
      provides: "AI chat button with Zap icon"
      contains: "Zap"
    - path: "app/components/workspace-navbar.tsx"
      provides: "Navbar with theme toggle and AI button"
      contains: "ModeToggle"
  key_links:
    - from: "app/components/workspace-navbar.tsx"
      to: "@aloha/ui/mode-toggle"
      via: "import ModeToggle"
      pattern: "ModeToggle"
---

<objective>
Add a theme toggle button next to the AI button in the workspace navbar, and change the AI button icon from Lightbulb to Zap (lightning bolt).

Purpose: Give users quick access to theme switching from the navbar and use a more fitting icon for the AI assistant.
Output: Updated navbar with both theme toggle and updated AI button icon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/components/workspace-navbar.tsx
@app/components/ai/ai-chat-button.tsx
@packages/ui/src/kit/mode-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change AI button icon to Zap and add ModeToggle to navbar</name>
  <files>app/components/ai/ai-chat-button.tsx, app/components/workspace-navbar.tsx</files>
  <action>
In `app/components/ai/ai-chat-button.tsx`:
- Replace `Lightbulb` import with `Zap` from `lucide-react`
- Replace `<Lightbulb className="size-4" />` with `<Zap className="size-4" />`
- Keep the X icon for the open/close toggle unchanged

In `app/components/workspace-navbar.tsx`:
- Import `ModeToggle` from `@aloha/ui/mode-toggle`
- Add `<ModeToggle />` in the right-side `div` (the one with `gap-3`), positioned between `<NavbarSearch />` and `<AiChatButton />`
- Apply `className="size-8 rounded-full text-muted-foreground"` to `ModeToggle` to match the AI button styling (the ModeToggle component accepts a `className` prop that is passed to its trigger Button)
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck</automated>
  </verify>
  <done>
- AI button displays Zap icon (lightning bolt) instead of Lightbulb
- ModeToggle appears in the navbar between search and AI button
- Both buttons have consistent styling
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Visual: navbar right side shows Search | ThemeToggle | AI (Zap icon) | Profile
</verification>

<success_criteria>
- Zap icon renders on the AI button
- Theme toggle dropdown works (light/dark/system) from navbar
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260403-a68-add-theme-button-next-to-ai-button-with-/260403-a68-SUMMARY.md`
</output>
