---
id: 260430-k88
type: quick
status: complete
date: 2026-04-30
commit: 75b7e8f
---

# Quick Task 260430-k88 — Summary

## What changed

| Area | Change |
|------|--------|
| `app/components/ag-grid/housing-map-view.tsx` | New `vacancy` field on `AccommodationRow` (`max(0, capacity − tenantCount)`) plus optional `isTotal` flag. New "Vacancy" numericColumn between Baths and Occupancy. `rowData` builder appends a TOTAL summary row that sums beds, baths, tenantCount, capacity, vacancy. `HomeIconRenderer` returns null on the TOTAL row; `OccupancyCellRenderer` switches its bar fill to solid `bg-foreground` on TOTAL. `handleRowClicked` skips navigation for TOTAL. `getRowStyle` returns bold + `var(--color-muted)` bg + `cursor: default` for TOTAL; existing inactive-opacity branch retained. |

## Verification

- `pnpm typecheck` — clean
- `pnpm format:fix` — full turbo cache, no diff

## Manual checks pending

Browse `/home/<account>/human_resources/housing`:
- Vacancy column appears with numeric values per row.
- A bold TOTAL row sits at the bottom with summed numbers.
- TOTAL row has no home-icon badge, doesn't navigate on click, occupancy bar is full-strength color.

## Out of scope

- No changes to detail view or housing-rooms grid.
- Vacancy is computed client-side from capacity/tenantCount; no DB column or view change.
