---
id: 260430-k88
type: quick
status: planned
created: 2026-04-30
---

# Quick Task 260430-k88: Housing list — bed totals + vacancy

## Scope

On the Housing list AG Grid (`HousingMapView`):
- Add a Vacancy column = `capacity − tenantCount` (clamped ≥ 0).
- Append a TOTAL row at the bottom summing beds, baths, tenantCount, capacity, and vacancy.
- Style the TOTAL row bold + muted background, suppress the home-icon badge and disable row click navigation for it.

## Tasks

### T1 — Vacancy column + TOTAL row

- File: `app/components/ag-grid/housing-map-view.tsx`
- Add `vacancy: number` and `isTotal?: boolean` to `AccommodationRow`.
- New ColDef before Occupancy: `{ field: 'vacancy', headerName: 'Vacancy', type: 'numericColumn', flex: 1, minWidth: 110 }`.
- `rowData` builder computes `vacancy = max(0, capacity − tenantCount)` per accommodation, then appends a TOTAL row summing each numeric field.
- `HomeIconRenderer` returns null for `isTotal` rows (no avatar in summary line).
- `OccupancyCellRenderer` swaps the bar color from `bg-foreground/70` → `bg-foreground` on the TOTAL row.
- `handleRowClicked` early-returns when `row.isTotal`.
- `getRowStyle` returns `{ fontWeight: 'bold', background: 'var(--color-muted)', cursor: 'default' }` on the TOTAL row; existing inactive opacity branch retained.

## must_haves

- Housing list shows a Vacancy column populated per accommodation.
- A bold TOTAL row appears at the bottom with summed beds, baths, tenantCount, capacity, vacancy.
- TOTAL row is non-clickable and has no home-icon badge.
- `pnpm typecheck` clean.
