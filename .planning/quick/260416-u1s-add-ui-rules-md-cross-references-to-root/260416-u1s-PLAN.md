---
phase: quick-260416-u1s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - U1S-01
  - U1S-02
  - U1S-03

must_haves:
  truths:
    - "Root CLAUDE.md `## Design System` section mentions UI-RULES.md as the behavior/structure companion to DESIGN.md."
    - "Root CLAUDE.md `## Data Tables` bullets include a reference to UI-RULES.md §Tables for stripped-chrome rules."
    - "UI-RULES.md itself is unchanged."
    - "DESIGN.md is unchanged."
    - "No code or logic files are modified — CLAUDE.md is the only edit."
    - "`origin/design` has the new commit pushed so PR #11 picks it up."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Cross-references to UI-RULES.md in Design System section + Data Tables section"
      contains: "UI-RULES.md"
  key_links:
    - from: "CLAUDE.md §Design System"
      to: "UI-RULES.md"
      via: "inline sentence after DESIGN.md source-of-truth line"
      pattern: "UI-RULES\\.md"
    - from: "CLAUDE.md §Data Tables"
      to: "UI-RULES.md §Tables"
      via: "early bullet referencing stripped-chrome rules"
      pattern: "UI-RULES\\.md.*§?Tables"
---

<objective>
Wire the two existing companion docs together in root `CLAUDE.md` so that when Claude reads
project instructions it is pointed at `UI-RULES.md` (behavior + structure) alongside
`DESIGN.md` (tokens). Doc-only, no code changes.

Purpose: Close the loop on the design-branch doc alignment work — `DESIGN.md` already
cross-references `UI-RULES.md` (see §1 "Companion document" callout at DESIGN.md:23 and
§3 / §10 mentions at DESIGN.md:149, 161, 317). `CLAUDE.md` is the one remaining entry point
that doesn't surface `UI-RULES.md`, so a Claude session that only reads `CLAUDE.md` can miss
the table/search/filter/detail-view rules entirely.

Output: Two small additions in `CLAUDE.md` — one sentence in `## Design System`, one bullet
in `## Data Tables`. Nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@UI-RULES.md
@DESIGN.md

<interfaces>
<!-- Anchor text to match in CLAUDE.md so edits land in the right place. -->
<!-- Do NOT rewrite surrounding prose; only insert the new sentence/bullet. -->

Target 1 — `## Design System` section (CLAUDE.md around line 127-129):

```md
## Design System

The current project is a comprehensive retheme adopting a Supabase-inspired design system. **`DESIGN.md` is the source of truth** for all visual decisions — colors, typography, spacing, component styling, and theme tokens. Read it before any UI/CSS work.
```

Target 2 — `## Data Tables` section (CLAUDE.md around lines 114-125):

```md
## Data Tables

**Always use AG Grid Community** for data tables — never TanStack Table, HTML tables, or other table libraries.

- **Standard CRUD grids**: Use `AgGridListView` (drop-in, handles search, CSV export, column visibility, pagination)
- **Custom grids** (filters, toggles, pinned rows): Compose `AgGridWrapper` directly for full toolbar control
- **Theming**: AG Grid is themed to DESIGN.md via `ag-grid-theme.ts` (dark/light, Supabase-inspired)
- ...
```

Existing style precedent in DESIGN.md §1 callout (copy the tone, not the exact text):

> **Companion document:** `UI-RULES.md` covers app-wide **behavior and structure** rules (tables, search, filters, detail views, form fields, layout). This file (`DESIGN.md`) covers **tokens** — colors, typography, radius, shadows. Read both when working on UI.

And the UI-RULES.md §Tables rules to cite (summarized, one bullet):

- No table search (global navbar search only)
- `defaultColDef.filter = false`
- No coloring / no semantic cell tints
- One datum per cell (no compound renderers)
- Flat `ColDef[]` only (no group/super headers)
- Uniform `text-sm`, ghost zebra fill, pinned TOTAL rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UI-RULES.md companion pointer to `## Design System` in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Edit `CLAUDE.md`. Locate the `## Design System` section (currently line 127). After the
existing sentence that ends "Read it before any UI/CSS work." append one blank line and a
single new sentence pointing at `UI-RULES.md` as the behavior/structure companion.

Match the tone of DESIGN.md's existing companion callout (DESIGN.md §1, around line 23) —
mirror phrasing ("covers behavior and structure", "tables, search, filters, detail views,
form fields, layout") but keep it to one sentence, no block-quote formatting, no heading.

Exact insertion — use the Edit tool with this old_string / new_string pair:

old_string:
```
## Design System

The current project is a comprehensive retheme adopting a Supabase-inspired design system. **`DESIGN.md` is the source of truth** for all visual decisions — colors, typography, spacing, component styling, and theme tokens. Read it before any UI/CSS work.
```

new_string:
```
## Design System

The current project is a comprehensive retheme adopting a Supabase-inspired design system. **`DESIGN.md` is the source of truth** for all visual decisions — colors, typography, spacing, component styling, and theme tokens. Read it before any UI/CSS work.

**Companion:** `UI-RULES.md` covers app-wide **behavior and structure** rules — tables, search, filters, detail views, form fields, layout, and the floating create button. Read both `DESIGN.md` (tokens) and `UI-RULES.md` (behavior) before any UI work.
```

Do NOT touch the `### Constraints` sub-heading that follows, and do NOT modify any other
part of the `## Design System` block (including its bullets). Preserve all trailing
whitespace and the blank line before `### Constraints`.
  </action>
  <verify>
    <automated>grep -n "UI-RULES.md" CLAUDE.md | head -5 &amp;&amp; grep -c "Companion:" CLAUDE.md</automated>
  </verify>
  <done>
CLAUDE.md contains a new `**Companion:**` sentence immediately after the
`**DESIGN.md** is the source of truth` sentence in `## Design System`, referencing
`UI-RULES.md` with the listed behavior/structure topics. `### Constraints` still follows
below, untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add UI-RULES.md §Tables bullet near top of `## Data Tables` in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Edit `CLAUDE.md`. Locate the `## Data Tables` bullet list (currently lines 118-125). Insert
a new bullet as the **first** item in the bullet list — before `**Standard CRUD grids**` —
pointing at `UI-RULES.md §Tables` for the stripped-chrome rules.

Exact insertion — use the Edit tool with this old_string / new_string pair:

old_string:
```
**Always use AG Grid Community** for data tables — never TanStack Table, HTML tables, or other table libraries.

- **Standard CRUD grids**: Use `AgGridListView` (drop-in, handles search, CSV export, column visibility, pagination)
```

new_string:
```
**Always use AG Grid Community** for data tables — never TanStack Table, HTML tables, or other table libraries.

- **Rules**: See `UI-RULES.md §Tables` for the stripped-chrome conventions — no per-table search (navbar-only), `defaultColDef.filter = false`, no coloring, one datum per cell, flat `ColDef[]` (no group headers), uniform `text-sm`, ghost zebra fill, pinned TOTAL rows.
- **Standard CRUD grids**: Use `AgGridListView` (drop-in, handles search, CSV export, column visibility, pagination)
```

Do NOT reorder or modify any of the existing bullets below this insertion, and do NOT touch
the `## Design System` section added in Task 1 or anything below `## Data Tables`.
  </action>
  <verify>
    <automated>grep -n "UI-RULES.md §Tables" CLAUDE.md &amp;&amp; grep -n "defaultColDef.filter = false" CLAUDE.md &amp;&amp; grep -c "Always use AG Grid Community" CLAUDE.md</automated>
  </verify>
  <done>
CLAUDE.md `## Data Tables` section has a new first bullet `**Rules**: See UI-RULES.md
§Tables …` that enumerates the six stripped-chrome conventions. Existing bullets
(`Standard CRUD grids`, `Custom grids`, `Theming`, `Detail rows`, `Column mapping`,
`Cell renderers`, `Column state`, `MCP docs`) remain in their original order with their
original text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify diff scope, commit, and push `origin design`</name>
  <files>CLAUDE.md</files>
  <action>
Final guardrail + shipping step.

1. Run `git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app status --porcelain` and confirm
   `CLAUDE.md` is the **only** modified path. If anything else appears (e.g. `UI-RULES.md`,
   `DESIGN.md`, any file under `app/`, `packages/`, `supabase/`), abort and surface the
   unexpected file to the user — do NOT commit.

2. Run `git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app diff --stat CLAUDE.md` and confirm
   only a small insertion delta (roughly +4 to +6 lines, 0 deletions). No block was removed
   or reorganized.

3. Run `git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app diff CLAUDE.md` and read it to
   confirm:
   - The new `**Companion:**` sentence sits directly below the DESIGN.md source-of-truth
     sentence in `## Design System`.
   - The new `**Rules**: See UI-RULES.md §Tables …` bullet sits as the first bullet under
     `## Data Tables`, before `**Standard CRUD grids**`.
   - The existing `### Constraints` block and all pre-existing Data Tables bullets are
     unchanged.

4. Commit with exactly this command (single file add, no `-A`):

   ```bash
   cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app &amp;&amp; \
     git add CLAUDE.md &amp;&amp; \
     git commit -m "docs(quick-260416-u1s): cross-reference UI-RULES.md from root CLAUDE.md

   - Add 'Companion' pointer in ## Design System linking to UI-RULES.md (behavior + structure)
   - Add first bullet in ## Data Tables linking to UI-RULES.md §Tables (stripped-chrome rules)
   - Closes the doc-alignment loop started in quick-260416-ovx (DESIGN.md already points at UI-RULES.md)

   No code changes; UI-RULES.md and DESIGN.md untouched.

   Co-Authored-By: Claude Opus 4.7 (1M context) &lt;noreply@anthropic.com&gt;"
   ```

5. Push the `design` branch so PR #11 picks up the new commit:

   ```bash
   git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app push origin design
   ```

   If push fails (branch protection, missing upstream, auth), surface the exact error to the
   user and stop — do NOT force-push, do NOT push to main/master, do NOT amend.
  </action>
  <verify>
    <automated>git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app log -1 --name-only --pretty=format:"%h %s" | tee /dev/stderr | grep -q "quick-260416-u1s" &amp;&amp; git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app log -1 --name-only --pretty=format:"" | tr -s '\n' | grep -E "^CLAUDE\.md$" &amp;&amp; git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app status --porcelain | wc -l | grep -qx " *0" &amp;&amp; git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app rev-parse HEAD = $(git -C /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app rev-parse origin/design)</automated>
  </verify>
  <done>
- `git log -1` on `design` shows the `docs(quick-260416-u1s): …` commit.
- Commit touches exactly one file: `CLAUDE.md`.
- Working tree is clean (`git status --porcelain` returns empty).
- `origin/design` SHA equals local `design` SHA — PR #11 sees the new commit.
- UI-RULES.md and DESIGN.md remain byte-identical to their pre-task state.
  </done>
</task>

</tasks>

<verification>
Overall doc-only phase verification (run after Task 3 completes):

```bash
# 1. Only CLAUDE.md changed vs. the PR base
git diff origin/main -- CLAUDE.md | grep -E "^\+.*UI-RULES\.md" | wc -l
# expect: at least 2 (one in Design System, one in Data Tables bullet)

# 2. UI-RULES.md and DESIGN.md unchanged on this branch since the previous ovx commit
git diff origin/main -- UI-RULES.md DESIGN.md
# expect: no output (assuming ovx commit is already on origin/main for PR comparison;
# otherwise diff shows only the ovx changes, not u1s changes)

# 3. Working tree clean and remote in sync
git status --porcelain
# expect: empty
git rev-parse HEAD
git rev-parse origin/design
# expect: equal
```
</verification>

<success_criteria>
- CLAUDE.md `## Design System` section ends with a sentence referencing `UI-RULES.md`
  as the behavior/structure companion to `DESIGN.md`.
- CLAUDE.md `## Data Tables` section's first bullet references `UI-RULES.md §Tables` and
  enumerates the stripped-chrome rules (no search, `filter: false`, no coloring,
  one-datum-per-cell, flat ColDef, uniform text-sm, ghost zebra, pinned totals).
- `UI-RULES.md` byte-identical to the pre-task version.
- `DESIGN.md` byte-identical to the pre-task version.
- Single commit on `design` branch with prefix `docs(quick-260416-u1s):` modifying only
  `CLAUDE.md`.
- `origin/design` updated so PR #11 reflects the new commit.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-u1s-add-ui-rules-md-cross-references-to-root/260416-u1s-SUMMARY.md`
with:
- Confirmation that both insertions landed exactly where specified.
- The commit SHA and the confirmation that `origin/design` was pushed.
- A note that `UI-RULES.md` and `DESIGN.md` were not modified.
</output>
