---
phase: quick
plan: 260402-jf6
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/sidebar/module-sidebar-navigation.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Module names in the sidebar appear capitalized (first letter of each word uppercase)
    - Sub-module names in the sidebar appear capitalized
    - Behavior is consistent in both collapsed (icon dropdown) and expanded (collapsible) modes
  artifacts:
    - path: app/components/sidebar/module-sidebar-navigation.tsx
      provides: Sidebar navigation with capitalized display names
  key_links:
    - from: app/components/sidebar/module-sidebar-navigation.tsx
      to: display_name values from AppNavModule/AppNavSubModule
      via: Tailwind capitalize class on span elements
---

<objective>
Capitalize module and sub-module display names in the sidebar navigation.

Purpose: Module names ("human resources", "inventory") and sub-module names render without guaranteed capitalization. Adding Tailwind's `capitalize` class to the display spans ensures first-letter capitalization for every word regardless of the underlying DB value.

Output: Updated `module-sidebar-navigation.tsx` with `capitalize` on all `display_name` spans in both collapsed (dropdown) and expanded (collapsible) rendering paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capitalize class to sidebar module and sub-module name spans</name>
  <files>app/components/sidebar/module-sidebar-navigation.tsx</files>
  <action>
In `module-sidebar-navigation.tsx` there are four places where `display_name` is rendered as text:

1. **Collapsed mode — module button span** (line ~96):
   `<span>{mod.display_name}</span>`
   Change to: `<span className="capitalize">{mod.display_name}</span>`

2. **Collapsed mode — dropdown sub-module item** (line ~119):
   `{sm.display_name}` (bare text node inside `<a>`)
   Wrap in: `<span className="capitalize">{sm.display_name}</span>`

3. **Expanded mode — collapsible trigger span** (line ~148):
   `<span className="flex-1 text-left">{mod.display_name}</span>`
   Change to: `<span className="flex-1 text-left capitalize">{mod.display_name}</span>`

4. **Expanded mode — sub-module menu button span** (line ~175):
   `<span>{sm.display_name}</span>`
   Change to: `<span className="capitalize">{sm.display_name}</span>`

No other changes. Do not modify any logic, layout classes, or other elements.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>All four display_name render sites have the `capitalize` Tailwind class. `pnpm typecheck` passes with no errors.</done>
</task>

</tasks>

<verification>
- Run `pnpm typecheck` — no errors
- Run `pnpm lint:fix` — no lint warnings introduced
- Visual: Start `pnpm dev` and navigate to sidebar — module names and sub-module names render with first-letter capitalization in both expanded and collapsed sidebar states
</verification>

<success_criteria>
Sidebar module names (e.g., "Human Resources", "Inventory") and sub-module names (e.g., "Employees", "Time Off") display with capitalized first letters in both sidebar states.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-jf6-sidebar-modules-names-should-be-capitali/260402-jf6-SUMMARY.md`
</output>
