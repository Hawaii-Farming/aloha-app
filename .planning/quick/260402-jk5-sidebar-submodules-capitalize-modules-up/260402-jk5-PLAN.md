# Quick Task 260402-jk5: Submodules capitalize, modules uppercase

## Task 1: Change submodule spans from uppercase to capitalize

- **Files:** `app/components/sidebar/module-sidebar-navigation.tsx`
- **Action:** Change `uppercase` → `capitalize` on the 2 submodule `sm.display_name` spans (collapsed dropdown line 119, expanded accordion line 175). Module `mod.display_name` spans (lines 96, 148) remain `uppercase`.
- **Verify:** `pnpm typecheck` passes; grep confirms 2x uppercase (modules) + 2x capitalize (submodules)
- **Done:** Modules = ALL CAPS, Submodules = Title Case
