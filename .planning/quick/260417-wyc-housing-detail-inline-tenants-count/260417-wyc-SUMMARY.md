---
id: 260417-wyc
type: quick
status: complete
---

# Quick Task 260417-wyc — Summary

Housing detail view: moved `Tenants (N)` label into the top bar beside the house name, dropped the old body-level "TENANTS (N)" heading + Separator, and removed body padding so the tenants grid sits edge-to-edge right below the header.

Notes (when present) now renders as a compact one-line bar between the header and the grid.

File: `app/components/crud/housing-detail-view.tsx`. Unused `Separator` import removed.
