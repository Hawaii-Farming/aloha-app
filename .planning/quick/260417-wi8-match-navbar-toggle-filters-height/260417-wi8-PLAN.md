---
id: 260417-wi8
type: quick
status: complete
---

# Quick Task 260417-wi8

## Objective

Match the height of the navbar view toggle (Payroll Comp icons) and the Filters button to the search bar so all three controls share the same visual weight.

## Context

Search bar uses `py-2.5` + text-sm → ~40px tall. Toggle + Filters used `h-9` (36px). The 4px diff was visible per user screenshot.

## Changes

### app/components/navbar-filter-button.tsx
- Trigger button `h-9` → `h-10`.

### app/components/ag-grid/payroll-view-toggle.tsx
- Outer pill `h-9` → `h-10`.
- Inner icon buttons `h-8 w-8` → `h-9 w-9`.

## Verification

- `pnpm typecheck` clean.
- Visual: toggle, Filters, search bar all read as same height.
