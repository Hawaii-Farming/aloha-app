---
type: quick
id: 260402-vj5
description: Create CRUD configs for all growing submodules (seed_batches, harvests) and register them
---

<objective>
Create CRUD config files for the two growing submodules — seed_batches and harvests — and register them in the CRUD registry.

Purpose: Enable the generic CRUD factory to render list/detail/form views for growing module submodules.
Output: Two new config files + updated registry with 9 total entries.
</objective>

<context>
@app/lib/crud/types.ts
@app/lib/crud/registry.ts
@app/lib/crud/invnt-onhand.config.ts (reference pattern for uuid PK, FK fields, date fields, number fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grow-seed-batch.config.ts and grow-harvest-weight.config.ts</name>
  <files>app/lib/crud/grow-seed-batch.config.ts, app/lib/crud/grow-harvest-weight.config.ts</files>
  <action>
Create two CRUD config files following the established pattern (see invnt-onhand.config.ts).

**grow-seed-batch.config.ts** — maps to submodule slug `seed_batches`:
- tableName: `grow_seed_batch`, pkType: `uuid`, pkColumn: `id`, orgScoped: true
- views: list/detail both `grow_seed_batch`
- Zod schema fields (all from DB columns):
  - batch_code: z.string().min(1)
  - grow_seed_mix_id: z.string().optional()
  - invnt_item_id: z.string().optional()
  - seeding_uom: z.string().min(1)
  - number_of_units: z.number({ required_error: 'Required' })
  - seeds_per_unit: z.number({ required_error: 'Required' })
  - number_of_rows: z.number({ required_error: 'Required' })
  - seeding_date: z.string().min(1)
  - transplant_date: z.string().min(1)
  - estimated_harvest_date: z.string().min(1)
  - status: z.enum(['planned', 'seeded', 'transplanted', 'harvesting', 'harvested']).default('planned')
  - notes: z.string().optional()
  - farm_id: z.string().min(1)
  - site_id: z.string().optional()
  - grow_cycle_pattern_id: z.string().optional()
  - grow_trial_type_id: z.string().optional()
  - invnt_lot_id: z.string().optional()
- columns (list view): batch_code (sortable), status (type: workflow), seeding_date (date, sortable), transplant_date (date), estimated_harvest_date (date), number_of_units (number), created_at (datetime, sortable)
- search: columns ['batch_code'], placeholder 'Search seed batches...'
- filters: status as select with options ['planned', 'seeded', 'transplanted', 'harvesting', 'harvested']
- workflow config:
  - statusColumn: 'status'
  - states: planned (default), seeded (secondary), transplanted (warning), harvesting (success), harvested (success)
  - transitions: planned->['seeded'], seeded->['transplanted'], transplanted->['harvesting'], harvesting->['harvested']
- formFields: batch_code (text, required), farm_id (fk to org_farm, label column 'name', required), site_id (fk to org_site, label column 'name'), grow_seed_mix_id (fk to grow_seed_mix, label column 'name'), invnt_item_id (fk to invnt_item, label column 'name'), grow_cycle_pattern_id (fk to grow_cycle_pattern, label column 'name'), grow_trial_type_id (fk to grow_trial_type, label column 'name'), invnt_lot_id (fk to invnt_lot, label column 'id'), seeding_uom (fk to sys_uom, label column 'id', required), number_of_units (number, required), seeds_per_unit (number, required), number_of_rows (number, required), seeding_date (date, required), transplant_date (date, required), estimated_harvest_date (date, required), status (select, options from enum, required), notes (textarea)

**grow-harvest-weight.config.ts** — maps to submodule slug `harvests`:
- tableName: `grow_harvest_weight`, pkType: `uuid`, pkColumn: `id`, orgScoped: true
- views: list/detail both `grow_harvest_weight`
- Zod schema fields:
  - grow_seed_batch_id: z.string().min(1)
  - harvest_date: z.string().min(1)
  - grow_harvest_container_id: z.string().min(1)
  - number_of_containers: z.number({ required_error: 'Required' })
  - weight_uom: z.string().min(1)
  - gross_weight: z.number({ required_error: 'Required' })
  - net_weight: z.number({ required_error: 'Required' })
  - farm_id: z.string().min(1)
  - site_id: z.string().optional()
  - grow_grade_id: z.string().optional()
- columns (list view): grow_seed_batch_id (sortable), harvest_date (date, sortable), gross_weight (number, sortable), net_weight (number, sortable), weight_uom, number_of_containers (number), created_at (datetime, sortable)
- search: columns ['grow_seed_batch_id'], placeholder 'Search harvests...'
- filters: empty array
- formFields: grow_seed_batch_id (fk to grow_seed_batch, label column 'batch_code', required), farm_id (fk to org_farm, label column 'name', required), site_id (fk to org_site, label column 'name'), grow_grade_id (fk to grow_grade, label column 'name'), harvest_date (date, required), grow_harvest_container_id (fk to grow_harvest_container, label column 'id', required), number_of_containers (number, required), weight_uom (fk to sys_uom, label column 'id', required), gross_weight (number, required), net_weight (number, required)
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Both config files exist, export named configs, and pass typecheck</done>
</task>

<task type="auto">
  <name>Task 2: Register growing configs in CRUD registry</name>
  <files>app/lib/crud/registry.ts</files>
  <action>
Add imports for the two new configs at the top of registry.ts:
- import { growSeedBatchConfig } from './grow-seed-batch.config';
- import { growHarvestWeightConfig } from './grow-harvest-weight.config';

Add two entries to the registry Map (these slug keys match the sys_sub_module_id values from seed data):
- ['seed_batches', growSeedBatchConfig]
- ['harvests', growHarvestWeightConfig]

Registry should now have 9 entries total.
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>Registry has 9 entries. Navigating to /home/{account}/growing/seed_batches and /home/{account}/growing/harvests renders CRUD list views.</done>
</task>

</tasks>

<verification>
pnpm typecheck passes with no errors.
pnpm lint:fix produces no errors in the new/modified files.
</verification>

<success_criteria>
- grow-seed-batch.config.ts exists with full column/form/workflow/schema config
- grow-harvest-weight.config.ts exists with full column/form/schema config
- registry.ts maps 'seed_batches' and 'harvests' to their configs (9 total entries)
- typecheck passes cleanly
</success_criteria>
