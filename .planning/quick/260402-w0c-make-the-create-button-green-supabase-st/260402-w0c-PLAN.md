---
type: quick
task_id: 260402-w0c
description: Make the + Create button green Supabase style in sub-module list pages
files_modified:
  - packages/ui/src/shadcn/button.tsx
  - app/routes/workspace/sub-module.tsx
---

<objective>
Add a `brand` button variant using the existing `--supabase-green` CSS variable, then apply it to the Create button on sub-module list pages.

Purpose: The Create button should visually stand out with the Supabase green brand color.
Output: Green-styled Create button across all sub-module list pages.
</objective>

<context>
@packages/ui/src/shadcn/button.tsx
@app/routes/workspace/sub-module.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand variant and apply to Create button</name>
  <files>packages/ui/src/shadcn/button.tsx, app/routes/workspace/sub-module.tsx</files>
  <action>
1. In `packages/ui/src/shadcn/button.tsx`, add a `brand` variant to `buttonVariants` after the `pill` variant:
   ```
   brand: 'bg-[var(--supabase-green)] text-white hover:bg-[var(--supabase-green)]/90 shadow-sm',
   ```
   This uses the already-defined `--supabase-green` CSS variable (oklch(73.5% 0.158 162)) for both light and dark modes, with white text for contrast and a subtle shadow.

2. In `app/routes/workspace/sub-module.tsx`, find the Create button (around line 159) and change:
   ```tsx
   <Button asChild size="sm">
   ```
   to:
   ```tsx
   <Button asChild size="sm" variant="brand">
   ```
  </action>
  <verify>
    <automated>cd /Users/jmr/GitHub/JJB/HawaiiFarming/aloha-app && pnpm typecheck</automated>
  </verify>
  <done>The Create button on sub-module list pages renders with the Supabase green background and white text in both light and dark modes. The `brand` variant is available for reuse on other buttons.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- Button component exports unchanged (no breaking changes)
- Create button visually renders green on sub-module pages
</verification>

<success_criteria>
- New `brand` variant exists in buttonVariants
- Create button in sub-module.tsx uses `variant="brand"`
- Green background with white text in both themes
</success_criteria>
