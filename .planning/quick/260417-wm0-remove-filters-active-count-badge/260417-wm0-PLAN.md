---
id: 260417-wm0
type: quick
status: complete
---

# Quick Task 260417-wm0

## Objective

Remove the green active-count badge (the "1" circle) from the navbar Filters button. The active-chip summary (e.g. date range) remains — it carries enough signal that a filter is applied, and dropping the badge reclaims ~28px of horizontal width so the Filters cluster stops crowding the centered search bar.

## Change

File: `app/components/navbar-filter-button.tsx`

Delete the badge span inside the active state fragment; keep the `activeSummary` span. `activeCount` is still used by the "Clear all" link inside the popover body.

## Verification

`pnpm typecheck` clean. Data-test selectors unchanged.
