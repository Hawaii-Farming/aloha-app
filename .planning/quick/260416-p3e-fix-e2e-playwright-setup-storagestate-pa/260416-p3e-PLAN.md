---
phase: quick-260416-p3e
plan: 01
type: execute
wave: 1
files_modified:
  - e2e/playwright.config.ts
  - e2e/README.md
autonomous: true
---

<objective>
Make `cd e2e && pnpm test` work out of the box on a fresh clone. Three
surgical fixes, no logic rewrites.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix storageState path</name>
  <files>e2e/playwright.config.ts</files>
  <action>
`use.storageState: 'e2e/.auth/user.json'` → `use.storageState: '.auth/user.json'`.
The config file lives in e2e/ and tests run from that CWD, so the old path
resolved to `e2e/e2e/.auth/user.json`. auth.setup.ts writes to the correct
__dirname-based absolute path — only the config was wrong.
  </action>
</task>

<task type="auto">
  <name>Task 2: Default webServer to pnpm dev</name>
  <files>e2e/playwright.config.ts</files>
  <action>
Replace the env-gated `webServer: process.env.PLAYWRIGHT_SERVER_COMMAND ? {...} : undefined`
with an always-on webServer that defaults to `pnpm dev` and allows CI to
override via `PLAYWRIGHT_SERVER_COMMAND` (e.g. `pnpm start` for built server).
Preserve `reuseExistingServer: !process.env.CI` so an already-running dev
server is reused locally.
  </action>
</task>

<task type="auto">
  <name>Task 3: Document E2E env vars in e2e/README.md</name>
  <files>e2e/README.md</files>
  <action>
Create a new README documenting:
- How to run the suite (`cd e2e && pnpm test`)
- Required env vars (`E2E_USER_EMAIL`, `E2E_USER_PASSWORD`) and why they're
  checked in auth.setup.ts
- How auth state is captured and reused via `use.storageState`
- The CI override via `PLAYWRIGHT_SERVER_COMMAND`

Rationale for README vs .env.template: `.env.template` is gitignored via
.gitignore line 60 (`.env*`), so edits to it never reach the repo. A README
is the durable home for E2E setup docs.
  </action>
</task>

</tasks>

<verification>
- `cd e2e && pnpm exec playwright test --list` — config parses, 26 tests listed
- `pnpm typecheck` — still green
- No source files changed beyond the 2 listed
</verification>

<success_criteria>
- Config no longer has a CWD-relative path bug for storageState
- Local E2E runs auto-start `pnpm dev` (no need for manually-started server)
- CI keeps the ability to swap in a built-server command
- Required env vars are documented in e2e/README.md
- User can now run `cd e2e && pnpm test` after setting E2E_USER_EMAIL + E2E_USER_PASSWORD in .env
</success_criteria>
