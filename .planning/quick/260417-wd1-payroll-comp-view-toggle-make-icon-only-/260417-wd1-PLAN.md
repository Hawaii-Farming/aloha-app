---
id: 260417-wd1
type: quick
status: complete
---

# Quick Task 260417-wd1

## Objective

Fix Payroll Comparison view toggle visual weight in the navbar:

- Reported issue: the text "By Department / By Employee" toggle takes too much horizontal space; when the Filters button shows active summary chips, the search bar gets shifted.
- Desired: replace the text buttons with icons; move the toggle out of the filter-slot (which sits before the search bar) and into a new action slot AFTER the search bar, LEFT of the avatar.

## Tasks

### Task 1 — add right-side action slot in navbar

File: `app/components/workspace-shell/workspace-navbar.tsx`

Add `<div id="workspace-navbar-action-slot">` between `<NavbarSearch>` and `<WorkspaceNavbarProfileMenu>` (same styling pattern as `workspace-navbar-filter-slot`).

### Task 2 — rewrite PayrollViewToggle

File: `app/components/ag-grid/payroll-view-toggle.tsx`

- Portal target changes from `#workspace-navbar-filter-slot` → `#workspace-navbar-action-slot`.
- Replace text labels with Lucide icons: `Users` for By Department, `User` for By Employee.
- Buttons become square icon buttons (`h-8 w-8`). Outer pill stays the same shape (`h-9 rounded-full border`).
- `aria-label` + `title` attributes preserve accessibility and hover tooltip.
- Preserve all three data-test selectors: `payroll-view-toggle`, `view-toggle-by-task`, `view-toggle-by-employee`.

## Verification

- `pnpm typecheck` passes.
- Visual check: Payroll Comparison page shows [🧑 🧑🧑] icon toggle between search bar and avatar; Filters button and search remain aligned regardless of active-filter chip width.
